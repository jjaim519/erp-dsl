# 03. 로드맵과 미해결 (어디로 가는가)

> 본 문서는 ERP No-Code 빌더 프로젝트의 **참조 문서**이다. 상위 원칙·규칙·색인은 「프로젝트 지침」을 따른다.
> 본 문서는 작업 순서·진행 상황, "올바른 방식" 문제, 미해결 지점을 다룬다. 가장 빈번히 갱신된다.

---

## 작업 순서 (로드맵)

1. ✅ 토큰 세트 확정 (theme.ts, mantine.d.ts)
2. ✅ **검증** — `npx tsc --noEmit` EXIT=0. 인터페이스 이름(`MantineThemeColorsOverride`/`MantineThemeOther`/`MantineThemeSizesOverride`) 전부 정상 확인.
3. ✅ **첫 부품 + 강제 인프라** — Button 래퍼(그물 1) + eslint 두 문(그물 2) 구현·probe 검증 완료. 별도 DSL 라이브러리 레포로 분리(특정 ERP 레포 아님).
4. ✅ **원자 분류·기준 확정** — 의미/레이아웃 원자 + 배치 프리미티브 구분, 1차 원자 집합 목록(「02. 토큰과 구현」), 판별 기준·경계 닫기 규칙·크기(밀도)·공간 장부(「01. 규칙과 구조」).
5. ✅ 의미 원자·레이아웃·프리미티브 prop 규격 확정 — 의미 원자 22 + 레이아웃 3 + 프리미티브 3, 개별 표까지(→ 「02」 원자 prop 규격 절). 입력군 3골격 + 표시·텍스트 3묶음 + 횡단 규칙 4개로 수렴(error를 입력 원자에서 제거하며 "error는 쪼갠다" 규칙이 사라져 5→4). **→ 22 원자 전부 래퍼 구현·검증 완료(tsc·eslint·강제 발화). 색·타이포·테두리는 "역할 변수 통로"(CSS 변수)로 통일(→ 「02」 역할 변수 통로 절).** (Stage5에서 Popover·Image 추가 → 의미 원자 **24**. 22는 Stage4 시점 수치.)
6. ✅ 첫 분자 — FormField 확정 — 방식 A(컨트롤을 children으로). 장식(label·withAsterisk) + 에러 표현(메시지 + 테두리)을 독점. 입력칸은 에러를 모르고, 테두리는 CSS 역할 변수(--field-border)로 FormField가 덮음(→ 「02」 분자 prop 규격). "분자가 상태를 고정한다"의 첫 사례. (description은 placeholder와 경쟁 경로라 미채택 — 「02」.) **구현·검증 완료.**
6-A. ✅ **일반 분자 확보 (kk 무관, 압도적 우선)** — 02가 적어둔 분자 후보부터: MultiSelect(Select+Chip), 로딩버튼(Button+Spinner), FileUploader, DateRangeField 등. 분자까지는 어떤 ERP에도 있는 보편 부품이므로 빈도 조사 없이 만든다. **판별 기준: "이 부품 정의에서 kk를 빼도 말이 되는가?" — 안 되면 그건 부품이 아니라 스키마다.** 
(진행: FormField·MultiSelect·DateRangeField·InputGroup·FileUploader·Pagination·IconButton·Callout ✅ — 보편 분자 **8** 구현·검증 완료. 조합 커버리지(모든 입력 원자를 FormField·InputGroup 슬롯에 꽂아 tsc 통과)도 확인. SegmentedControl·TabBar 원자 추가(의미 원자 **22** — prop 표 실측. 과거 21/23 표기는 off-by-one 오기). FileButton은 폐기(파일 업로드는 4단계 상태가 본질 → FileUploader 단일 경로). 분자 중 MultiSelect·Pagination·FileUploader는 "원자 + 격리된 Mantine 엔진(Combobox/Pagination/Progress)"으로 조립 — 도메인 무관·격리 유지, 순수 원자 조합만은 아님. **IconButton**은 Button을 아이콘 전용으로 고정한 named 분자(renderAction이 iconOnly 라우팅), **Callout**은 인라인 비휘발 안내(토스트와 의미 분리).)

6-B. ⏳ **kk 화면 빈도 지도 (병렬 가능, 유기체 착수 전 선행)** — 클로드 코드로 kk 로컬을 스캔해 "xx형 페이지 N개 / yy형 모달 N개" 식의 *현황* 지도를 받는다. 이는 *무엇이 부품인지*를 kk가 정하는 게 아니라(헌법 1번 — 도메인은 컴포넌트로 안 샘), *무엇부터 부품화할지 우선순위*를 실측으로 정하는 것이다. N≥3이 지금 추출할 후보(rule of three), N=1은 보류. 현황→부품 추출은 사람의 일(11-1).

6-C. ✅ **유기체 디벨롭 (빈도순)** — Modal·DataTable·EmptyState·PageHeader·DescriptionList·AppShell 6개 **구현·검증 완료**(「02」). 공유 패턴 2개(셀/값 type enum `text|badge|number|currency|date|actions`, actions 배열)를 공유 모듈로 선추출. AppShell은 A안(상단바 없음·로고 넷바 편입). DetailPage 통째는 템플릿(7번)으로 미룸.

6-D. ✅ **ERP 공통 셸** — 좌측 넷바(고정) + **우상단 바(고정)** + 콘텐츠. (초기 A안=상단바 없음에서 **B안=상단바 도입**으로 진화.) 프로필 카드는 넷바 하단→상단바 우측으로 이동, 알림 bell(안 읽음 점) 신설, 우측 밀착 `[알림][프로필]`. 좌측은 비움(넷바가 전체 경로를 제공해 브레드크럼 중복), 로고는 넷바 최상단(a형). 넷바 우측·상단바 하단 그림자로 둘 다 떠 있는 고정 패널. 모바일 햄버거는 상단바 좌측. kk 전용이 아니라 모든 ERP 공통(교집합).
7. ✅ **스키마 층 1차 + 1차 확장** — `FieldSpec`(name·label·type·required·placeholder·options·lookupKey·span·**pattern·mask·requiredWhen·disabledWhen**) + `FieldType` 7종(text·number·textarea·select·date·checkbox·**lookup**) + `buildZodSchema`(Zod: 타입+required+**정규식 refine+조건부 필수 superRefine**, 패턴 가드). 마스크 카탈로그(`_masks.ts`, phone). 도메인은 오직 FieldSpec[]로만 진입(헌법 1번). 함수(resolver)는 스키마 밖(코드 주입, patch 시그니처). **남은 것: min/max·교차필드(끝≥시작)·다중 술어·위저드 스키마는 데이터 계약 수령 후.**
8. ⏳ **조립 계약 + 카탈로그 문서화** — 헌법(「01. 규칙과 구조」)을 LLM 컨텍스트로 + 부품 어휘를 LLM이 읽을 지도로(「강제 vs 인지」).
9. ✅ **첫 수직 슬라이스 1종** — 고객 관리(목록 ListPage + 상세 DetailPage + 신규등록 Modal/FormSection)를 **config-driven으로 증명**(→ 「02」 템플릿/조직 절, `src/app/customers/`). 도메인 0줄 — schema(FieldSpec[])·rows·resolver만으로 작동. **남은 것: 위저드(MultiStepFormWizard)는 N단계 = 1엔진 + N스키마로 별도 증명 예정(현재는 단일 폼/목록/상세 슬라이스).**

### 실용 경고 (과도한 선제 추상화 주의)
- 모든 화면을 알기 전에 완벽히 일반적인 위젯 프레임워크를 먼저 짜면 틀린 프레임워크가 나온다.
- 같은 패턴이 **세 번째 반복될 때** 어휘로 추출한다(rule of three). 어휘가 실사용에서 *발생*하게 둔다.
- (ERP는 도메인이 닫혀 있어 이 위험이 비교적 작지만, 요구사항은 만들면서 발견된다.)
- **도메인 누수 주의 (헌법 1번의 실무 형태):** 부품은 kk를 모른다. kk(작업 중인 실제 ERP)는 이 시스템의 *부분집합*이어야 하며, kk 전용이라 다른 ERP엔 안 맞는 부품(여집합)이 생기면 안 된다. kk가 필요해서 만들고 싶어지는 부품은 부품이 아니라 *스키마*다(예: "발주항목카드"가 아니라 DataTable + 발주 스키마). 판별: 정의에서 kk를 빼도 말이 되는가.
---

## "올바른 방식" 문제 (가장 어려운 핵심)

자연어 설명을 받아 유한한 부품으로 위젯을 "올바르게" 조립/배치하는 판단. **이건 하나가 아니라 네 겹이고, 정의 가능한 것과 불가능한 것이 섞여 있다. 분리가 핵심.**

1. **하드 제약** — 구조적으로 허용되는 것(타입·스키마·린트가 강제). 정의 가능, 유한.
2. **기본값** — 명세가 비었을 때 적용되는 것. 사용자가 저작. 정의 가능.
3. **소프트 관습** — 권장 관용(예: "발주서엔 주소가 먼저"). 문서화하되 강제는 안 함.
4. **순수 판단** — "이 사업체엔 어떤 위젯이 맞는가". **정의 불가능. 자동화 대상 아님.**

> **촘촘하게 짜야 하는 건 1~3(정의 가능 층)이다. 4번까지 정의하려는 게 함정.**
> 4번은 정의하지 말고 사람에게 라우팅한다.

**빈칸은 LLM이 추측으로 메우게 두지 않는다.** 명세가 빈 선택 슬롯은 → 기본값을 적용하거나, 빈칸을 *드러내서 사람에게 되묻는다*. 시스템의 일은 "맞게 추측"이 아니라 "빈칸을 드러내기".

### 11-1. 규모의 역설 (현재 전략에 직결)

- **큰 규모:** 클라이언트가 *카탈로그에 맞춘다* → LLM의 판단 부담↓.
- **작은 규모(현재):** 사용자가 클라이언트와 소통하며 *사전 구조 설계 후* LLM 투입 → LLM의 판단 부담↓.
- 양 극단 모두 LLM 부담이 준다. **함의: 지금은 "올바른 방식"의 판단 엔진이 LLM이 아니라 사람(나)이다.**
- → 지금 단계엔 판단 자동화 기계를 미리 짜지 말 것. 시스템엔 *실행 충실도*만 요구한다. 정의 가능한 층(제약·기본값·관습)만 촘촘히.

### 11-2. 판단은 "정의"하지 말고 "축적"한다

순수 판단(4번)을 함수로 만들려 하지 말 것. 대신:
- 지금: 클라이언트별 "이 페이지엔 이 위젯들" 결정을 사람이 내린다.
- 축적: 그 결정들이 **이름 붙은 선례 템플릿**으로 쌓인다("가구 업체 발주 페이지는 이렇게").
- 나중(규모↑): LLM은 판단을 *계산*하는 게 아니라 *쌓인 선례 중에서 고른다*.
- → 판단은 끝내 함수가 되지 않고 **점점 커지는 예시 라이브러리**가 된다. (rule of three의 페이지 단위 적용)

### 11-3. 위젯 변형: 옵션을 쌓지 말고 위젯을 늘려라

"어떤 사업은 최고/최저, 어떤 사업은 입력칸" 같은 변동성을 한 위젯의 옵션 토글로 다 받으면, 토글 15개 = 2^15 구성 = 무한 공간의 부활(새 옷 입은). **촘촘함(정의 밀도) ≠ 유연함(옵션 개수).**
- 차라리 의견이 뚜렷한 별개 변형 몇 개로: 날씨위젯A(기본) / 날씨위젯B(최고최저 포함) / 입력형은 아예 다른 위젯.
- Button의 prop 30개 함정과 같은 원리. "유한집합" 본능이 방어선.

### 11-4. 아직 안 푼 지점

- **스키마 층 형식화 (1차 해결, 확장 진행 중).** 강점/약점 정확히: *무엇이 부품화돼야 하는지 판별하는 도메인 감각*은 강점(ERP 만들어본 자산). *그걸 형식 규칙(DSL 문법)으로 정리하는 형식화*가 약점. → 사용자가 "무엇이 부품이어야 하고 무엇이 자유여선 안 되는가"를 판단하고, Claude가 이를 스키마 문법으로 형식화한다. **1차 FieldSpec 골격 확정(→ 「02」 스키마 층). 남은 것: 정밀 검증·교차필드·조건부 필드.**
- 위저드 스키마 구조: 단계 → 섹션 → 필드 배열, 필드 타입·필수·검증규칙, 완료 추적, 임시저장, "다음" 검증 게이팅, 조건부 필드. **(현재 단일 폼/목록/상세 슬라이스까지. 위저드는 다음.)**
- ~~**검증기(Zod)**~~ → **(해결, 확장)** `buildZodSchema(FieldSpec[])` — 타입 7종+required에 더해 **정규식(`pattern`, 가드)·조건부 필수(`requiredWhen`/`disabledWhen` superRefine)**까지. 마스크(`mask`)는 입력 변환(렌더 측), pattern은 검증(스키마 측) — 별개 축. 남은 것: min/max·교차필드(끝≥시작)·다중 술어(→ 「02」 스키마 층).
- **카탈로그 진화/버전 관리** — 새 부품 추가 시 기존과의 일관성을 사람이 어떻게 보장하는가(헌법이 사람에게도 강제되는가).

소프트 관습 후보(카탈로그 문서용, 8번) — 단일선택 부품 경계: 데이터 선택(폼 제출값)은 Radio/Select, 같은 대상 뷰·모드 토글(즉시 전환, 비제출)은 SegmentedControl, 다른 구획 전환(패널 교체)은 TabBar/Tabs. 셋의 결과물이 다르므로 경쟁 경로 아님 — 단 kk처럼 단일선택에 Segmented를 남용한 사례는 의미 경계로 차단. 강제 아님(의미 판단에 사람 여지). "올바른 방식" 3번(소프트 관습) 두 번째 등록.

한 화면 primary 행동은 하나. 헤더(PageHeader)에 주 행동이 있으면 EmptyState의 action은 비운다(우리 기본 — 헤더 안정성). first-use 온보딩처럼 헤더 액션이 없으면 EmptyState action 살림. 강제 불가(부품끼리 서로 모름) 

Autocomplete — 보류 유지, 확인 대상: 측량 3건("분류 입력")이 "기존 분류 중 검색 선택"(Combobox/searchable 회귀)인지 "자유 텍스트 입력"(진짜 Autocomplete)인지 그 3건 코드 확인 후 판단.

### 11-5. 이번 세션 해결/산출

- **역할 변수 통로 통일 (해결).** 색·타이포·테두리·아이콘 정렬 등 역할에 따라 값이 바뀌는 모든 것을 단일 메커니즘 = CSS 역할 변수로 통일하기로 확정·구현(→ 「02」 역할 변수 통로 절. 원칙의 01 등재는 검토 중). 모드 분기=resolver가 위에서 깖, 상태색=분자가 자식 영역에 덮음(`--field-border`).
- **부품 구현·검증 완료.** 토큰 + 원자 22 + 레이아웃 3 + 프리미티브 3 + 분자 6 실레포 구현. tsc·eslint EXIT=0, probe로 강제 3층 발화 재확인(거짓통과 경계 포함). 조합 커버리지(원자→분자 슬롯) tsc 통과.
- **세션 중 설계 정정 3건(→ 「02」 반영):** ① Chip에 `onChange`(클릭 신호) 추가 — controlled 짝이 빠져 있던 비대칭 교정. ② Switch 트랙 `radius=full` + thumb indicator 끔(전역 defaultRadius 누수·켜짐 정렬 어긋남 차단). ③ FileUploader를 단일 `value`+`onChange(next)` controlled로 통일(onSelect/onRemove/onRetry 분리는 경쟁 경로라 폐기).
- **카운트 정정:** 의미 원자 = **22**(prop 표 실측). 과거 21·23 표기는 off-by-one 오기였음.
- **백엔드 핸드오프 작성.** 동료(DB·RLS·RPC)에게 넘길 「데이터 계약 & UI 스키마 구조」 문서 작성 — 부품 카탈로그(지도) + 데이터 계약 요청 + 헌법 가드레일. **요청 1순위는 데이터 계약**(엔티티 shape·enum 도메인·서버 검증·RPC 시그니처)이며, UI 스키마 형식화(Zod)는 오너+Claude와 공동.
- **유기체 6 구현 + 시각 정정(이번 작업).** 공유 패턴 2개 선추출(셀 type enum·actions 배열) 후 Modal·DataTable·EmptyState·PageHeader·DescriptionList·AppShell 구현·검증. 시각 검증에서 잡은 정정: ① `currency` 셀 타입 신설(₩+콤마, number는 콤마) ② DescriptionList 이종 값(텍스트 vs 배지) baseline — 값 줄 높이 토큰 통일+세로중앙 ③ EmptyState 간격 위계(아이콘+제목·설명 묶음 / action 분리, 제목↔설명 한 단계↓) ④ heading 굵기 600→700(위계 강화, 잠정) ⑤ **Modal 헤더를 Mantine 내부 위임에서 우리 Group 조립으로 교체**(정렬 보장=우리 프리미티브) ⑥ AppShell A안(상단바 삭제·로고 넷바 편입).
- **따름정리(✅ 「01」 등재 완료 — 강제 3층 절):** 서드파티(Mantine)의 내부 레이아웃에 위임하면 우리 정렬 보장이 끊긴다. 경계가 보이는 곳은 우리 프리미티브로 직접 조립한다(Modal 헤더가 그 사례). 헌법 6번의 레이아웃 버전.

### 11-6. 다음 (우선순위)

1. **ERP 레포에서 실제 소비 + 시각 검증** — `@jjaim519/erp-dsl` 설치 후 띄워, 그간 코드로만 검증한 것들(폰트 적용·헤더 화살표 정렬·상단바 프로필/알림 배치)을 화면에서 확정. **Stage5 추가분 시각 확정 대기:** Popover 폭/화살표·Image 4:3 박스·fit·fallback·Modal 스크롤(헤더·푸터 고정·85vh)·새 아이콘 62종 광학 정렬·AppShell 알림/프로필 Popover 위치.
2. **데이터 계약 수령** (동료) — 스키마 정밀 검증·실제 엔티티 shape를 푸는 입력.
3. **스키마 층 추가 확장 (7)** — min/max·교차필드(끝≥시작)·다중 술어·위저드 스키마(단계→섹션→필드).
4. **조립 계약 + 카탈로그 문서화 (8)** — 헌법을 LLM 컨텍스트로 + 부품 어휘(Callout·notify 포함)를 LLM 지도로.
5. **위저드 수직 슬라이스 (9 확장)** — MultiStepFormWizard = 1엔진 + N스키마 증명.

### 11-8. 이번 세션 해결/산출 (Stage 4.6 — 매핑 조사 보강)

고객 관리 ↔ 부품 매핑 조사에서 빈 자리로 확인된 부품·기능을 헌법 점검 후 구현·검증(tsc·eslint·build EXIT=0, 런타임 probe 9건 PASS, hex 강제 발화 재확인).
- **Callout(분자, 6→8 중 하나).** 인라인 비휘발 안내(토스트와 의미 분리). tone 4종(info/warning/danger/neutral). Group+Stack+Icon+Text 조립, 색은 의미색 스케일 토큰. → 「02」 분자.
- **Toast(`notify`, 피드백 시스템).** 명령형 트리거(페이지 스키마 밖, 코드 배선). `@mantine/notifications`를 src/ui에 격리, tone 4종·위치/지속 고정. **경계: 작업 결과만, 필드 검증은 인라인.** → 「02」 피드백 시스템.
- **전화 = "부품"이 아니라 "format".** PhoneInput 같은 도메인 부품을 만들지 않고(헌법 1번 누수), text 필드에 `mask`(입력 하이픈, 큐레이션 카탈로그)+`pattern`(정규식 검증)을 분리해 얹음. 마스킹은 렌더 측, 검증은 스키마 측.
- **스키마 확장(#9~#12).** pattern(정규식)·mask(명명 enum)·requiredWhen/disabledWhen(선언형, 함수 금지)·resolver patch 단일화(여러 필드 한 번에, 단일 set 폐기). 검증 흐름 확정: 제출→safeParse→필드명→메시지→FormField 인라인 분배.
- **ListPage 정규 확장.** toolbar.segment(유형 토글, 닫힌 스키마) + totalCount("총 N건" 푸터). 임의 JSX 슬롯 없이 전부 데이터.
- **분자 7→8.** Callout 추가. 큐레이션 아이콘 +2(info·alert-triangle). 신규 의존성 +1(@mantine/notifications). 신규 토큰 0.
- **점검에서 짚은 것(동업자 판단):** ① `pattern: string`은 유일한 열린 스칼라지만 스키마(데이터) 층이라 수용+가드. ② Callout 텍스트 색은 스펙(tone 틴트) 대신 가독성 위해 primary 유지 — tone은 배경·보더·아이콘이 전달(구조 결정). ③ 전화 mask/regex 값은 KR 로케일 = 잠정(구조는 정합). ④ 헤더 정렬 화살표 어긋남 = flex에서 `vertical-align` 무시 → Icon 광학 보정을 transform으로 옮기는 안 제시(블래스트 반경 있어 화면 검증 후 적용 — 잠정 보류).

### 11-7. 이번 세션 해결/산출

- **첫 수직 슬라이스 1종 (해결).** 고객 관리 목록(ListPage)+상세(DetailPage)+신규등록(Modal/FormSection)을 config-driven으로 증명(`src/app/customers/`). 도메인 0줄 — FieldSpec[]·rows·resolver만으로 작동(헌법 1번 실증). lookup 필드(외부 조회, resolver 주입)로 "부품은 카카오를 모른다"를 실현.
- **스키마 층 1차 (해결).** `src/schema/` = FieldSpec(7타입, lookup 포함) + buildZodSchema(Zod). 부품 격리 구역(`src/ui`)과 분리된 데이터 세계. 검증의 진실 단일화(원자에서 벗긴 min/max·error의 귀착지).
- **템플릿 3종 확정.** ListPage·DetailPage(템플릿) + FormSection(폼 조립 조직) 구현·검증(→ 「02」). 템플릿은 도메인 형태만 알고 내용은 스키마로.
- **IconButton 분자 추가(분자 6→7).** Button을 아이콘 전용으로 고정한 named 분자(01 4-C 예고의 실현). `renderAction`이 `iconOnly` 액션을 라우팅 — Action 타입에 `icon`/`iconOnly` 확장. (이후 Callout이 더해져 분자 8 — 「11-8」.)
- **시각 수정 5건(이번 세션).** ① 넷바 우측 그림자(떠 있는 패널, sm 우향) ② PageHeader 액션 size=sm(제목이 주인공) ③ DataTable 데이터행 sm·헤더 xs(위계) ④ 표↔Pagination Divider ⑤ DetailPage description(상세=제목+보조 / 목록=제목만). **전부 기존 토큰·prop 활용, 신규 부품 0.** tsc·eslint·build EXIT=0, probe 재확인.
- **공유 헬퍼 정리.** `renderAction(action, key, size)` 단일 헬퍼로 표·PageHeader·Modal·EmptyState·DetailPage 액션 렌더 통일(경쟁 경로 차단). Card `padding='none'`(자식이 패딩 소유 시), DataTable `onRowClick`(행 이동, actions 셀은 전파 차단) 추가.

### 11-9. 이번 세션 해결/산출 (배포·폰트·셸 — v0.2.0)

라이브러리를 처음으로 **외부 소비 가능한 형태**로 만들고 실제 배포까지 마쳤다. 코드 tsc·eslint·build EXIT=0, `npm pack`으로 publish 셋 확인.
- **GitHub Packages 배포 (해결).** `@jjaim519/erp-dsl@0.2.0`을 GitHub Packages에 publish. 소스(.tsx) 배포 + 소비측 `transpilePackages`, peerDependencies(mantine·zod·dayjs·react), `exports`(`.`/`./schema`), `.npmignore`로 데모·dev 갤러리 제외(공개 배럴에서 갤러리 분리), 내부 `@/schema`→상대경로. 소비 경계가 레지스트리로 강제됨(헌법 4·7). 소비/갱신 루프·`.npmrc`/토큰 절차는 「02」 패키지·배포 절. (소비 모델 검토: 벤더링/워크스페이스/npm publish 중 **GitHub Packages publish** 채택 — 경계가 구조로 강제되고 버전 관리됨.)
- **폰트 self-host (해결).** 이름 오기 교정(`PretendardGOV`→실제 패밀리명 `"Pretendard GOV Variable"`) + 단일 가변 woff2(전 weight)를 패키지 동봉, `Providers`가 `fonts.css`로 로드. CDN 의존 0 → 사내망/오프라인 동작. 트레이드오프: 5.4MB·전체 글리프 로딩을 내주고 차단 무관 동작을 삼(내부망 맥락에서 합리적; 외부 공개로 확장 시 dynamic-subset이 다음 후보).
- **AppShell 재배치 (해결).** A안(상단바 없음)→**B안(좌측 넷바 + 우상단 바 고정)**. 프로필 카드 넷바→상단바, 알림 bell(안 읽음 점) 신설, 우측 밀착 `[알림][프로필]`, 좌측 비움(브레드크럼 중복), 로고 a형 유지. notification prop·bell 아이콘 추가(→ 「02」 AppShell). 신규 부품 0.
- **헤더 화살표 정렬 (해결 — 11-8 ④ 후속).** flex에서 죽는 Icon 광학 보정을 **DataTable 헤더 한정 transform으로 복원**(같은 `--icon-baseline-shift` 토큰 부호 반전, 매직넘버 0, 다른 곳 회귀 0). 전역 Icon 변경은 버튼 등 회귀 위험 있어 안 함. *픽셀 확정은 ERP 화면 검증 대기*(잠정).

### 11-10. 이번 세션 해결/산출 (Stage 5 — Popover·Image·Modal 스크롤·아이콘 대량 큐레이션)

ERP 이식 과정에서 빈 자리로 확인된 부품·기능·아이콘을 헌법 점검 후 구현·검증(tsc·eslint EXIT=0, 강제 3층 발화 재확인, Popover는 jsdom 런타임 상호작용까지 검증).
- **Popover(원자, 22→24 중 하나).** Tooltip의 클릭·슬롯 형제. content=raw 슬롯(Modal children 동형), position 닫힌 enum 4종(auto 제외=경쟁 경로 차단), width 토큰, controlled. → 「02」 표시·행동 원자.
- **Image(원자, 22→24 중 하나).** 콘텐츠 이미지 고정 종횡비 박스(Avatar와 별개). fit·radius·size 토큰. **명시 예외: 고정치수**(폭 분배 원칙 예외, Avatar 동형, 값은 토큰으로 닫힘 → 「01」 폭 절 각주). → 「02」.
- **DataTable boolean 셀.** CellType 6→7(currency에 이은 큐레이션 둘째). true→check 아이콘, false→엷은 대시. → 「02」 셀 enum.
- **AppShell 알림·프로필 Popover.** 보류했던 "더보기 팝업/알림 드롭다운"을 Popover 신설로 충족(rule of three). 프로필=`Action[]` 완전 닫힘, 알림=`content` 슬롯. 콜백 폴백 유지(경쟁 경로 아님 — 슬롯 있으면 Popover, 없으면 콜백). → 「02」 AppShell.
- **Modal 헤더·푸터 고정 + 본문 스크롤.** 3영역 flex, 본문만 overflowY, maxHeight 85vh, 패딩 우리 3영역 이관. **명시 예외 2건:** raw flex(=헤더 직접 조립 따름정리 재적용, Modal 한 파일 격리), 85vh(공간 장부 첫 vh 잠정 단위). xl size 보류(요청). → 「02」 Modal.
- **아이콘 23→85.** Tabler 원본 62종 함수별 큐레이션(kk 요구 home·users·building·store·book·package·database 포함). 다중 path를 bounding-box 제외 단일 d로 합침. SVG 렌더 시각 검증 후 삽입. 신규 토큰 0. → 「02」 Icon 절.
- **점검에서 짚은 것(동업자 판단):** ① content raw 슬롯은 새 구멍이 아니라 Modal children 동형(기존 경계 재사용)으로 수용 — 무결성은 "예외 0개"가 아니라 "모든 예외가 명시되고 경계 안에 갇힘"(hex가 theme.ts 한 파일에 갇히는 것과 동일). ② Popover trigger는 controlled 직접 토글 — Mantine Target 위임이 이중 래핑에서 깨져 trigger가 죽는 버그를 런타임 검증으로 잡고 수정. **"tsc·eslint 통과 ≠ 동작"의 사례로 기록**(타입·린트는 런타임 trigger를 못 잡음 → 상호작용은 jsdom 검증 필요). ③ Image size는 못 닫는 게 아니라(토큰으로 닫힘) "자기 치수를 갖는 분류 예외"일 뿐 — 근거는 Avatar 동형. ④ Modal 85vh의 vh는 잠정 단위로 명시. ⑤ kk 요구 7종을 1차 큐레이션에서 빠뜨렸다 대조로 보강(짐작 큐레이션의 위험 — 실수요 우선).

**카운트 정정:** 의미 원자 **24**(22+Popover+Image). 아이콘 **85**(23+62). 셀 타입 **7**(+boolean). 분자 8·유기체 6·신규 토큰 0·신규 의존성 0(불변). 새 명시 예외 3건(Image 고정치수·Modal raw flex·Modal 85vh) — 명시 안 된 누수 0건.

### 11-11. v0.3.0~0.8.0 산출 (신규 부품 6종 — 실수요 추출, 문서 정합 보강)

> Stage 5(v0.2.0) 이후 ERP 실화면 이식 과정에서 빈 자리로 확인된 부품들을 헌법 점검 후 구현·배포(v0.3.0~0.8.0). **이 절은 Material 문서가 코드(배럴 `src/ui/index.ts`)에 뒤처져 있던 드리프트를 메우는 정합 기록이다** — 부품 자체는 각 소스 헤더 주석이 헌법 조항을 인용하며 이미 규율 안에 들어와 있었고, 본 절은 그 6종을 03 로드맵·카운트에 등재한다. (각 부품의 prop 규격 표는 「02」에 추가 예정 — 본 절은 *존재·근거·경계*만 확정.)

**신규 의미 원자 (입력군 +1):**
- **MultiDatePicker.** 여러 *개별* 날짜를 한 입력칸에서 고르는 DatePicker 형제 원자(집합 = `string[]`). 화면상 "날짜 입력 한 칸"으로 더 쪼갤 수 없고(원자), 받는 데이터만 다름(단일 `Date` vs `Date[]` — TextInput/NumberInput을 데이터로 가른 논리와 동일). 연속 범위(start~end)는 의미가 달라 DateRangeField 분자가 담당(경쟁 경로 아님). 경계 닫기는 DatePicker와 동일(label→FormField·radius/variant 래퍼 고정·min/max→스키마·className/style 규칙 3). Mantine `DatePickerInput type="multiple"`로 조립. prop: `value: string[]`·`onChange`·`placeholder`·`disabled`·`size`·`name`.

**신규 분자 (8 → 11, 전부 rule of three 충족 추출):**
- **StatusRow.** 한 항목의 "현재 상태 + 지금 할 수 있는 행동"을 한 줄로: `[아이콘?] 라벨 [상태배지] ──spacer── [액션 0~2]`. Group(justify=between)+Badge+`renderAction` 결합. 배지는 좌측 고정(액션 0/1/2개 변동에도 위치 안 밀림). 버튼 개수는 토글 prop이 아니라 `actions` 배열 길이로 표현(옵션 쌓기 금지 — §11-3). 라벨=Text body-strong(행 위계). prop: `label`·`icon?`·`status:{label,tone:BadgeColor}`·`actions?: Action[]`.
- **SummaryCard.** KPI/요약 타일 — `[틴트 아이콘] 라벨 큰수치(건수) 보조수치(금액)`. 정산 요약(승인대기 N건·₩) 같은 대시보드 통계. Card+틴트 Icon+Title(count)+Text 결합. count·amount 둘 다 선택(건수만/금액만/둘 다 — 값 유무로 결정, 토글 아님). 아이콘 틴트는 Callout 선례와 동일 의미색 스케일 토큰(`var(--mantine-color-{tone}-{n})`, raw hex 0). 통화·숫자 포맷은 `_cells`의 단일 진실(`fmtCurrency`/`fmtNumber`) 재사용. prop: `label`·`icon?`·`tone?:BadgeColor`·`count?`·`amount?`.
- **TotalRow.** 리스트 하단 합계 행 — `위 Divider(마감선) + [라벨] ──── [금액 강조]`. 시공비·사용내역서·청구·정산 4곳 반복으로 추출(페이지에서 Divider+Group을 매번 조립하던 것을 분자가 흡수 → 합계 패턴 비통일 차단). 통화 포맷 `fmtCurrency` 재사용. prop: `label?`(기본 '합계')·`amount: number`.

**신규 유기체 (6 → 8):**
- **Timeline.** 시간순 누적 이벤트를 `[시각·작성자·구분·내용]` 말풍선으로. 이벤트 shape를 닫힌 스키마로 받음(DataTable=columns, DescriptionList=items와 동형 — 도메인 무지 헌법 1). 내부 고정: timestamp 오름차순 정렬 + 날짜 구분 + 말풍선 카드. DataTable(동종 다수행 표)과 결과물이 달라 경쟁 경로 아님. **스펙↔부품 정합 보정 2건:** ① 날짜 구분은 Divider(label=…)이 아니라 가운데 Text(caption) — 우리 Divider는 orientation만 노출(닫힘). ② actor 색(tone)을 안 받음 — Avatar 원자가 색을 primary로 고정(닫힘)하므로 닫힌 경계로 못 칠함(per-actor 색이 필요하면 Avatar에 tone 경계 여는 별도 결정, rule of three). prop: `events: TimelineEvent[]`·`emptyState?`. `TimelineEvent = {id, timestamp, actor:{name}, category?:{label,tone}, title?, body?}`.
- **Calendar.** 월 달력 — 셀당 이벤트 배지 + 셀 클릭. 이벤트 색·라벨은 데이터 주입(Timeline=events와 동형, 도메인 무지). **함정 회피(§11-3): 뷰 토글·셀 렌더 슬롯·옵션 토글 0개 — 월 뷰 단일**(변형 필요 시 별개 부품). **모달 비소유:** 셀 클릭은 `onSelectDate`로 신호만, 그날 무엇을 띄울지는 페이지가 결정(raw 슬롯 차단). **명시 예외:** 7열 그리드는 우리 Grid(12의 약수만)로 못 만들어 격리 구역 raw CSS grid로 짬(`calendar.css`). controlled month. prop: `month`·`onMonthChange`·`events: CalendarEvent[]`·`onSelectDate?`·`onSelectEvent?`. `CalendarEvent = {id, date, label, tone:BadgeColor}`.

**카운트 정정 (Stage 5 → 현재 v0.8.0):**
- 의미 원자 24 → **25**(+MultiDatePicker, 입력군).
- 분자 8 → **11**(+StatusRow·SummaryCard·TotalRow).
- 유기체 6 → **8**(+Timeline·Calendar).
- 아이콘 85·셀 타입 7·신규 토큰 0(불변). 신규 명시 예외 1건(Calendar 7열 raw CSS grid — Modal raw flex 따름정리의 재적용, calendar.css 한 파일 격리).
- 배럴(`index.ts`) 분류 메모: IconButton은 *분자*(01 §4-C)이나 배럴 주석상 유기체 절에 적혀 있음 — 차기 정리 대상(표기만, 실체 무관).

**드리프트 잔여(차기 정합 대상):** ① 「02. 토큰과 구현」에 위 6종 prop 규격 표 미추가(본 절이 임시 대체). ② 「02」 산출물·카운트 절도 v0.2.0 기준 — 동일 갱신 필요. ③ 배럴 주석의 "분자 6"/"유기체 6" 헤더 라벨이 실수와 불일치(주석 문자열, 동작 무관). **→ ①②는 11-12에서 해소(02 풀 정합).**

### 11-12. v0.9.0 산출 (계층·오브젝트 부품 + 셸 틀 표준화 + 화면 해부도 + 02 풀 정합)

> ERP 실화면 이식에서 빈 자리로 확인된 계층/오브젝트 부품군 + 엑셀 초기등록 도구를 추가하고, **AppShell·PageHeader를 "모든 화면 불변 틀"로 확정**했다. 그리고 **Material 문서를 코드에 풀 정합** — 11-11이 미뤘던 02 prop 규격 표(누락 12종)·카운트·셀 enum·AppShell logoMark·패키지 버전을 모두 02에 반영. 카운트는 코드(배럴·`_catalog`) 실측 기준. 동시에 Material 4문서를 라이브러리 레포 `docs/`로 병합(publish 제외 — `files` 화이트리스트 밖).

**신규 분자 (11 → 15):**
- **Menu.** 클릭 시 열리는 액션 메뉴(Popover + `Action[]` + 선택 header). Popover(슬롯)를 "닫힌 Action 배열 + 신원 header"로 좁힌 특수형. → 「02」.
- **SectionHeader.** 카드/구획 표준 헤더(subheading = 본문 +1 tier). "헤더 ≥ content+1tier" 규칙의 부품화. `titleNode`로 인터랙티브 헤더(Breadcrumb) 주입. 페이지=PageHeader(heading) / 구획=SectionHeader(subheading) 티어 분리. → 「02」.
- **Breadcrumb.** 위치 경로(a › b › c), 앞 단계 이동·마지막 현재. 인터랙티브라 그 자체가 헤더(SectionHeader titleNode). → 「02」.
- **ObjectCard.** 계층 안 오브젝트 한 칸의 *완결* 닫힌 표현. {title, subtitle?, badge?, thumbnail?, fields?, actions?} — 셀 어휘(CellType) 재사용. **`fields[].note`로 필드별 비정상 상태(예: 가격 '변경요청중')를 국소 표시** — 도메인 규칙(컨펌 대상)은 카드가 모르고 페이지가 actions/note로 먹임(헌법 1 보편화). → 「02」.

**신규 유기체 (8 → 9):**
- **Tree.** 재귀 계층 + 펼침/접힘 + 선택 + ⋯메뉴 + 제자리 편집(범용, VS Code Explorer). `editable`이 수정 전체의 게이트(off = 분류+보기 전용). controlled. → 「02」.

**신규 템플릿 (3 → 5):**
- **HierarchyExplorer.** 계층 마스터-디테일(좌 Tree / 우 ObjectCard 그리드). **페이지 템플릿으로 승격 — Container wide + 고정 PageHeader 내장**(ListPage·DetailPage 동급). 우측 헤더 인터랙티브 Breadcrumb. 불변식: 오브젝트는 한 디렉토리·직속만·단일 트리. → 「02」.
- **PageGrid.** 페이지 공간을 닫힌 격자로(Bento). **가변 높이 불허** — 셀 고정(ROW_UNIT), 위젯이 colSpan·rowSpan으로 footprint 선택. → 「02」.

**AppShell 표준화·정정:**
- **로고 슬롯: 단일 높이 캡 → 박스 contain.** 가변 종횡비 로고(가로/세로/정사각/원형)가 종횡비대로 박스를 최대 채우고 절대 안 찌그러짐(`max-width/max-height` + `width/height:auto`, object-fit 의존 0). 텍스트 로고는 호출측 글자 크기.
- **치수 시중 표준 정렬:** HEADER_HEIGHT 72→**64**(Material 데스크톱 toolbar 56~64), 넷바 폭 260·아이콘 md=36은 이미 정합. 로고 밴드/박스 88/56(헤더보다 큰 정체성 블록 — 의도적 비표준).
- **`logoMark` prop 제거**(실렌더 0 = 약속 안 지키는 prop). 주석 드리프트 정정(프로필/알림 Popover는 구현됨 — "보류" 문구 갱신). **경계 명시: AppShell은 children만, PageHeader는 페이지 템플릿 소유**(비소유).

**화면 해부도 확정 (불변 틀 + 본문 구성):**
```
AppShell            전역 넷바 + 우상단 바 (라우트 layout 소유)
 └ PageHeader       페이지 정체성 (페이지 템플릿이 소유 — 모든 화면 고정)
   └ [컨트롤 스트립]  선택 — 검색/필터/전환탭. 그리드 밖, 높이=내용. (ListPage toolbar 선례)
   └ PageGrid(bento) 위젯 타일 (고정 셀)
```
- **좌측 패널 2고도:** AppShell.Navbar = 고정깊이 모듈 메뉴(이동) / Tree = 가변깊이 데이터 디렉토리(탐색·편집). 공존 가능, 절대 안 합침.
- **bento 1×1 = 절대 높이 × 파생 폭**(폭 = 콘텐츠폭÷columns). ROW_UNIT은 SummaryCard 실측→8px 스냅으로 확정 예정(**백로그**).
- **컨트롤 고도 규칙:** 페이지 스코프(여러 타일) → 컨트롤 스트립 / 타일 스코프(한 위젯) → 그 타일 내부 헤더.

**스키마 층 + dev:**
- **`buildHierarchyFromRows`(순수).** 엑셀 '등록' 시트 한 장(분류 칸 + 품목 정보) → 계층 노드 + 오브젝트로 변환하는 순수 함수(스키마 헬퍼, `src/ui/hierarchyImport.ts`, 배럴 노출). 파일 해독(SheetJS)은 dev 도구에서만 — 배포 DSL 의존성 0 유지. 초기 등록(미숙자) 미리보기 워크플로.
- **dev 박물관 좌측을 Tree(보기 전용)로** 도그푸드 — 분류+펼침/접힘만(편집 0), 단일 출처(`_catalog`) 파생. 브랜딩 ERP-DSL. "슬라이스 데모" 분류 제거 → 최상위 '셸'. dev 셸은 AppShell로 교체 안 함(dev는 ERP 아님).

**카운트 정정 (v0.8.0 → v0.9.0):**
- 분자 11 → **15**(+Menu·SectionHeader·Breadcrumb·ObjectCard).
- 유기체 8 → **9**(+Tree).
- 템플릿 3 → **5**(+HierarchyExplorer·PageGrid).
- 의미 원자 25·아이콘 85·신규 토큰 0(불변). **셀 타입 7 → 14** — 02가 v0.2.0 7종에 멈춰 있던 것을 정합(+user·tags·link·percent·secondary·relative-time·thumbnail).
- **02 풀 정합:** 누락 prop 표 12종(분자 7·유기체 3·템플릿 2) + MultiDatePicker 추가 = 02 코드 완전 동기화. AppShell logoMark 제거·치수 반영. 패키지 버전 0.2.0→0.9.0, publish 격리 `.npmignore`→`files` 정정(단 `_catalog`/`_registry`가 tarball에 포함되는 잔여는 명시).

### 11-13. v0.10.0 산출 (애플식 squircle 곡률 + radius 상향)

> ERP 모서리에 애플식 통일성(squircle)을 도입. radius 스케일(sm/md/full)은 유지하되 값을 키우고, 그 위에 연속 곡률을 단일 토큰으로 얹었다. tsc·eslint EXIT=0, 크롬 화면 검증(아바타·스피너 원형 유지 + 카드 squircle 확인). 커밋·푸시·버전 태그(v0.10.0) 완료, **publish는 보류**(소비자 영향 0).

- **corner-shape(superellipse(2)=애플 squircle)을 단일 토큰으로 추가.** `theme.ts`의 `cornerShape` → `cssVariablesResolver`가 `--corner-shape` 방출(역할 변수 통로 — 색·타이포·아이콘 정렬과 동일 구조). 컴포넌트엔 prop으로 안 연다(헌법 5 — 0~100 smoothing 스칼라 노출 금지, 전역 토큰 1개).
- **박스 전용 opt-in(`squircle.css` 화이트리스트).** Button·Paper·Input·Modal·Popover·SegmentedControl에만 `corner-shape` 적용. **방향 결정:** 전역(*) 후 원형 제외(blacklist)는 빠뜨린 원형(Radio 등)이 각지므로 "박스에만 켠다(whitelist)"로 뒤집음 — 목록 밖(Avatar·Spinner·Switch·Chip·Radio·알림 점)은 자동 round. **squircle은 원/알약의 둥근 곡선까지 각지게 만들기 때문**(원→둥근 사각형). border·box-shadow·outline이 모양을 네이티브로 따라가 clip-path식 테두리·그림자 충돌 없음(MDN 확인).
- **radius 값 상향(스케일·키는 불변): sm 4→8, md 8→16.** squircle 곡률은 반경이 작으면 드러나지 않아(8px엔 곡률 바꿀 모서리가 8px뿐) md를 키워야 카드에서 보인다.
- **SummaryCard 아이콘 칩 sm 고정.** md=16이 작은 아이콘 칩(~36px)을 거의 원형으로 만들어 sm(8px)으로 복원(이전 md=8px 동치).
- **브라우저 정책:** corner-shape는 Chromium만(2026.06 ~67%, Safari/FF 미지원), **graceful fallback** — 미지원 시 평범한 둥근 모서리. radius 값은 전 브라우저 동일하므로 구조적 통일성 유지, squircle은 점진 향상 층. (사파리 혼재 환경이라 경로 A=네이티브 corner-shape 채택; 경로 B=clip-path는 테두리·그림자·no-px 충돌로 비채택.)
- **점검에서 짚은 것(동업자 판단):** ① raw-div 보조 표면(Callout·FileUploader 드롭존·DataTable 헤더)은 화이트리스트 밖이라 radius bump은 받고 squircle은 안 받음 — 경미한 cosmetic 갭, 수용. ② `Menu.tsx`의 `borderRadius: 4` 하드코딩 리터럴(토큰 아님)은 기존 드리프트, 차기 정리. ③ 소비자는 버전 bump+재설치 시 자동 반영(SSOT) — API/타입 무손상, 단 전 화면 시각 변화(둥글어짐)라 의도된 버전 업(minor)으로 표기.
- **README LLM 기준 재작성.** 소비자가 오너+동업자뿐이라 README를 *조립 컴파일러(LLM)용 레퍼런스*로 전면 개편(역할·절대 규칙·토큰 어휘·카탈로그·결정 가이드). 패키지명 버그(`@your-org`→실제 스코프, transpilePackages 포함) 수정.

**카운트 정정 (v0.9.0 → v0.10.0):**
- **신규 토큰 +1**(corner-shape/squircle — 색·간격·radius 이후 첫 신규 토큰 종류). radius 단계 불변(sm/md/full), 값만 상향(sm8/md16).
- **신규 산출물 파일 +1**(`squircle.css`). 의미 원자 25·분자 15·유기체 9·템플릿·아이콘 85·셀 타입 14 전부 불변(부품 추가 0).
- 패키지 버전 0.9.0→**0.10.0**(시각 변경 minor).

### 11-14. v0.11.0 산출 (반응형 셸 + Collapsible + HierarchyExplorer 응집 재설계 + 흡수 전략 명문화)

> ERP 실이식 과정에서 (a) 모바일 반응형 셸, (b) Collapsible 분자, (c) HierarchyExplorer 우측 영역의 응집된 재설계(폴더 탐색·전역 검색·다중 뷰·헤더 안정성)를 추가하고, **"흡수 전략"을 전략 원칙으로 명문화**했다. tsc·eslint·build EXIT=0, dev 박물관 시각 검증.

**AppShell — 모바일 반응형(하단 탭 내비):**
- 햄버거+서랍 폐기 → **데스크탑=좌측 넷바 / 모바일=하단 탭 내비**(`AppShell.Footer` 슬롯, `useMediaQuery`로 sm 분기). 같은 `menuItems`/`activePath`/`onNavigate` 재사용(새 prop 0) — *같은 내비의 모바일 재배치*. 탭 5개 초과 시 앞 4 + '더보기' Popover(§11-3 옵션 쌓기 차단). 모바일 상단바 좌측에 로고(데스크탑은 넷바 소유). 탭 타일은 우리 Stack/Icon/Text로 조립, footer 골격·safe-area는 격리 raw(명시 예외).
- **신규 셸 치수**: `MOBILE_LOGO_HEIGHT=40`·`BOTTOM_NAV_HEIGHT=56`(잠정, 8px 스냅). **`env(safe-area-inset-bottom)`** = 공간 장부의 새 동적 단위(명시 예외, Modal 85vh 동류).

**신규 분자 (15 → 16):**
- **Collapsible.** 헤더(항상 보임)+본문(접힘/펼침) 한 단위, 헤더 클릭 토글. header·children = raw 슬롯(Modal children 동형). `defaultOpen`(uncontrolled — 펼침은 UI 표현 상태). **경계: accordion(하나만 열림)은 이 부품 일 아님**(쌓아서 구성, §11-3). Mantine `Collapse` 격리 래핑. 헤더엔 보통 StatusRow·TotalRow를 꽂는다(요약행 재사용). → 정산 요약행·발주 완료섹션 실수요.

**ObjectCard — 다중 뷰(상세/요약) + 풀폭 썸네일:**
- `layout: 'card' | 'row'` 닫힌 enum(Finder식 2뷰). **두 뷰의 의의 = 정보량**: `card`(상세)=배너 썸네일+**전체 필드** / `row`(요약)=작은 썸네일+**summary 필드만**+핵심. `ObjectField.summary?: boolean` 신설(brief 노출 필드 명시 — `note`처럼 닫힌 필드 플래그).
- **Image `size: 'full'` 추가**(컨테이너 폭 100% + 4:3 비율 잠금, 왜곡·임의 px 0). 카드 뷰 배너 썸네일용 — "고정 종횡비 박스" 예외에 *부모가 분배한 폭*을 받는 닫힌 값 하나 추가.
- void 차단: `fill` 제거 + 그리드 `alignItems:start`(카드=내용 높이).

**SectionHeader — controls 슬롯 + 안정 밴드:**
- **`controls?: ReactNode`** 신설 — 헤더 *좌측*(제목 옆)에 붙는 인라인 도구(검색·뷰 토글). **우측은 액션 존**(actions)이라 도구는 좌측. (→ 헤더 안정성, 아래.)
- **헤더 행 안정 밴드**: `min-height`(잠정 36) 기본 예약 → 액션 유무로 헤더가 위아래로 안 흔들림. 실사용 2곳(Tree·Explorer 패널 헤더) 정렬 통일.

**HierarchyExplorer — 우측 영역 응집 재설계:**
- **폴더(상위 분류) 선택 → 하위 분류를 클릭 가능한 폴더 타일로**(빈 상태 텍스트 폐기 — 폴더는 하위를 드러내야 로직상 맞다). 타일 클릭=드릴다운. 폴더 타일 grid는 품목과 같은 4열(span 3, 12-span 내부 그리드).
- **전역 검색**(controlled): `searchQuery`·`onSearchChange`·`searchResults: HierarchySearchResult[]`(오브젝트+경로). 쿼리 시 결과 모드(각 결과에 경로 Breadcrumb, 클릭=집 점프+검색 해제). 필터의 진실은 소비처(DataTable/ListPage 선례). 중복은 불변식(오브젝트↔한 디렉토리)상 0.
- **자동 펼침**: 우측에서 노드로 점프(폴더 타일·브레드크럼·검색결과) 시 좌측 트리의 접힌 조상을 자동 펼침(onToggle은 접힌 것만 가드).
- **헤더 안정 재설계(→ [헤더 안정성 원칙]):** 좌=제목+도구(검색이 좌측 앵커·항상, seg는 그 오른쪽·잎일 때만), **우=액션 존(품목 추가, 품목 있을 때만)**. 빈 잎은 EmptyState 중앙 CTA가 추가를 대신(배타 — +타일·이중 노출 중복 제거). 조건부가 고정 요소를 안 민다.
- 그리드: 품목 카드 **고정 4열**(`repeat(4)`, 아이템 수로 동적 분할 안 함 — 2개면 좌측 2칸·나머지 빈 채). bento 가로 분할의 위젯-내부 적용(높이는 내용 기준).

**벤토 그리드 방향(논의 — 별도 청사진 진행 중):**
- page 본문 ≡ bento, Tile=widget, 검색/필터=widget 내부 변형(ControlStrip 폐기), 12-span 유지 + **bento 한 칸=2-span(6 거친 셀)**, footprint=2×n. 두 고도: 페이지(footprint+ROW_UNIT 고정) / 위젯 내부(가로 분할만+내용 높이=Grid 프리미티브). **정식 Bento 프리미티브(PageGrid 승격)는 첫 widget-배치 페이지를 만들 때** — 투기적 선제작 금지. (상세 청사진은 별도 세션/파일, 본 배포엔 미포함.)

**카운트 정정 (v0.10.0 → v0.11.0):**
- **분자 15 → 16**(+Collapsible). 의미 원자 25·유기체 9·템플릿 5·아이콘 85·셀 타입 14 불변.
- Image `size` 값 +1(full). ObjectCard `layout`·ObjectField `summary`, SectionHeader `controls`, HierarchyExplorer 검색 3 prop, AppShell footer/safe-area 추가. 신규 명시 예외: `env(safe-area-inset-bottom)`.
- 패키지 버전 0.10.0→**0.11.0**(신규 부품 + 반응형, minor).

---

## 흡수 전략 (전략 원칙 — 명문화)

> **우리는 "ERP를 닫힌 부품으로 조립한다"는 *방식*을 만들지만, 그 방식을 떠받치는 엔진·렌더링·인프라까지 전부 직접 만들지 않는다. 검증된 외부를 *흡수*한다.**

- **이미 굴지의 기업들이 같은 "방식"으로 접근했다** — JSON/스키마로 UI를 구동하는 스키마-드리븐 렌더링(예: Vercel **json-render** 류), 닫힌 컴포넌트 레지스트리, 디자인 토큰 시스템. 이건 우리만의 발명이 아니라 *업계가 수렴 중인 패턴*이다.
- **그래서 "내가 전부 만든다"는 욕심을 버린다.** 엔진(스키마→렌더 변환·resolver·런타임)은 검증된 외부로 **흡수**하고, 우리는 *대체 불가한 고유 가치*에만 집중한다:
  - **디자인 토큰 + 닫힌 부품 카탈로그**(이 DSL의 헌법·강제 3층) — 통일성의 원천.
  - **`FieldSpec`/스키마 층**(도메인 진입로) — ERP 도메인 형식화.
  - **ERP 특화 부품**(HierarchyExplorer·ObjectCard·정산/발주 조립 패턴) — 도메인 자산.
- **판별 기준:** "이게 우리 ERP-DSL의 고유 가치인가, 아니면 누구나 쓰는 범용 엔진인가?" 후자면 흡수(외부 채택), 전자면 직접. (헌법 4의 "큐레이션"이 *부품* 차원이라면, 흡수 전략은 *인프라* 차원의 같은 사상 — 우리가 통제할 경계와 빌려올 경계를 가른다.)
- **현 단계:** kk 건은 지금 구조(자체 렌더)로 마무리하고, 그 이후 json-render 류로 엔진을 흡수하는 전환을 디벨롭한다. 토큰·FieldSpec·ERP 부품은 흡수 후에도 우리 것으로 남는다(엔진만 교체, 어휘·문법·도메인은 유지).

---

### 11-15. v0.12.0~0.15.0 산출 (오브젝트 카드 단일화 + 주의 층위 funnel + 헤더 안정)

> kk 실이식에서 (a) ObjectCard를 사진 카드 한 종류로 단일화, (b) "처리해라" 신호를 넷바→탭→Callout→행으로 일관 관통시키는 **주의 층위 funnel**, (c) 헤더가 제목 길이·조건부 토글에 안 흔들리는 **3존 안정 레이아웃**을 확정했다. 각 단계 tsc·eslint·build EXIT=0, dev 박물관 + CDP 실측 검증.

**v0.12.0 (HE 뷰 전환 + ObjectCard 사진카드 + LedgerPage):**
- ObjectCard `layout: detail/normal` 폐기 → **사진 카드 단일 종류**(media 고정높이 캡 + 역할 슬롯 status/headline/attributes + 케밥 액션). 밀도 비교는 카드 축소가 아니라 Explorer **'목록'(DataTable)** 뷰가 맡음(카드/목록 토글).
- **FileUploader 결함 수정**: 고른 `File`을 버리고 메타만 onChange 하던 걸 `FileItem.file?: File`로 노출 — 소비처가 FormData로 실제 업로드 가능(이전엔 물리적 불가).
- Image `size:'fill'`(부모 박스 cover, object-fit 인라인 고정 — viewBox 없는 SVG 찌그러짐까지 차단). 돈 지표 템플릿 LedgerPage.

**v0.13.0 (CountBadge 신설 — 의미 원자):**
- 카톡/Gmail식 **빨강 N 동그라미**. Badge(상태·반투명 light)와 별개 역할 = 솔리드 채움으로 "처리할 건수" 환기. `count`(0이면 숨김)·`tone`(danger/neutral)·`max`(N+)·`dot`. 색은 토큰 var(danger-6/neutral-6)+white — Badge로는 솔리드가 안 나와 별도 부품.

**v0.14.0 (주의 funnel 배선 + 헤더 3존 안정 + 행 강조):**
- **CountBadge 배선**: `AppShell.menuItems[].count`(넷바·모바일탭·더보기) + `TabBar.options[].count/countTone`(탭). → 같은 행동요구 신호를 넷바→탭에서 일관 반복(행동요구만 빨강, 정보는 중립 — 대비가 핵심, 남발 금지).
- **Accordion `tone:'attention'`**: 행동요구 행 = 좌측 빨강 띠 + 은은한 틴트(같은 색축). 행 자체가 "내 차례"임을 펼치기 전에 말함. 행동 불필요엔 중립.
- **SectionHeader 3존**: `[제목존 flex:1 말줄임][controls 고정][actions 우측끝 고정]`. 제목(breadcrumb)이 길이 변동을 흡수해 도구·액션이 **제목 길이·조건부 토글에 안 밀림**(이전 "검색 좌측 앵커"가 제목에 흔들리던 문제 해소). min-height 36→**38**(sm InputGroup 실측), 좌/우 패널 divider 정렬 CDP 0px 검증.
- **Breadcrumb 마지막 크럼 우선**: 좁아지면 앞 조상부터 클립, 현재 위치는 끝까지 보존(nowrap — 두 줄/글자쪼갬 차단).

**v0.15.0 (Accordion 펼치면 틴트 해제):**
- **`clearAttentionOnOpen`**: 펼침(=봤음)을 트리거로 attention 틴트를 **부드럽게 페이드 해제(영구, seen 누적)**. 큐/인박스(읽지않은 메일) 모델 — 같은 목록 전부 틴트면 대비가 죽으니, 본 것은 빠지고 "아직 안 본 것"만 남아 의미가 산다. 좌측 띠를 보더→**inset box-shadow**로 바꿔 너비 점프 없이 색만 전이, `prefers-reduced-motion` 존중. seen은 ephemeral 표현 상태(도메인 아님). CDP로 펼침 전(danger 띠)→후(transparent) 검증. **(v0.15.1 — 해제 상태에 인라인 override를 0으로 두려고 `controls.css .erpAccordionAttn[data-attn]`로 클래스화: data-attn="off"면 Mantine 기본만 적용 → 본 뒤 접으면 일반 행과 *픽셀 동일*(이전 인라인 `bg:bg-primary` 강제가 기본 gray와 달라 보이던 버그 fix). CDP로 seen+접힘 == neutral 동일 검증.)**
- **보류**: 행 헤더에 1차 액션(수락) 노출(컨셉 B)은 Accordion "헤더 전체가 토글" 불변식과 충돌 → 전파차단 actions 슬롯 신설로 차후 재검토. 행 exit 애니메이션은 발주 수락=탭 이동(행 삭제)이라 불필요.

**v0.17.0 (HE 검색 좌측·목록 단일화 + Tree 인라인 ✕✓ + Accordion 틴트 색):**
- **HE 검색 좌측 이전**: 전역 검색 입력을 우측 헤더 → **좌측 분류 헤더 아래**(Tree `toolbar` 슬롯 신설). 검색은 트리를 관통하는 행위라 트리 옆이 정합(dev 박물관과 동형). 결과는 우측 **목록 + `분류` 경로 컬럼**(`부자재 › 경첩`), 건수는 헤더 우측 `controls` 고정(폭 밀림 0).
- **HE 뷰 단일화**: 카드/목록 `SegmentedControl` 토글 폐기 → **목록(DataTable)만**. 잎 품목·검색 결과 모두 목록. 카드(12×6 벤토)·우측 ObjectCard 사용은 잠정 보류(가성비, 복원 백로그).
- **Tree 인라인 편집 ✕✓**: 추가/이름변경 입력칸에 취소 ✕·확인 ✓ **컴팩트 버튼(20px)** — 행 높이가 아래 행과 픽셀 동일(sm IconButton 36px는 행을 키워 불가). ✕ 클릭 시 blur(=confirm) 선점 버그는 `mousedown preventDefault`로 가드.
- **Accordion 틴트 색 개방**: `AccordionItem.color?: BadgeColor`(토큰 enum, 기본 danger) — 색을 CSS 역할 변수(`--erp-attn-bg/-band`)로 흘려 `controls.css`가 칠한다(임의 hex 진입 0).
- **(v0.17.1) 목록 `비고` 컬럼**: 목록 단일화로 빠졌던 `headline.note`(값-국소 배지, 예 '변경요청중')를 **`비고` 배지 컬럼**으로 복원(핵심값 뒤·상태 앞, `_cells` 어휘 무변경 — 기존 badge 타입 재사용).

**v0.32.0 (HE 디렉토리·제품 공존 + 추가 드롭다운 + 목록 열폭/디스클로저):**
- **잎/폴더 이분법 폐기**: 한 디렉토리가 하위 분류(n+1)와 직속 제품을 *동시에* 보유 가능 — 디렉토리 선택 시 우측 본문 = [하위 분류 타일] + [직속 제품 목록] 공존(`isLeaf` 분기 제거). 자식 있는 디렉토리의 직속 제품이 숨겨지던 잠재 버그 해소. 용어 '품목→제품'(UI 라벨).
- **추가 단일화(우측 ＋ 드롭다운)**: 우측 헤더 `Menu`[제품 추가 / 분류 추가] — 둘 다 완전 위임(`onAddObject` / `onAddChild(parentId)`, 소비처 모달). Tree 케밥에서 `하위 디렉토리 추가` 제거(이름변경·삭제만), 최상위 추가는 트리 헤더 ＋ 유지. **Tree `onAddChild` 전면 제거**(범용 부품 정리).
- **페이지네이션 폐기**: HE `page/onPageChange/totalPages` 제거 → 본문 통째 스크롤(폴더 타일 5분할 + 목록, 좌측 트리도 내부 스크롤).
- **목록 케밥 → 디스클로저(›)**: 행 액션(수정·삭제)은 *상세 모달*에서 — 목록 끝은 `chevron` 셀(상세로 인도하는 시각 장치, 자체 동작 0·클릭은 행 onRowClick으로 버블). 행/케밥 이중 동작을 단일 진입으로 통일. **셀 타입 +1 = `chevron`**.
- **목록 열 폭 안정 + 헤더 강조**: DataTable `column.grow`(이름 열=남은 폭 채우고 길면 `…` 말줄임)·`column.maxWidth`(상한) — table-layout auto 유지라 나머지 열(단가·상태)은 내용 맞춤(단가는 headline.label로 도메인 주입 → 부품이 폭 안 박음). 헤더 밴드 = 회색 배경+하단 divider를 **셀(th)** 에 부여(sticky `<thead>` 배경·보더 미페인트 회피) + `body-strong`로 본문보다 강조. 폴더 타일 라벨도 한 줄 말줄임(높이 통일).
- **breaking**: HE `page/onPageChange/totalPages` 제거 · `onAddChild` 시그니처 `(parentId,label)`→`(parentId)` · Tree `onAddChild` 제거 → 소비처(kk ERP) 호출부 조정 필요(소비처 인지 완료).

**v0.33.0 (Accordion 강조 그림자화 + FieldGrid node 셀 정합):**
- **Accordion 강조 재설계(윤곽 최소·그림자 위주)**: 기본 = 회색(`--bg-tertiary` — `bg-secondary`는 흰 배경과 거의 동색이라 한 단계 진하게) + 그림자(shadow-sm), 윤곽 없음. 강조(attention) = 틴트 채움(`{color}-0`) + *얇은* 틴트 윤곽(`{color}-3`) + 그림자. **좌측 띠 폐기.** Mantine separated가 active(열린 항목)만 칠하는 문제는 전 항목에 직접 명시(unlayered가 @layer mantine 이김)로 해결. `clearAttentionOnOpen` 페이드(채움·윤곽) 유지. (시장 조사: Material 톤/그림자 elevation·PatternFly 카운트 배지 등 검토 후 '윤곽 배제·그림자' 지향으로 확정.)
- **FieldGrid `node` 셀 정합**: 비표준 컨트롤 통째를 받는 raw 슬롯(`FieldGridCell.node` — label/field/image와 배타·mode 무관)을 카탈로그 `rows`·데모(Cascader 셀)에 반영(코드엔 존재했으나 문서 누락). composition `_cells(renderCell)`→`_masks(applyMask)` 정정(read=입력 원자 inert 재사용, renderCell 미사용).

**v0.34.0 (Modal/Drawer xl·full + Cascader 재정비 + MillerColumns 신설 + Popover xl·align):**
- **Modal/Drawer `xl`·`full`**: size에 `xl`(Mantine 네이티브)·`full`(거의 꽉 찬 wide — Modal 95vw·90vh, Drawer 축 95%, 풀스크린 아님·radius/여백 유지) 추가. 기본 md 유지(영향 0).
- **Popover `xl`(600)·`align`(start/center/end)**: 다단 패널용 폭 + 드롭다운형 좌측정렬(`bottom-start`). Cascader·MillerColumns가 `align="start"`로 트리거 시작모서리에 정렬.
- **Cascader 재정비**: 순차 인라인(원래 방식) 유지하되 드롭다운 박스를 Mantine Select → **공유 컬럼-아이템 박스**(Popover 기반)로 교체(모양 통일). 리프 시 브레드크럼 압축.
- **MillerColumns 신설(분자)**: 트리거 1개 → 팝오버 다단 컬럼(Finder·Ant Cascader 패턴), 좁은 화면(≤600px) 단일 컬럼 드릴인 폴백, 완료 시 브레드크럼 압축(Cascader와 동형 결과 표시). Cascader(인라인)의 footprint 형제 — Select/SegmentedControl/Radio처럼 같은 데이터·다른 레이아웃을 별 부품으로(선례 일관).
- **정렬 보정**: 컬럼형 트리거 텍스트·꺽쇠 박스 기준 수직 중앙(`align-items:center` + 아이콘 래퍼 `line-height:0`).

**v0.41.0 (MillerColumns 완료 브레드크럼 말줄임 + Popover block + edit 아이콘 수정):**
- **MillerColumns 완료 트리거 재작성**: 깊은 경로(예: 6단)가 좁은 셀에서 잘리고 변경 버튼이 밀려 잘리던 문제. 브레드크럼을 말줄임(`min-width:0`+`nowrap`+`overflow:hidden`+`ellipsis`, 전체경로는 `title` hover)으로 — 경로 깊이·폭 무관 안 깨짐. (Text 원자는 truncate 미노출 → 격리구역 raw span + typo 토큰, HE/Tree 선례.)
- **Popover `block`(닫힌 bool, 기본 false)**: true=트리거 span `display:flex;width:100%;min-width:0` → 소비처 컨테이너 폭에 맞춰 줄어듦(트리거 안 말줄임 가능). MillerColumns만 켬, 기존 소비처(Menu·Cascader·TreeSelect) 불변(blast radius 0).
- **변경 어포던스 풀 Button → chevron-down 아이콘**: Button 자체 높이가 FieldGrid 행 높이를 키우던 제약 해소(높이 중립·`flex-shrink:0` 항상 노출). placeholder('선택 ⌄')와 동일 글리프로 일관·선명.
- **`edit` 아이콘 path 수정**: 단순화 연필이 자기교차·미닫힘 코너로 작게 그리면 흐릿하게 뭉개지던 것 → 표준 Tabler 연필로 교체. Menu '수정'·ObjectCard·Tree '이름 변경'·거래처 등 모든 edit 사용처 선명.

**v0.40.0 (AppShell 모바일 — 아바타-only 프로필 + 네이티브 앱 스크롤):**
- **모바일 프로필 아바타-only**: 좁은 모바일 헤더에서 이름(4글자+)이 줄바꿈되던 문제 — 이름·직책·caret을 `visibleFrom="sm"`로 감싸 데스크탑만 노출, 모바일은 아바타만(탭→메뉴 헤더에 이름·이메일 노출, 정보 손실 0). 데스크탑 불변.
- **모바일 네이티브 스크롤**(appshell.css `@media (max-width:47.99em)`): 화면 끝 고무줄 바운스·뒤배경 노출 + 내용보다 긴 잉여 스크롤 제거. `html,body{overflow:hidden;overscroll-behavior:none}`로 페이지 스크롤 잠금, `.erp-appshell-main{height:100dvh;min-height:0;overflow-y:auto;overscroll-behavior:none}`로 본문만 고정 헤더·하단탭 사이에서 스크롤(짧으면 0·길면 그만큼·끝에서 바운스 없이 멈춤). 인라인 minHeight:100vh 무효화가 잉여 스크롤 제거의 핵심. em 미디어쿼리라 font-scale 무관·Mantine sm 정합. 데스크탑은 문서 스크롤 그대로.
- **(소비처 필수)** 하단 탭바가 iPhone 홈 인디케이터에 가리는 건 `env(safe-area-inset-bottom)`이 0이라서 — 소비처 layout에 `viewport: { viewportFit: 'cover' }` 필요(이게 있어야 env 채워짐, 패키지의 기존 safe-area 처리 작동). dev 앱 layout.tsx에 레퍼런스로 추가(패키지 미포함).

**v0.39.0 (PaperModal 인쇄 빌트인 — A4 1:1 WYSIWYG · 1장 · 디바이더 크리스프):**
- **PaperModal 인쇄 빌트인(@media print, controls.css)**: 소비처가 우회 인쇄 CSS를 붙이던 걸 .erpPaper 소유자(PaperModal)가 가져옴. ① `body * {visibility:hidden}` + `.erpPaper * {visible}` → 종이만 출력(모달 백드롭·앱 화면·머리말/꼬리말 제거). ② `.mantine-Modal-content {transform:none}` + 종이 `position:fixed` → 화면 맞춤 scale·모달 변형 무효화 → 물리 A4 1:1(우측 잘림·축소 0). ③ orientation별 `@page {size:A4 …; margin:0}` + `html/body` 페이지 높이·overflow:hidden 주입(`<style>`) → 빈 2페이지 차단·**1장**(visibility:hidden이 남기는 배경 높이를 클립). ④ `print-color-adjust:exact` → 라벨 음영 등 배경 출력.
- **FieldGrid 인쇄 크리스프 보더(fieldgrid.css)**: 1px 보더가 인쇄 DPI 픽셀 스냅으로 일부 사라지는 것 방지 — 색 `--border-strong` + 폭 0.2mm 물리단위로 고정 → 디바이더 균일 출력.
- 화면(자세히/전체)은 전부 @media print 안에서만 다뤄 불변. orientation별 @page size는 닫힌 enum 기반 주입. (소비처 kk OrderViewDocument의 우회 인쇄 CSS는 제거 가능 — 중복.)

**v0.38.0 (PaperModal 뷰 토글 — 자세히/전체 + 모달 폭 가로 A4 고정):**
- **PaperModal 두 뷰(헤더 SegmentedControl)**: 전체 보여주니 글자가 작던 문제 해결. **모달 폭을 가로(landscape) A4 기준으로 고정**(세로 문서여도 넓은 박스 — 한 모달에서 두 뷰를 받으려 가로만 넓힘, 토글해도 모달 크기 불변). ① **자세히**(기본·좌): 문서를 모달 폭에 꽉 채워 확대(fit-to-width)·세로 넘치면 스크롤 → 글자 읽기용. ② **전체**(우): 높이 맞춤 contain·무스크롤(세로 문서는 좌우 데스크). 토글=내부 상태(공개 prop 아님). 측정은 fitA4(...,'landscape')로 프레임 고정, 스케일만 뷰별 분기(전체=contain·크게=폭채움).
- **가로 문서 데모 일단 삭제**(`orientation` prop은 부품에 유지 — 닫힌 값, 데모만 제거).

**v0.37.0 (Popover 폭 동적화 — MillerColumns 다단 컬럼이 분류 레벨마다 정확히 고정):**
- **Popover `width="auto"`(내용폭) + `reposition="anchored"`(flip·shift 둘 다 off)**: 닫힌 enum 값 *추가*(기존 sm/md/lg/xl·flip/fixed 불변 → 소비처 영향 0). auto=Mantine에 폭 미지정 → content 크기로(컬럼 수 따라 늘고 줆). anchored=좌상단 앵커 *완전 고정*(`position="bottom-start"`의 -start 정렬 성질로 박스 크기와 무관하게 좌상단 좌표 불변), 내용은 오른쪽·아래로만 성장.
- **MillerColumns 폭 동적화**: 0.36.0의 `width="xl"`(600 *고정* — 1컬럼이어도 600px 빈 공간) 폐기. `width="auto"` + `reposition="anchored"`로 — 1컬럼(하위분류 없음)이면 ~210px로 좁게, 드릴할수록 오른쪽으로 확장, **앵커는 분류 레벨이 늘어도 픽셀 단위로 고정**(점프 0). 넓은 모드 컬럼 컨테이너 `maxWidth:600` + 내부 가로 스크롤(상한 넘어도 박스 위치 불변·화면 무한 이탈 방지). *(미해결: 모달 안 팝오버를 모달에 *가두기*는 별도 — 포탈 여부/boundary 레버, 대기 중.)*

**v0.36.0 (PaperModal 재정의 — 작성용 종이 → 순수 A4 문서 *뷰어*):**
- **PaperModal 재정의(유기체)**: 0.35.0의 "본문 자체가 A4 한 장(작성·확인, 길면 스크롤)" 개념을 폐기하고 **순수 문서 뷰어**로 다시 지음(완성 문서를 한 화면에 통째로, **스크롤 절대 0**). public prop명(opened/onClose/title/actions/orientation/closeOnOverlayClick/children)은 동일, 동작만 교체.
  - **종이가 자기 윤곽을 가짐**(.erpPaper = --bg-primary·--border-default·shadow md) — 모달이 아니라 *종이*가 A4 한 장. 본문은 회색 데스크(--bg-secondary).
  - **CSS aspect-ratio/max-* 안 씀**: 뷰포트(95vw×95vh 상한)−크롬을 실측해 fitA4로 A4 contain 박스를 px 계산. 내용은 표준 A4 캔버스(794×1123 @96dpi)에 그린 뒤 transform:scale로 박스에 꽉 맞춤 → 폰트·여백 비율 통째 축소(프레임만 줄이는 내용 넘침 회피). ResizeObserver+resize로 추적, 콜백 ref로 Portal 늦은 mount 안전.
  - **모달이 종이에 hug**: 95vw×95vh는 상한으로만, 모달 폭·본문 높이를 종이+8px로 줄여 세로 A4의 좌우 회색 낭비 제거.
  - 인쇄=소비처 위임(.erpPaper print 훅 + actions에 인쇄 배선). 뷰어 빌트인 인쇄 없음.
- **FieldGrid `size`(sm/md/lg)**: 타이포·행 단위(--fg-row)·세로패딩(--fg-pad-y) 한 세트 닫힌 스케일(기본 md). 행 높이가 타이포 따라 동적으로 커짐(큰 부품은 rowSpan 병합으로 엑셀형 균일 유지). 라벨 셀은 raw 타이포(`.erp-fg-label`)로 size 변수를 탐.
- **MillerColumns 박스 동적화**: 0.35.0의 상수(600×320) 고정을 폐기하고 박스를 *내용에 맞춰 동적*으로 — 점프 방지는 `reposition="fixed"`(flip off·좌상단 앵커)가 담당(컬럼은 아래/오른쪽으로만 증식, 앵커 불변). 긴 컬럼은 `.erpColList` max-height 내부 스크롤.

**v0.35.0 (PaperModal 신설 + MillerColumns 위치 고정 + Popover reposition):**
- **PaperModal 신설(유기체)**: 모달 본문 자체가 A4 한 장인 문서 모달(거래명세서·견적서 등 작성·확인·인쇄). 회색 backdrop 중첩 없이 본문이 A4 비율로 폭을 꽉 채움(최소 여백=모달 패딩). orientation=세로/가로 *버전* 고정(폭 lg/xl). 내용=소비처 FieldGrid, 인쇄=소비처 위임(.erpPaper print 훅). *(→ 0.36.0에서 순수 뷰어로 재정의)*
- **MillerColumns 위치 고정**: 팝오버 바깥 박스를 컬럼 수 무관 *상수(600×320)*로 — 넓은/좁은 모드 컨테이너 고정 높이 320 + 컬럼 `.erpColListFill`(height100%, 내부 스크롤). 컬럼 증식 시 floating-ui 재배치(이동) 제거. (Cascader 단일 컬럼은 max-height 280 유지 — 클래스 분리.)
- **Popover `reposition`(flip/fixed)**: fixed=flip 끄고 shift만(`middlewares`) → content 크기 변해도 위치 고정. MillerColumns가 fixed 사용.
- 컬럼형 트리거 정렬 보정(이전 0.34 후속) — align-items center + 아이콘 래퍼 line-height 0.

**카운트 정정 (v0.11.0 → v0.16.0):**
- **의미 원자 +2 = CountBadge**(알림 카운트)·**CurrencyInput**(돈 입력 — ₩ prefix·천단위 콤마·무소수, NumberInput 형제: 옵션-스태킹 대신 named 부품). ObjectCard `layout`/`summary` 폐기(단일 사진카드)·`status/headline/attributes` 역할 슬롯. Image `size` +1(fill).
- 신규 props: AppShell·TabBar `count`, Accordion `tone:'attention'`/`clearAttentionOnOpen`, FileUploader `FileItem.file`.
- **스키마 FieldType +1 = `'currency'`**(검증은 number와 동일 z.number, 표현만 ₩·콤마 → FormSection이 CurrencyInput로 매핑).
- SectionHeader 3존(controls 좌측→우측 고정)·Breadcrumb 마지막크럼 우선 = **02 정합 반영**.
- 패키지 버전 0.11.0→0.12.0(뷰 전환·카드 재작성)→0.13.0(CountBadge)→0.14.0(funnel·헤더)→0.15.0(Accordion 틴트 해제)→0.15.1(틴트 픽셀 정합)→0.16.0(CurrencyInput 돈 입력)→0.17.0(HE 검색 좌측·목록 단일화·Tree 인라인 ✕✓·Accordion 틴트 색)→0.17.1(목록 `비고` 컬럼 — headline.note 배지 복원)→0.32.0(HE 디렉토리·제품 공존·＋드롭다운·셀타입 chevron·DataTable grow/maxWidth·Badge min-width·breaking: HE page/onAddChild·Tree onAddChild)→0.33.0(Accordion 강조 그림자화·FieldGrid node 정합)→0.34.0(Modal/Drawer xl·full·Cascader 재정비·MillerColumns 신설·Popover xl·align)→0.35.0(PaperModal 신설·MillerColumns 위치 고정·Popover reposition)→0.36.0(PaperModal 재정의 — 순수 A4 문서 뷰어: JS 실측 fit·종이 hug·무스크롤 · FieldGrid size sm/md/lg · MillerColumns 박스 동적화)→0.37.0(Popover width=auto·reposition=anchored · MillerColumns 폭 내용 동적·앵커 완전 고정)→0.38.0(PaperModal 자세히/전체 뷰 토글 · 모달 폭 가로 A4 고정 · 가로 데모 삭제)→0.39.0(PaperModal 인쇄 빌트인 — A4 1:1·1장·디바이더 크리스프)→0.40.0(AppShell 모바일 — 아바타-only 프로필·네이티브 앱 스크롤·viewport-fit 안내)→**0.41.0**(MillerColumns 완료 브레드크럼 말줄임·Popover block·변경 chevron 어포던스·edit 아이콘 수정). minor(신규 부품/prop·시각) + patch(회귀 수정).

### 11-16. FieldGrid(장표) + 폰트 스케일(접근성) + 줌 검수 (커밋, **publish 보류**)

> kk 실수요에서 **"엑셀 인쇄물 그대로의 테두리 격자 문서(도면)"**가 나왔다. 판단 결론(주객전도 경계): *임의 픽셀 충실 재현*은 토큰-닫힘 DSL과 정반대라 DSL에 안 넣지만, **"테두리 친 셀 격자 안에 라벨/입력칸을 배치하는 장표(帳票)"는 한국 ERP·관공서 양식의 보편 포맷**(kk 빼도 성립)이라 범용 부품으로 확보. + 고령 클라이언트 대비 폰트 스케일 레버. tsc·eslint·build EXIT=0, dev 박물관 시각 검증.

**신규 유기체 (FieldGrid):**
- 테두리 2D 셀 격자. 셀 = 고정 라벨 / 스키마 필드(`FieldSpec` 재사용) / 이미지 / 빈 칸, `colSpan`·`rowSpan` 병합. 머리표(라벨:값)·명세표(헤더+값 행)·대분류 밴드(라벨 colSpan=전체) 다 같은 `rows: FieldGridCell[][]` 모델. **라벨↔입력 쌍 강제 아님**(셀 종류 독립).
- **mode `edit`/`read` — 셀 박스 기하 모드 불변, 값 렌더러만 스왑.** 확인=작성과 *같은 입력칸을 그대로* 쓰되 편집만 차단(`inert`) → "차이는 편집 가능 여부 하나뿐"(폰트·높이·인셋·값 표현 구조적 동일). 별도 텍스트 렌더(`renderCell`) 제거. 입력칸은 자체 테두리·radius·여백을 벗겨 **셀 테두리에 흡수**(작성/확인 같은 크기), 모든 셀 라인박스를 `--typo-body-*`로 못박아 행 높이 균일.
- 에러 = `--field-border` danger + Tooltip(기하 보존, FormField 안 씀 — 라벨이 별도 셀). `mask` 파리티. **명시 예외**: raw CSS grid(`fieldgrid.css`, 임의 N열+rowSpan — Calendar 7열·Modal raw flex 동류). 모서리 radius 0(엑셀 평면). prop 표는 `_catalog` 위임.

**폰트 스케일(접근성 — 기존/크게/아주크게):**
- 타이포·간격이 *둘 다 rem*, line-height 단위 없는 배수 → **루트 `font-size` 하나로 글자+간격+Mantine 컨트롤이 비율 그대로 함께 줌**(상대 비율 고정). 6단계 타이포·컴포넌트 0줄 수정. 포털(Modal/Popover/Toast)도 `:root` 상속. `--mantine-scale`은 rem-기명 spacing 스케일 불확실로 배제, 루트 font-size 채택.
- `fontscale.css`: `:root[data-font-scale="large"]→112.5%(18px)` / `"xlarge"→125%(20px)`. 소비 앱은 `<html data-font-scale="large">` 1회(플래시 0). dev 좌측 토글로 검증.
- **전역 줌 검수**: 안 따라오는 리터럴 px 전수 점검. **CountBadge가 유일 실문제**(18/11/8px → 옆 라벨만 커지고 빨강 N만 쪼그라듦 = 접근성 역행) → **rem화**(1.125/0.6875/0.5rem). Tree 인라인 20px·터치타깃 44px는 고정이 맞음(ROW_H 36 내부정합 / 절대 하한). 모바일 하단탭 56px·Image 썸네일 고정폭은 선택적 후속(미관·모바일 한정).

**Tree 라벨 말줄임:** Tree 표시 라벨이 길면 줄바꿈 → 고정 행(ROW_H 36) 넘어 다음 행과 겹침. → **한 줄 `nowrap`+`…`+`title` 호버**(VS Code·Breadcrumb 선례). `Text` 원자가 truncate 미노출 → 격리 구역에서 `--typo-body-*` role 변수로 직접.

**카운트 정정 (v0.17.1 → 현재, publish 전):**
- 유기체 +1 = **FieldGrid**. 산출물 파일 +2(`fieldgrid.css`·`fontscale.css`). 신규 명시 예외 1건(fieldgrid.css raw grid). CountBadge px→rem·Tree 라벨 말줄임은 회귀/접근성 수정(부품 추가 아님).
- **publish 보류** — 커밋·문서·메모리만. 배포 시 0.18.0(신규 유기체+접근성, minor)로 bump 예정.

---

### 11-17. v0.44.0 산출 (게시판 템플릿 3종 + 리치 에디터 흡수 + 누적 정합)

> 사내 게시판을 도메인 무지 템플릿 3종으로 추가하고, 본문 리치 에디터를 **TipTap(헤드리스) 흡수**로 확보(흡수 전략 실행). 0.42(HierarchyCollector)·0.43(CalendarPage)은 메모리·`_catalog`에 있고, 본 절에서 README/카운트 누적 드리프트까지 정합. typecheck·lint·build EXIT=0.

**신규 템플릿 (3):**
- **BoardList** — 사내 게시판 목록. 밀도형 행(스캔 우선) + 상단 고정 공지 밴드(무테 tonal) + 필독/안읽음 강조. 분류(말머리)·검색·페이징은 데이터(노출은 소비처). 행 6열 = raw CSS grid 명시 예외, 공지/필독 솔리드 배지 = 템플릿 전용 span(Badge light가 못 냄, 01 4-D). 작성자=실명(아바타 제거).
- **BoardView** — 글 보기. 본문 = `content` ReactNode 슬롯(보통 RichText). 업무용 핵심: 필독 **읽음확인**(Progress + CTA, 분모=수신자 N) · 첨부 well · 이전/다음글 · 가벼운 댓글(1단 답글, 답글=인라인 텍스트 액션+아이콘+hover pill).
  - **답글 저작(v0.49.0).** 그전엔 `onReply(id)` 콜백만 쏘고 부품 안에선 아무 일도 안 일어나 **죽은 어포던스**였다(버튼은 보이는데 눌러도 화면 불변). 작성란이 부품 내부라 소비처가 자력으로 못 고치는 구조였음. → **중첩 인라인 답글 폼**으로 해소: 답글 클릭 시 *그 댓글의 답글들 뒤*(= 새 답글이 실제 생길 자리)에 대상 칩(`○○님에게 답글 ✕`) + 입력란이 열린다. 하단 작성란은 **새 댓글 전용으로 그대로 유지** — 답글과 다른 행위라 폼이 둘인 건 경쟁 경로가 아니다(§11-3은 *같은 행위*의 경로 중복을 막는 규칙). 근거: 답글 폼은 댓글 *안*, 새 댓글 폼은 목록 *뒤*라는 표준 구분(Semantic UI Comment; Reddit·YouTube·Facebook 동일). *한때 "작성란 하나가 이동" 안을 검토했으나 어떤 제품도 안 쓰는 형태라 기각.*
  - **`reply: boolean` → `parentId?: string`(breaking).** 부모 링크가 없어 배열 순서에 의존하던 구조 — 표시만 할 땐 버텼지만 답글을 *새로 만들면* 삽입 위치를 순서로 추정해야 해 깨진다. 대상·초안은 부품이 내부 소유하고 제출만 `onReplySubmit(parentId, body)`로 상향(CalendarPage가 태그 라벨·색을 인라인 수집해 `onTagRange`로 넘기는 것과 동형).
- **BoardWrite** — 작성. 분류·제목·**수신자(칩 프리셋 + ‘직접 지정’ 조직도 드릴, 안 C — `audiences` 트리, `exclusive`=배타)**·본문(Editor)·첨부(FileUploader)·게시옵션(상단고정→필독/댓글허용). **공개 범위는 수신자로 통일(제거)** — 같은 ‘누구’ 축 중복 해소.
  - **수신자 재설계(v0.50.0) — 칩의 *정체*를 형태로 가른다.** 프리셋 칩과 결과 칩을 **같은 `Chip`으로 같은 회색 알약**으로 그려 "같은 게 두 번" 으로 읽히던 문제. 상태 의미만 손대는 수정(선택→비활성)은 형태가 그대로라 **효과가 없었다**. 근거: M3는 칩을 4종으로 나누고 판별 기준을 *"콘텐츠를 제품이 생성했나(suggestion) vs 사용자가 입력했나(input)"* 로 못박는다 — 프리셋=suggestion, 결과=input. **`Chip.variant: 'value'|'suggest'` 신설**로 형태를 분리(값=primary 채움·✕ / 제안=아웃라인·＋), `color`에 `primary` 추가.
  - **값은 필드 표면 안에 산다.** input chip은 원래 Gmail 연락처처럼 필드 안 물건인데 맨바닥에 떠 있어 "값"으로 안 읽혔다. → `_fieldStyles`의 `fieldBorder` 통로를 써서 분류·제목 입력칸과 한 몸으로(= `MultiSelect`의 `PillsInput` 어휘와 동형). **별도 소제목 삭제** — `FormField` 라벨이 이미 ‘수신자’라 `수신자 N` 소제목은 라벨 중복이었다. 개수는 필드 안 우측에 조용히.
  - **빠른 추가는 남은 제안만.** 담긴 프리셋은 비활성으로 남기지 않고 목록에서 뺀다(suggestion의 정의에 맞고 회색 시체가 안 쌓임). 담기=`add` 한 경로 / 해제=✕ 한 경로(§11-3).
  - **포섭(subsumption) 규칙.** 그룹을 담으면 하위는 *이미 포함*이다 — 토큰은 **그룹 하나로 유지**하고(명단 박제 방지: 팀원이 늘어도 수신 범위가 따라온다) 하위 트리 행은 `covered`(윤곽 체크·비활성·툴팁)로 표시. 그전엔 `디자이너`+`UI팀`이 **양방향 모두 중복 토큰으로 쌓였다**(데모 기본값이 그 상태였음). `add`가 상향(하위 토큰 흡수)·하향(조상에 포함되면 무동작) 양쪽을 정리. 트리 행 상태 3종 = `added`(직접 담김·채운 체크) / `covered` / `open`.

**신규 분자 (2 — 리치 에디터 한 짝, TipTap 흡수):**
- **Editor** — 리치 텍스트 작성기. **TipTap v3(헤드리스, ProseMirror) 엔진 격리 흡수**(@tiptap/* = Editor·RichText 2파일에만, 헌법 7 — Mantine 격리와 동형). 툴바·서식은 우리 토큰(무테, `editor.css`). `features` 닫힌 세트(bold·italic·heading·bulletList·orderedList·quote·link·image·table·divider)로 소비처가 부분집합 선택(옵션 스태킹 아님 — CellType/FieldSpec 동형, 새 기능=큐레이션). 출력 HTML. immediatelyRender:false(Next SSR).
- **RichText** — 저장 HTML 읽기 뷰어 = Editor의 짝(같은 스키마 → ProseMirror 새니타이즈, dangerouslySetInnerHTML 안 씀). BoardView 본문이 소비. **헤드리스 선택 근거**: Toast UI Editor는 자기 UI·스타일을 들고 와 토큰 충돌 → TipTap이 헌법(토큰·무테·완전통제) 정합.

**DSL 전역 변경:**
- **Pagination 무테화** — 페이지 버튼 테두리 제거(비활성 무테/활성 톤 채움). 무테 테넷("윤곽은 구조 구분선·입력칸에만") 정합, DataTable·ListPage·LedgerPage 공통.
- **신규 아이콘 5** — `message`(댓글)·`arrow-back-up`(답글)·`quote`·`list-numbers`·`photo`(에디터 툴바). Tabler 원본 path 큐레이션(헌법 4). 광학 시각검증 권장.
- **신규 의존성(peer+dev) 6** — `@tiptap/react·pm·starter-kit·extension-image·extension-table·extension-placeholder`(^3.27.1, React19 OK). 소비처 설치(mantine·zod 동일).

**카운트 정정 (실측 — `_catalog` 기준):**
- 분자 22 → **26**(+NumberStepper·Editor·RichText; IconButton은 `_catalog`상 분자). 유기체 15 → **16**(+LineItemList). 페이지 템플릿 5 → **9**(+HierarchyCollector·CalendarPage·BoardList·BoardView·BoardWrite; PageGrid→Bento 프리미티브 이동). 배치 프리미티브 3 → **4**(+Bento). 의미 원자 31·아이콘 85 → **91**.
- README 5절·§3(아이콘)·§8(peerDep) 정합, §9 죽은 widget-blueprint 링크 제거. docs/04 죽은 링크 2건 정정.

**버전:** 0.43.0 → **0.44.0**(신규 템플릿 3·분자 2·신규 peerDep, minor). build EXIT=0(TipTap SSR 검증 통과).

**남은 것:** ① 정밀 드릴 raw 트리 → TreeSelect/Transfer 치환(실데이터 후) ② docs/02 확장부품 prop 표는 `_catalog` 위임 유지(의도) ③ 반응형(@container) 게시판 미적용.

---

### 11-18. v0.46.0 산출 (AppShell 3티어 반응형 재설계 + 반응형 프리뷰 하네스)

> AppShell을 2뷰(데스크탑↔모바일, 768 하나)에서 **3티어(데스크탑/태블릿/폰)**로 재설계하고, 상단바를 해체해 유틸리티를 넷바 하단으로 이관했다. 뷰포트-분기 셸을 눈으로 검증할 dev 하네스(3티어 iframe 갤러리)도 신설. typecheck·lint EXIT=0, `/dev/preview`·`/shell/frame` 200·SSR 마운트 확인. **커밋까지만(publish 보류 — relayout이라 화면 최종 확정 후 배포).**

**AppShell 3티어:**
- **상단바 해체(데스크탑·태블릿).** 헌법상 상단바 좌측은 비고(넷바가 전체 경로)·정체성은 PageHeader 소유 → 상단바에 남은 책임이 유틸리티(알림·프로필)뿐이라 해체. 둘을 **넷바 하단 유틸리티 존**(3존: 로고/메뉴grow/유틸)으로 이관 → 세로공간 64px 본문 반환. `header.collapsed=tier!=='phone'`로 폰만 슬림 상단바 유지(좌측 레일이 없어 유틸리티 담을 데가 상단바뿐 — 결정 1-a). 조사 근거: 사이드바-주도 앱셸(Linear·Slack·Notion·Vercel·VS Code)은 계정·유틸을 레일 하단에 두고 상단바를 없앤다("알림은 상단바"는 상단바가 이미 있을 때의 통념).
- **태블릿 아이콘 레일(72px).** 메뉴 = 세로 타일(아이콘+라벨, 하단탭 `tile()` 재사용), 활성 = 좌측 강조막대(primary). **로고 밴드 없음**(72px엔 정사각 마크만 들어가 부실 → 제거, 메뉴부터 시작). Popover/Menu `position="right"`.
- **브레이크포인트 768/1280.** Mantine `sm`(768)은 폰↔(태블릿·데스크탑) collapse만, 태블릿↔데스크탑은 JS 티어(`useMediaQuery` 2개)가 넷바 width(260↔72)·내용을 스왑. **1280 근거**: iPad 가로(≤Pro11 1194)를 태블릿 레일에 담기(1440은 1366px 노트북이 새므로 상한 — 뷰포트-only라 iPad/노트북 폭 구분 불가). 폰은 불변.
- **유틸리티 존 = 같은 데이터·다른 배치**(menuItems가 넷바↔탭 재배치되는 것과 동형): 데스크탑 넷바 하단 [프로필 확장행 `justify:space-between` 알림] / 태블릿 레일 하단 [알림·아바타 아이콘] / 폰 상단바 우측.
- **`logoMark` prop 재도입→재삭제.** 태블릿 레일용으로 잠시 추가했다 로고 밴드 삭제로 렌더 0 → 다시 제거(미배포, breaking 아님 — v0.9.0 "약속 안 지키는 prop 정리" 재확인).
- **크러쉬/브레이크 없음.** public prop 계약이 0.45.0과 동일(logoMark 미배포), 옵셔널 prop 가드, 신규 의존성 0(`useMediaQuery` 기존 peer). 변화는 순수 relayout(소비처가 상단바 64px 고정을 가정했다면 시각 회귀만 — 크러쉬 아님, 업그레이드 노트 1줄).

**반응형 프리뷰 하네스(dev 영구 자산):**
- **`/dev/preview`** — AppShell+페이지를 3티어 iframe으로 본다. **iframe 필수 근거**: 셸은 `@container`가 아니라 뷰포트 `useMediaQuery`로 분기 → 한 화면 안 폭만 다른 div는 전부 바깥 뷰포트로 평가돼 소용없음. iframe은 자체 뷰포트라 각 폭에서 미디어쿼리 정확히 발화. 비교 모드(3티어 스케일다운 대조) + 집중 모드(1:1 + 우측 드래그 핸들로 폭 실시간·768/1280 경계 튜닝). 페이지 선택(_registry Demo 재사용 — home/LedgerPage/HierarchyExplorer/CalendarPage/Board 3종).
- **`/shell/frame?page=`** — iframe 타깃(셸+페이지). `/shell` 아래 둬서 `/dev` 박물관 레이아웃 미상속(셸 단독). 도메인-제네릭 샘플 데이터(dev 앱 소유, 라이브러리 아님).
- 박물관 '기초'에 '반응형 프리뷰' 등재, AppShell 데모(_registry)를 `/dev/preview`로 링크(ListPage→/customers 선례).

**카운트/버전:**
- 부품 추가 0(AppShell 재설계·기존 유기체). 신규 prop 0(logoMark 순증감 상쇄). 신규 토큰 0. 신규 산출물 파일 0(appshell.css에 `.erp-rail-item` 규칙 추가).
- **02 정합**: AppShell 섹션 3티어로 갱신(prop 표·티어 표·브레이크포인트·상단바 해체·레일). README 5절 AppShell 라인 갱신. `_catalog` AppShell 엔트리 갱신(role·composition +Menu·CountBadge·Group).
- **버전 0.45.0 → 0.46.0**(셸 레이아웃 변경, minor·API break 없음). (0.45.0=CalendarPage 태그·드래그선택은 `_catalog`·커밋엔 있으나 03 절 미기재 — 잔여 드리프트.)

**남은 것:** ① **페이지 구성 리플로우의 주인** 결정(2컬럼→1컬럼을 셸 컨텍스트 vs 템플릿 `@container` vs Grid 반응형 중 뭐로 — 셸 틀 잡혔으니 다음 대상) ② 태블릿 레일 밀도·유틸 간격 시각 미세조정 ③ publish(화면 최종 확정 후) ④ `/shell` 단일-티어 도그푸드 정리 여부.

---

### 11-19. v0.51.0 산출 (모바일 계열 신설 — MobileShell 외 7부품 + 캘린더 축 중립화)

> 모바일을 AppShell의 *축소판*이 아니라 **형제 층**으로 분리하고, 그 위에 올릴 모바일 어휘 7부품을 신설했다.
> 조사 기반(Toss TDS · Apple HIG · Material 3)으로 설계했고, 기각한 안까지 남긴다. typecheck·lint·build EXIT=0.

**왜 분리인가 (삭제가 아니라).** 데스크탑 전제 page 템플릿(PageHeader + bento 면 + elevation)을 폭만 좁혀 넣으면 성립하지 않는다 — `/shell/m/legacy`에 **반례 화면**을 남겨 이걸 눈으로 확인할 수 있게 했다(PageHeader 우측 액션 충돌 / DataTable 가로 넘침 / 면이 헤어라인 체계와 안 섞임). 셸만 반응형으로 만들어도 안쪽 어휘가 안 따라오므로 층 전체를 갈랐다.

**시각 체계 — 무테 지향의 반대.** 면·그림자를 안 쓰고 **배경 + 가로 헤어라인**으로만 나눈다. tenet과 충돌하지만 *모바일의 정체성*이라 정체성이 이긴다(선례: [[tenet-vs-identity]]). 근거: TDS 코어 11부품에 **`Border`가 독립 부품**으로 있고 리스트 어휘가 `ListRow`·`ListHeader`인 것 / Apple HIG가 inset grouped(둥근 카드 묶음)를 *"compact 폭, 특히 현지화 콘텐츠에서 텍스트 줄바꿈을 유발한다"* 며 말리는 것(iPhone 세로=compact, 한국어=localized).

**부품 8종.** `MobileShell`(Navigation + 본문 + 하단 고정 CTA + 탭) · `MobileTop`(화면 제목 — **보조 설명 슬롯 없음**) · `MobileSection`(제목 + 자유 슬롯 + 헤어라인) · `MobileListRow`(탐색형 행) · `MobileStatRow`(KPI 행) · `MobileDisclosure`(펼침 행) · `MobilePhotoPicker`(썸네일 격자) · `MobileCalendar`(월 달력).
- **입력은 부품을 만들지 않았다** — `FormField`가 이미 라벨·에러·`--field-border` 통로를 갖고 있고, 부족한 건 타이포와 터치타깃뿐이었다. 둘 다 셸 스코프가 처리(아래).
- 상단 2층 구분(TDS `Navigation` / `Top`): 셸은 Navigation만 소유하고 화면 제목은 *화면*이 갖는다. 데스크탑 PageHeader의 우측 CTA는 없다 — CTA는 **하단 고정**(TDS `BottomCTA`).
- 탭 오버플로('더보기') **미승계**: HIG가 *"오버플로 탭은 피하라, 숨은 탭은 도달·인지가 어렵다"* 고 말리는 패턴이라, 소비처가 3~5개로 추려서 준다.

**모바일 타이포 스케일 — 새 기제 0.** 데스크탑 body 14px는 폰에서 심각하게 작다(iOS Body는 **17pt**). `theme.ts`에 `typographyMobile`을 두고 **셸 루트에 역할 변수를 깔아** 자손의 `Text`·`Title`·`Badge`가 전부 따라오게 했다 — 원자는 하나도 안 고쳤고, 색·타이포가 같은 통로라 기제가 늘지 않았다. 탭 라벨만 11px로 되돌림(iOS 탭 라벨은 ~10pt로 *일부러* 작다).

**모바일 웹 찐빠 봉인.** ① 문서 스크롤·고무줄 바운스 = 셸이 마운트 동안만 `<html>`에 클래스를 붙였다 뗀다(CSS로 무조건 걸면 전역 부작용) ② `overflow-x: hidden`(폰 가로 스크롤은 레이아웃 사고) ③ `touch-action: manipulation`(더블탭 확대만 차단, **핀치는 접근성상 유지**) ④ 입력 `font-size: max(16px, …)` — iOS는 16px 미만 입력 포커스 시 페이지를 자동 확대하므로 하한을 못박아 봉인 ⑤ 터치타깃은 Mantine `--input-height-*` 단계 변수를 셸 스코프에서 덮어 42→48(HIG 44pt).

**MobileCalendar — 점→스팬 바(설계 반전).** 처음 점(dot)으로 축약했다가 *"바 자체가 읽는 대상"* 이라는 지적으로 뒤집었다. 여러 날 일정은 **하나의 연속 알약**이어야 기간·겹침·연속이 읽힌다. 근거: **iOS 18이 월 뷰를 Compact/Stacked/Details로 재편하며 iOS 17의 점 표기를 교체**했다 — 취한 건 Stacked.
- **데스크탑과 데이터·알고리즘 공유**: `CalendarEvent`·`CalendarEncoding`·`CalendarAnnotation` 타입 그대로, `packLanes`는 **`_calendarLanes`에 한 벌**만 둔다(복제하면 같은 데이터가 두 화면에서 다른 순서로 쌓여 버그로 읽힌다). 주 경계 연속은 데스크탑과 같은 `clip-path` 삼각형·같은 클래스명(`cont-l`/`cont-r`).
- **화면 전체 + 동적 행 높이**: 달력을 섹션에 가두면 주 행이 46px로 짓눌려 바가 "날짜 밑줄"처럼 보인다(iOS 실측 ~87pt). `fill` 모드에서 셸 본문을 전부 차지하고, 주 행은 `flex: 1 0 <필요 높이>`로 **바쁜 주가 먼저 가져가고 남는 높이를 균등 분배**한다. `maxLanes` 기본 **6**(공간이 아니라 가독이 한계 — 라벨은 시작 칸에만 있어 6줄 넘으면 추적이 안 된다).
- 3뎁스: 달력 → (바 탭)일정 상세 / (날짜 탭)그날 목록 → 상세.

**범례 겸 필터 칩 — `Chip.variant='legend'` 신설.** 모바일 달력엔 범례가 아예 없어 색의 뜻과 배경 태그의 정체를 알 수 없었다. 축이 둘(분류/태그)이라 **가로 스크롤 한 줄 + 세로 divider**로 가른다(필터는 *카테고리 간 AND, 내 OR* 이라 실제 논리 경계다). 기각한 안: 그룹 라벨(가로 스크롤에서 라벨이 화면 밖으로 사라짐) · 두 줄(세로 공간 2배).
- **체크마크를 쓰지 않는다**: 기본이 전부 켜짐이라 체크가 모든 칩에 붙어 아무것도 구분하지 못한다(체크는 기본이 꺼짐일 때 값을 한다). 켜짐=**바와 같은 레시피**(`-2` 배경/`-8` 글자) / 꺼짐=투명+흐림.
- Mantine이 체크 상태에서 `--chip-checked-padding`(좁은 패딩)을 쓰므로 아이콘만 지우면 **크기가 신호가 돼버린다** — 패딩을 고정해 해소.

**바에서 뺀 것 / 넣은 것.** 좌측 3px 강조선 **제거**(데스크탑 포함) — 분류는 이미 톤 배경이 말하고 있어 중복 신호이고, 범례 칩과 바가 달라 보이게 만든 원인이었다([[no-decorative-subtext]]). 대신 **표식 슬롯**을 넣어 두 번째 범주 축을 실었다(색=분류 / 윤곽=상태 / 표식=두 번째 축 — 1차원이 아니다).

**`encoding.person` → `mark` (breaking).** 부품이 그 축을 *사람*으로 전제하고 있었다: 필드명 `initial`, 하드코딩 문자열 `'담당'` 2곳, **원형 아바타**(원은 사람을 함의 — 시공 종류·자재 코드가 들어가면 모양이 거짓말을 한다). → `mark: { attr, label, values: { glyph, label, color? } }`로 중립화하고 축 이름까지 소비처가 준다. 모양은 라운드 사각. 헌법 1 위반의 실사례.

**곁가지.** `Chip.variant`(value/suggest/legend) · `FileItem.url`(썸네일 주소, objectURL 회수는 PhotoPicker가 책임) · `MobilePhotoPicker`는 `capture`를 **일부러 안 준다**(주면 카메라 고정이라 갤러리 경로가 막힌다).

**남은 것:** ① **AppShell 2티어 축소**(폰 티어 삭제 + 최소 폭 상수 export) — 착지점(MobileShell)이 생겼으니 이제 가능. 경계값은 **768 잠정**(CalendarPage 타임라인이 하한을 결정 — 그 아래는 일 컬럼 24px대) ② **DataTable 가로 스크롤 부재**(모바일과 무관한 기존 결함 — `ListWidget`엔 있고 `DataTable`엔만 없다) ③ 모바일 page 어휘 확장은 rule of three로.

---

