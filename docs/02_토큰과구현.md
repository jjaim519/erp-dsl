# 02. 토큰과 구현 (구체적 값과 구현)

> 본 문서는 ERP No-Code 빌더 프로젝트의 **참조 문서**이다. 상위 원칙·규칙·색인은 「프로젝트 지침」을 따른다.
> 본 문서는 기술 스택, 디자인 토큰 값, 산출물 파일, 래퍼 패턴, 1차 원자 집합, 검증 방법을 다룬다.
> 값(색 hex·여백 px 등)은 잠정값이며 화면 검증 후 조정한다. 구조와 규칙은 「01. 규칙과 구조」를 따른다.

---

## 기술 스택과 각 도구의 역할

브라우저가 최종적으로 이해하는 것은 **HTML(뼈대) + CSS(스타일) + JavaScript(동작)** 셋뿐이다. 아래는 전부 "이 셋을 더 잘 쓰기 위한 중간 도구"다.

- **React** — 화면을 조각(컴포넌트)으로 짓는 방식. "상태가 이러면 이렇게 생긴다"를 선언하면 React가 알아서 다시 그린다. → 본 시스템 부품 철학의 세계관 그 자체.
- **JSX** — JavaScript 안에 HTML처럼 생김새를 쓰는 문법. 빌드 때 평범한 JS로 번역됨.
- **TypeScript (strict)** — 실행 전에 "여기 들어갈 값의 종류"를 검사하는 안전장치. **본 시스템의 "임의 판단 구역 좁히기"를 코드 단에서 강제하는 핵심 도구.** 잘못된 prop은 컴파일 단계에서 빨간 줄로 잡힌다. 사용자에겐 흔적도 안 남는다.
- **Mantine v8** — 이미 잘 만들어진 컴포넌트 + 토큰(theme) 시스템 묶음. **본 시스템의 베이스 층.** 단, 기본적으로 너무 열려 있어서(임의 hex/px 허용) 래퍼로 닫아야 한다.
- **Next.js (App Router)** — React 조각들을 주소별 페이지로 묶어 굴리는 틀. 폴더 구조 = URL. 인터랙티브 조각은 `'use client'` 표시.
- **빌드** — JSX/TS/흩어진 조각 → 브라우저용 HTML/CSS/JS로 번역·압축하는 공장.
- (Supabase PostgreSQL, Recharts, PWA — 지금 단계(시각 부품)와는 다른 층. 나중에 연결.)

> **베이스는 새로 고르는 게 아니라 이미 Mantine으로 정해져 있었다.**
> shadcn 등은 Mantine과 스타일 시스템이 충돌하므로 쓰지 않는다.
> 진짜 작업은 "Mantine을 어떻게 **닫느냐**"이다.

---

## 지금까지 내린 구체적 결정

### 디자인 토큰 (확정 — 단, 값은 화면에서 조정 가능)

- **primary** = 미지수 그릇. 현재 navy(hue 216). 10칸 사다리(50~900), index 6이 메인.
  - 회사가 바뀌면 이 사다리만 통째로 교체 → 전체 화면이 따라온다.
- **neutral** = `f(primary)`. primary와 같은 hue, 채도만 ~7%로 죽인 차가운 회색. 10칸(50~900).
  - primary가 바뀌면 neutral의 색감도 자동으로 따라온다. (관계만 박고 값은 안 박는다)
- **상태색** = success/warning/danger/info. 각 10칸. **"A-deep(jewel-tone)" 개정(2026-07)** — Tailwind 기본 스케일이 곧 "AI/부트스트랩 어디서나 본 색"(제네릭·바이브 코딩 냄새의 주범)이라, 각 색을 미묘한 사촌으로 옮기고 한 단계 깊게(heritage/premium). index 6=메인 솔리드, theme.ts 단일 출처(배지·SummaryCard·Callout·CountBadge·text.danger 전부 자동 전파).
  - success=forest jade(`#0B7357`, 초록에 청록기·cool 정합) · warning=deep amber(`#A5700D`, **순노랑 폐기** — 예전 "더 노란 yellow" 결정을 뒤집음, 순노랑이 제네릭 최대 tell) · danger=garnet(`#B62E4A`, 청색기 도는 깊은 로즈레드·소방차빨강 탈피) · info=deep indigo(`#3A46AE`, 네이비 primary와 근접하되 구분 — "한 식구").
- **간격** = 4px 베이스. xxs(4)/xs(8)/sm(12)/md(16)/lg(24)/xl(32)/xxl(48). 임의 px 금지.
- **타이포(6단계, 묶음)** = display/heading/subheading/body/**body-strong**/caption. 각각 {크기·굵기·행간} 묶음.
  - Mantine fontSizes는 크기만 담으므로, 6단계 묶음 전체를 `theme.other.typography`에 둔다(단일 진실 공급원).
  - 폰트: **PretendardGOV**(가변). `fontFamily="Pretendard GOV Variable"`(실제 패밀리명 — 공백 포함, 과거 `PretendardGOV` 붙임표기는 이름 불일치로 적용 안 됐던 오기). 단일 가변 woff2(전 weight 45~920)를 **패키지에 동봉(self-host)** 하고 `Providers`가 `fonts.css`로 로드 → CDN 없이 사내망·오프라인에서도 동작. 폴백은 `-apple-system`·시스템.
  - 이름은 **위계/스펙 축**으로 통일한다(용도 단어 금지). `body-strong` = body와 같은 크기 + 굵게(강조). 폼 라벨이 입력값(body)보다 작으면 위계가 거꾸로이므로 같은 크기 + 굵기로 구별한다.
  - **"라벨"은 타이포 단계 이름이 아니라 *역할* 이름이다.** 어느 단계를 쓸지는 컴포넌트(맥락)가 정한다 — 폼 라벨(FormField·Label 원자)은 `body-strong`(입력값과 동급 위계), 정보 라벨(DescriptionList)은 `caption`(값보다 보조). 색 역할(`text.primary`)이 맥락 따라 다른 값에 매핑되는 것과 같은 구조. (과거 `label`이 용도 이름이라 단계=역할 혼동을 일으켜 `body-strong`으로 개명.)
- **radius** = xs(4)/sm(8)/md(16)/full(9999). full은 Mantine 기본에 없어 커스텀 추가. (v0.10.0: squircle 곡률이 보이도록 sm 4→8·md 8→16 상향 — 작은 반경은 곡률이 안 드러남. v0.42.0: 타이트한 컨트롤용 **xs(4)** 재도입.) **borderRadius 하드코딩(px·숫자)은 hex와 동급으로 린트 금지(강제 문2)** — 토큰을 바꿔도 하드코딩 px가 안 따라오는 누수를 막는다.
- **모서리 곡률(squircle)** = `corner-shape: superellipse(2)`(애플식). radius 위에 *연속 곡률*만 얹는 단일 토큰(`--corner-shape`, theme.ts). **박스 컴포넌트에만 opt-in**(squircle.css 화이트리스트) — 원/알약(Avatar·Spinner·Switch·Chip·Radio 등)은 round 유지(squircle이 원을 둥근 사각형으로 각지게 만들기 때문). border·box-shadow·outline이 모양을 네이티브로 따라가 clip-path식 충돌 없음. 미지원 브라우저(Safari/FF)는 무시 → 평범한 둥근 모서리로 graceful fallback(radius 값은 전 브라우저 동일).
- **그림자** = sm(카드)/md(모달). none은 "안 줌". (+ elevation 2축의 raised/overlay 그림자 — 아래.)
- **보더** = 굵기 1px 한 종류 + 색은 neutral 참조(기본 200, 강조 300, 포커스 primary-600). 독립 토큰 아님. **무테 지향(01 「지향」): 윤곽은 최후 — 구조적 구분선·입력칸에만, 담음·부상은 아래 surface/elevation으로.**
- **surface(면 톤) / elevation(부상) 2축 — "윤곽→음영"(v0.42.0)** = 분리를 *테두리가 아니라 톤·그림자*로 표현(무테 지향의 토큰 기제).
  - `surface`: sunken(우묵·well)/default/raised/overlay — *담음(containment)*을 톤으로. 카드 안 well은 sunken, 떠 있는 위젯은 raised.
  - `elevation`: raised/overlay 그림자 — *페이지 위로 뜸*. 위젯·오버레이만 그림자, 내부 요소는 flush.
  - 역할 변수로 노출(`--surface-*`/`--elevation-*`). HierarchyCollector가 첫 소비자(위젯=raised, 카트=sunken well).
- **아이콘 정렬 보정** = `--icon-baseline-shift`(기본 `-0.125em`). 아이콘 SVG의 광학 중심이 텍스트 baseline과 어긋나는 것을 보정하는 em 상대 상수(1/8 룰). **임의 px이 아니라 폰트 크기에 비례하는 토큰.** Icon 원자가 내장하여 텍스트와 함께 놓인 모든 아이콘이 자동 광학 정렬된다. 아이콘은 **SVG로 그린다(유니코드 글리프 금지)** — 유니코드는 브라우저·폰트마다 렌더가 달라 변동이 생기고, SVG는 viewBox를 통제하므로 환경 무관하게 일관(단 SVG도 정렬은 공짜가 아니라 위 보정 토큰이 함께 필요).
- **시맨틱 역할(텍스트·배경)** = 위 색 사다리를 *용도 이름*으로 한 겹 더 감싼 것. 컴포넌트는 사다리 칸(neutral-900)이 아니라 이 역할 이름을 참조한다.
  - 텍스트: `text.primary`(기본 본문·제목색) / `text.secondary`(보조·흐림) / `text.danger`(에러) / 필요시 `text.disabled` 등.
  - 배경: `bg.primary`(기본 면) / `bg.secondary`(표면) / `bg.tertiary`(페이지) 등.
  - **모드 분기는 이 역할 층에서 일어난다.** `text.primary`는 라이트=neutral 최상단(거의 검정), 다크=neutral 최하단(거의 흰색)으로 토큰이 매핑한다. 컴포넌트(Title 등)는 `text.primary`만 참조하고 검정/흰색 분기를 *알지 못한다*. (색이 뒤집히는 책임은 토큰이 지고, 컴포넌트는 역할 이름만 안다 — 에러 빨강을 입력칸이 boolean만 알고 실제 hex는 theme가 답하는 것과 같은 구조.)
  - 값(어느 칸에 매이나)은 화면에서 조정할 잠정값. 구조(역할 존재 + 모드 분기는 여기서)만 확정.

### 역할 변수 통로 (✅ 구현 확정 — 이번 세션)

역할에 따라 값이 바뀌는 모든 것(색·타이포·테두리·아이콘 정렬)을 단일 메커니즘 = **CSS 역할 변수**로 통일한다. 컴포넌트는 `var(--…)` 역할 이름만 부르고 실제 값·모드 분기를 모른다. (원칙: 「01」 강제 3층/역할 이름 참조. 구현 메커니즘은 본 절.)
- **모드 분기:** `cssVariablesResolver`(theme.ts)가 라이트/다크별로 `--text-*`/`--bg-*`/`--border-*`를 화면 최상단에 *깐다*. 원자는 읽기만 한다.
- **타이포:** 같은 resolver가 `--typo-{단계}-{size|weight|lh}`(6단계 묶음)를 방출. Text/Title/Label은 `var(--typo-body-size)` 식만 부른다(크기·굵기·행간을 모름).
- **상태색(에러):** FormField가 에러일 때 자식 영역에 `--field-border`를 danger 토큰으로 *덮는다*. 입력칸은 `var(--field-border, var(--border-default))`만 그려 자기가 에러인지 모른다.
- **아이콘 정렬:** `--icon-baseline-shift`(-0.125em)도 같은 통로 — Icon이 `verticalAlign`으로 소비.
- **모서리 곡률(v0.10.0):** `--corner-shape`(superellipse(2))도 같은 통로 — `squircle.css`가 박스 컴포넌트에서 `corner-shape: var(--corner-shape)`로 소비(원형 제외, 미지원 브라우저는 graceful fallback).
- 이 통로 외에 색·타이포를 부품에 주입하는 다른 길은 없다(임의 hex·style은 린트·타입이 차단).

### 산출물 파일 (✅ 검증 완료)

- `theme.ts` — 위 토큰을 Mantine `createTheme`로 담은 파일 + `cssVariablesResolver`(역할 변수 방출 — 위 「역할 변수 통로」; `--corner-shape` 포함). radius(sm8/md16/full)·`cornerShape`(squircle) 토큰의 단일 출처. `src/ui/` 안에 둔다.
- `mantine.d.ts` — 커스텀 색 이름, 추가 size 키(full/xxs/xxl), theme.other 타입을 TypeScript에 선언. **자동완성 힌트(지도) 역할.** tsconfig include에 잡혀야 함.
- `eslint.config.mjs` — 강제 규칙(그물 2). 문 1(Mantine 직접 import 금지) + 문 2(hex 금지).
- **격리 구조** — Mantine에 닿는 모든 것(컴포넌트·CSS·Provider·ColorSchemeScript)은 `src/ui/` 안에. 바깥은 배럴(`src/ui/index.ts` → `@/ui`)만 import. Provider 배선도 `src/ui/Providers.tsx`에 둔다.
- `Providers.tsx` — MantineProvider에 theme + `cssVariablesResolver` + `defaultColorScheme` 주입. CSS import도 여기(격리): `fonts.css`(폰트) · `@mantine/core/styles.css` · `@mantine/dates/styles.css` · `@mantine/notifications/styles.css` · `squircle.css`(모서리 곡률). 토스트 `<Notifications position="top-right"/>`도 마운트.
- `squircle.css` (v0.10.0 신설) — 박스 컴포넌트에 `corner-shape`(애플식 squircle)를 opt-in으로 적용하는 전역 CSS. 화이트리스트(Button·Paper·Input·Modal·Popover·SegmentedControl)라 원형(Avatar·Spinner·Switch·Chip·Radio 등)은 자동 제외. Providers가 로드(격리, 헌법 7).
- `src/ui/fonts.css` + `src/ui/fonts/PretendardGOVVariable.woff2` — PretendardGOV 가변 폰트 self-host(@font-face). 패키지 동봉이라 CDN 의존 0(사내망 OK). 5.4MB(전 weight 단일 파일) — 용량을 내주고 차단 무관 동작을 산 트레이드오프(내부망 맥락에선 LAN+swap+캐시로 체감 작음).
- `_fieldStyles.ts` — 입력칸 테두리 통로(`var(--field-border, var(--border-default))`)를 입력 원자들이 공유.
- `_cells.tsx` — 유기체 공유 패턴(셀 type enum·`Action` 타입·`renderAction` 헬퍼). 부품 격리 구역 안.
- `src/schema/` — **데이터 세계**(부품 격리 구역 `src/ui`와 분리). `fields.ts`(FieldSpec·FieldType — pattern/mask/조건부 포함) + `validation.ts`(buildZodSchema·isFilled, Zod) + `index.ts`. 도메인 진입로.
- `src/ui/_masks.ts` — 마스크 카탈로그(명명 enum→입력 변환, 큐레이션). 현재 phone(KR). 마스킹은 렌더 측, 검증(pattern)은 스키마 측 — 별개 축.
- `src/ui/notify.tsx` — 토스트 명령형 API. `@mantine/notifications`를 격리(바깥은 notify만). Providers에 `<Notifications/>` 마운트.
- 신규 의존성: `@mantine/notifications`(토스트용 공식 패키지, src/ui 격리).
- `src/app/customers/` — **첫 수직 슬라이스**(데모 앱, 라이브러리 아님). 목록(`page.tsx`)·상세(`[id]/page.tsx`)·데모 도메인(`_data.ts` — FieldSpec[]·rows·mockResolvers(patch)). 부품은 `@/ui`·`@/schema`만 import.

**검증된 환경 디테일 (재현용):**
- Next.js(App Router) + TS strict, Mantine v8, ESLint 9 flat config, npm.
- ⚠️ **ESLint 9 flat config는 `files: ['**/*.ts','**/*.tsx', ...]`를 명시해야 .ts/.tsx를 린트한다.** 안 하면 디렉터리 린트 시 .js/.mjs/.cjs만 훑어서 규칙이 *안 돌고도 "에러 0"으로 거짓 통과*한다. (→ 「검증 방법」 절)
- flat config엔 `@typescript-eslint/parser`, `ignores`(`.next/` 등) 필요.
- Mantine v8은 `postcss.config.cjs` 필요. hydration 경고는 `mantineHtmlProps`로 해결(본 DSL과 무관한 Mantine SSR 배선).

### 패키지·배포 (✅ GitHub Packages publish 완료 — `@jjaim519/erp-dsl`)

라이브러리를 **GitHub Packages npm 레지스트리에 publish하는 진짜 패키지**로 만들었다. 소비 레포(ERP)는 이걸 **import만** 하고 절대 수정하지 않는다 — 카탈로그 확장은 본 라이브러리 레포에서만(헌법 4가 레지스트리 경계로 강제됨).

- **이름**: 스코프형 `@jjaim519/erp-dsl`(GitHub Packages는 스코프=오너명 필수, 소문자). `publishConfig.registry=https://npm.pkg.github.com`. 현재 버전 **0.59.0**.
- **발행 경로 = CI 단일화**: `v*` 태그 push → `.github/workflows/publish.yml`이 `npm publish`(인증은 워크플로 `GITHUB_TOKEN`). 로컬 수동 publish는 제거됐다(커밋했는데 배포 누락되는 사고 방지). 즉 **발행 트리거는 태그 하나**.
- **소스 배포 + transpilePackages**: 빌드 산출물(dist) 없이 `.tsx` 원본을 그대로 publish하고, 소비 앱(Next)이 `transpilePackages: ['@jjaim519/erp-dsl']`로 컴파일. `exports`: `.`(부품 `src/ui/index.ts`) / `./schema`(`src/schema/index.ts`).
- **peerDependencies**: `@mantine/*`·`react`·`react-dom`·`dayjs`·`zod` + **흡수한 헤드리스 엔진 2종**(`@tiptap/*` — Editor/RichText / `@tanstack/react-table` — ListWidget)을 소비 앱이 설치(패키지엔 dependencies 0). 로컬 빌드용으론 devDependencies에도 둠. **규율: 배포 대상(`src/ui`·`src/schema`)이 import하는 외부 패키지는 반드시 peer에 선언한다** — dev에만 있으면 우리 레포는 통과하는데 소비 앱에서만 해결 실패한다(`@tanstack/react-table`이 실제로 그 상태였고 v0.59.0 정합에서 교정).
- **publish 대상 격리**: `files` 화이트리스트(`["src/ui","src/schema","README.md","!src/ui/_dev.ts","!src/ui/_Dev*"]`)로 publish 셋 = `src/ui`(부품) + `src/schema` + 폰트 woff2 + README. 데모 앱(`src/app`)·문서(`docs/`)는 화이트리스트 밖이라 자동 제외, dev 갤러리(`_dev.ts`·`_Dev*`)는 명시 제외. dev 갤러리는 공개 배럴(`index.ts`)에서도 빼고 `_dev.ts`/`_registry.tsx`로 분리(데모만 사용). (v0.2.0 초기엔 `.npmignore` 방식이었으나 v0.9.0 현재 `files` 화이트리스트로 전환 — 포함을 명시하는 쪽이 누수 위험이 작다. **단 `_catalog.ts`·`_registry.tsx`는 `_dev`/`_Dev*` 패턴에 안 걸려 현재 tarball에 포함됨 — 공개 dist 시 추가 제외 필요.**)
- **자기완결**: 라이브러리 내부의 `@/schema` 별칭을 상대경로(`../schema`)로 바꿔 패키지가 alias 없이 동작.
- **소비측 배선**: ERP 레포 루트 `.npmrc`(스코프 레지스트리 + `write/read:packages` 토큰) → `npm i @jjaim519/erp-dsl` + peer 설치 → `transpilePackages` → 루트 레이아웃 `Providers`로 감싸기.
- **갱신 루프**: 라이브러리 레포에서 코드 반영 → `npm version`(같은 버전 재publish 불가) → `npm publish`(prepublishOnly가 typecheck+lint 선행) → ERP에서 `npm i …@latest`.


### 래퍼 패턴 (✅ 구현·검증 완료 — Button)

각 Mantine 컴포넌트를 얇게 감싸 열린 prop을 잘라낸다. **핵심: Mantine 타입을 상속하지 않고 Props를 손으로 쓴다** (그래야 `(string & {})` 탈출구가 안 딸려온다).
```tsx
// src/ui/Button.tsx (요지)
type ButtonVariant = 'primary' | 'secondary' | 'danger' | 'ghost';
type ButtonSize = 'sm' | 'md';
type ButtonProps = {              // Mantine ButtonProps를 extends 하지 않음 (의도)
  variant?: ButtonVariant; size?: ButtonSize; children: ReactNode;
  leftIcon?: ReactNode; rightIcon?: ReactNode; loading?: boolean;
  disabled?: boolean; fullWidth?: boolean; type?: 'button' | 'submit';
  onClick?: () => void;
  // className / style 은 의도적으로 노출 안 함 (우회 차단)
};
const VARIANT: Record<ButtonVariant, { color: string; mantineVariant: string }> = {
  primary:   { color: 'primary', mantineVariant: 'filled' },
  secondary: { color: 'neutral', mantineVariant: 'default' },
  danger:    { color: 'danger',  mantineVariant: 'filled' },
  ghost:     { color: 'neutral', mantineVariant: 'subtle' },
};
// radius="sm" 등은 래퍼 내부에 정책으로 고정. 바깥에서 못 바꾼다.
```
- 래퍼는 색 *역할 이름*(`'primary'`)만 안다. navy 같은 실제 hex는 모른다(theme가 답한다).
- 바깥에선 `<Button variant="primary">`만 쓸 수 있다. color/px/radius/className은 못 넘긴다.
- 닫힘은 "바깥에 노출하는 경계(Props)"에서 일어난다. 래퍼 *내부*는 Mantine 열린 API를 만져도 된다(예: `mantineVariant: string`은 느슨해도 경계가 아니라 내부라 OK).
- 나머지 원자(Input, Badge, Select…)는 전부 이 틀의 복붙이다.

### 1차 원자 집합 (확정 — 분류·기준 완료.)

**의미 원자** (도메인 무관, 범용. "의미 고유성"으로 등록 — 빈도 아님):
- 입력: `TextInput` `NumberInput` `CurrencyInput` `Textarea` `PasswordInput` `Select` `Checkbox` `Radio` `Switch` `DatePicker` `MultiDatePicker`
- 표시·행동: `Button` `Badge` `Chip` `Text` `Title` `Label` `Anchor` `Icon` `Avatar` `Tooltip` `Popover` `Spinner` `SegmentedControl` `TabBar` `Image`

**레이아웃 원자:** `Card`(variant로 elevated/outlined/flat 닫음) `Divider` `Container`(maxWidth로 콘텐츠 폭 천장 결정)
**배치 프리미티브:** `Stack` `Group` `Grid` (prop·기본값은 4-1)

확정된 판별 디테일:
- Input이 셋(Text/Number/Password)인 이유: *생김새가 아니라 받는 데이터·행동이 다름*. variant가 아니라 별개 원자.
- `Switch`(on/off 한 개) ≠ `Tabs`(화면 전환, 유기체).
- `Text`(본문 3: body/body-strong/caption) / `Title`(제목 3: display/heading/subheading) — 6단계를 나눠 가짐.
- `Label` = "무언가의 이름표"(입력칸 전용 아님). 항상 짝이 있어야 함(Text는 혼자 섬).
- `Badge`(표시 전용) ≠ `Chip`(행동 있음: 선택/삭제).
- `Anchor` = 이동 의미 고유 → Button만으론 대체 불가라 등록(빈도 낮아도).
- `MultiSelect`는 원자 아님 → Select+Chip *분자*. `로딩버튼`도 Button+Spinner 분자.
- `Spinner`는 독립 원자(Button의 loading과 별개 — 페이지/영역 로딩용).
- `StepperBar`/`ProgressBar`는 원자 아님 → 노드+연결선의 *유기체*(연결선은 원자가 아니라 유기체 내부 배치).
- `Popover` = Tooltip(hover·텍스트)의 형제. trigger가 클릭이고 content가 *슬롯(부품)*인 점이 다름. content는 raw ReactNode 슬롯으로 **Modal children과 동형**(방식 A — Popover는 안이 뭔지 모름). 자식을 받지만 의미 원자 옆에 두되 "content 슬롯을 받는 예외 원자"로 명시. 안의 스타일 우회는 인지(부품만 조립)로 막고 hex는 eslint가 차단.
- `Image` = 콘텐츠 이미지(제품사진·도면썸네일·로고). Avatar(사람 식별용 원형·이니셜 폴백)와 별개 — 임의 콘텐츠 이미지의 고정 종횡비 박스. 자식 안 받음(순수 의미 원자).

원자 판별 기준·경계 닫기 규칙·가변/고정은 「01. 규칙과 구조」 참조. 각 원자의 개별 prop은 아래 「원자 prop 규격」 절에서 확정(Button 포함 **25개** 의미 원자 + 레이아웃 3 + 프리미티브 3 — prop 표 실측 기준. Stage5에서 Popover·Image 추가 22→24, v0.3.0~에서 MultiDatePicker 추가 24→25; 0.11~0.16에서 Skeleton·Progress·Combobox·TimePicker·CountBadge·CurrencyInput 추가 → **31**(신규 6종 prop 규격은 `_catalog`·README 5절·「04」가 단일 출처). 원자 전부 래퍼 구현·검증 완료(tsc·eslint·강제 발화).

## 원자 prop 규격 (✅ 확정 — 구현·검증 완료)

> 「1차 원자 집합」에서 분류·판별이 끝난 각 원자의 노출 prop을 확정한다. 컬럼은 `prop / 범주(스타일·콘텐츠·기능) / 값 / 노출`. ❌(미노출)에는 *왜 닫는가*를 적는다 — 래퍼 고정 / FormField 소유 / 스키마로 / 규칙 3 / 경쟁 경로 제거 등.
>
> 범주는 「01. 규칙과 구조」 4-B의 선결 분류(스타일·구조 / 콘텐츠 / 기능·데이터)를 따른다. 닫기 규칙은 스타일/구조 prop에 걸리고, 데이터 prop엔 규칙 2(경쟁 경로)만 걸린다.

### 입력군을 가로지르는 횡단 규칙 4개

입력군 9개는 아래 4개 규칙을 공통으로 적용한 뒤, 원자별 고유분만 더한다. 각 표의 ❌ 사유는 이 규칙들의 약칭이다.

1. **장식·검증의 표현은 FormField(분자)가 소유** — `label`·`description`·`withAsterisk`·별표는 물론, **에러 표현(메시지 + 에러 시 빨간 테두리)도** 입력 원자에서 벗긴다. 입력칸은 입력에만 집중한다. (입력칸은 자기가 에러인지 모른다 — 테두리 색은 FormField가 CSS 역할 변수로 덮어 그린다. → 「FormField」 절)
2. **controlled 전용** — `value`/`onChange`(또는 `checked`)만 노출하고 `defaultValue`/`defaultChecked`(uncontrolled 입구)는 벗긴다. 상태 주인은 렌더러 하나여야 한다.
3. **검증의 진실은 스키마(Zod)로** — "이 필드가 받을 값의 제약"(NumberInput `min`/`max`/`step`, Select `clearable`=비울 수 있나, DatePicker 선택 가능 날짜 범위)은 원자 prop에 두지 않는다. 원자에도 두면 같은 진실이 두 곳(원자+스키마)에 생긴다. **error도 검증의 산물이므로 같은 이유로 원자 밖이다** — min/max를 스키마로 민 것과 동일한 처리.
4. **`className`·`style` 안 노출(규칙 3) + raw `ReactNode` 구멍 안 엶** — 어댄먼트(`leftSection`/`rightSection`)·인라인 라벨 등은 닫거나 `string`으로 좁힌다. 정말 필요해지면 raw 노드가 아니라 `unit?: string`처럼 좁힌 형태로 연다(rule of three).

---

### 표시·행동 원자

#### Button (✅ 구현·검증 완료 — 위 「래퍼 패턴」의 코드 요지 참조)

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `variant` | 스타일 | `'primary' \| 'secondary' \| 'danger' \| 'ghost' \| 'accent'` | ✅ |
| `size` | 스타일 | `'xs' \| 'sm' \| 'md'` (밀도 — 높이 28/32/40, 좌우여백 = 간격 토큰 sm/md/lg = 12/16/24. **좌우 ≈ 세로 여백의 2배**를 지킨다 — 안 지키면 한글 2음절에서 버튼이 정사각이 된다) | ✅ |
| 글자 크기 | — | **전 사이즈 본문 단(14px) 고정.** size는 밀도만 정한다 | ❌ |
| `children` | 콘텐츠 | `ReactNode`(텍스트/아이콘) | ✅ |
| `leftIcon` `rightIcon` | 콘텐츠 | `ReactNode`(아이콘으로 좁힘) | ✅ |
| `loading` | 스타일 | `boolean`(경쟁 경로 없음 → 노출) | ✅ |
| `disabled` `fullWidth` | 스타일 | `boolean` | ✅ |
| `type` | 기능 | `'button' \| 'submit'` | ✅ |
| `onClick` | 기능 | 콜백 | ✅ |
| `color` `radius` | — | 래퍼 고정(역할 이름·radius=sm 정책) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Badge

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `color` | 스타일 | `'neutral' \| 'success' \| 'warning' \| 'danger' \| 'info'`(primary 제외) | ✅ |
| `children` | 콘텐츠 | `string` | ✅ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Chip

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `color` | 스타일 | 상태색 + `neutral` enum | ✅ |
| `selected` | 기능 | `boolean`(controlled — 부모가 줌) | ✅ |
| `onChange` | 기능 | 콜백(클릭 신호 — controlled 짝, 상태는 부모 소유) | ✅ |
| `onRemove` | 기능 | 콜백(있으면 X를 알약 안 우측에 붙여 노출 — Icon `x`) | ✅ |
| `children` | 콘텐츠 | `string` | ✅ |
| `defaultSelected` | — | uncontrolled 입구 제거(상태 주인은 부모 하나 — 입력군 횡단규칙 2와 동형) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Text

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `variant` | 스타일 | `'body' \| 'body-strong' \| 'caption'` | ✅ |
| `color` | 스타일 | 시맨틱 텍스트 역할 `'primary' \| 'secondary' \| 'danger'` | ✅ |
| `children` | 콘텐츠 | `string` | ✅ |
| `className` `style` | — | 규칙 3 | ❌ |

> `color`는 브랜드 primary가 아니라 *텍스트색 역할*이다(primary=기본 텍스트색, 검정/흰색 자동). 에러·보조 텍스트 때문에 색이 실수요라 노출한다.

#### Title

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `variant` | 스타일 | `'display' \| 'heading' \| 'subheading'` | ✅ |
| `children` | 콘텐츠 | `string` | ✅ |
| `color` | — | 고정(`text.primary` — 검정/흰색 자동). 제목 색은 거의 항상 기본 → Text와 비대칭 | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Label

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `children` | 콘텐츠 | `string` | ✅ |
| `htmlFor` | 기능 | 짝 연결 배선 | ✅ |
| `variant` `color` | — | 고정(타이포 `body-strong` 단계, `text.primary`). "폼 라벨" 역할이라 body-strong(입력값과 동급 위계) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Anchor

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `href` | 콘텐츠 | 이동 대상 | ✅ |
| `children` | 콘텐츠 | `string` | ✅ |
| `variant` `color` | — | 고정(링크 스타일: 색+밑줄) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Icon

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `name` | 콘텐츠 | 아이콘 세트 식별자 | ✅ |
| `size` | 스타일 | 밀도 토큰 `'sm' \| 'md' \| 'lg'` | ✅ |
| `color` | 스타일 | 텍스트색 역할 enum | ✅ |
| `className` `style` | — | 규칙 3 | ❌ |

> Icon은 **SVG**로 그리고(유니코드 글리프 금지), `--icon-baseline-shift`(-0.125em) 보정을 내장한다. 그래서 텍스트와 **인라인으로** 나란히 놓일 때 광학 중심이 자동으로 맞는다(드롭다운 체크·칩 X·캐럿 등). 정렬은 SVG라서 공짜가 아니라 이 보정 토큰이 만든다. **단 `vertical-align`은 flex 아이템엔 무시되므로**, Group으로 아이콘+텍스트를 묶는 자리(DataTable 헤더 정렬 화살표 등)에서는 보정이 죽는다 → 그 자리만 **같은 토큰을 `transform: translateY(calc(-1 * var(--icon-baseline-shift)))`로 복원**(헤더에 구현, 매직넘버 없이 토큰 재사용. 버튼·칩 등 align-center로 충분한 곳은 안 건드려 회귀 0). 세트는 사람이 큐레이션으로만 늘린다(헌법 4): tone 매핑용 `info`·`alert-triangle`, 알림용 `bell` 추가. **Stage5에서 ERP 실수요 62종 일괄 큐레이션(23 → 85)** — 전부 **Tabler 원본 path**(기존과 동일 스타일: 24그리드·stroke 2·round). 다중 `<path>`는 bounding-box(`fill=none`) 제외하고 단일 `d`로 합쳐 기존 단일 path 렌더와 호환. kk 요구 7종(`home`·`users`·`building`·`store`(=tabler building-store)·`book`·`package`·`database`) 포함. 추가 62종을 함수별로: 셸·내비(home·dashboard·menu·settings·logout·user·users·building·store·book·external-link·help·arrow-right), 액션·툴바(download·save·copy·print·send·dots-vertical·adjustments·history), 상태(check-circle·x-circle·clock·ban·lock·lock-open), 문서·서식(file·file-text·file-invoice·files·folder·clipboard·receipt·report), 재고·물류(package·box·truck·warehouse·barcode·cart·database), 영업·재무(coin·won·credit-card·calculator·tag·discount·chart-bar·chart-line·wallet), 고객·연락(phone·mail·map-pin·id-card), 데이터·표(paperclip·link·star·list·table·sort-asc·sort-desc). 각 path는 SVG 렌더(PNG)로 시각 검증 후 삽입, IconName union으로 미등록 이름은 tsc가 거부.

#### Avatar

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `src` | 콘텐츠 | 이미지 경로 | ✅ |
| `children` | 콘텐츠 | 이니셜 폴백(`string`) | ✅ |
| `size` | 스타일 | 토큰 | ✅ |
| 모양(원/사각) | — | 고정 | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Image

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `src` | 콘텐츠 | `string`(이미지 경로 — placeholder류, 닫기 대상 아님) | ✅ |
| `alt` | 콘텐츠 | `string`(접근성 필수) | ✅ |
| `fallbackSrc` | 콘텐츠 | `string`(로드 실패 시) | ✅ |
| `fit` | 스타일 | `'cover' \| 'contain'`(기본 cover) | ✅ |
| `radius` | 값(토큰) | `'sm' \| 'md' \| 'full'` | ✅ |
| `size` | 스타일(밀도) | `'sm' \| 'md' \| 'lg' \| 'full'`(sm/md/lg=고정 종횡비 박스 폭 / **full=컨테이너 폭 100%**, 둘 다 4:3 비율 잠금) | ✅ |
| 높이 | — | 폭 × 3/4(4:3 박스, 비율 도출) | ❌ |
| `className` `style` `w`/`h`(임의 px) | — | 규칙 3 / 토큰 우회 | ❌ |

> **명시 예외 — 고정치수.** 「01」의 "어떤 부품도 자기 폭을 임의로 정하지 않는다(폭은 Container가 분배)" 원칙에 대한 *고정치수 예외*다. 이미지는 텍스트 블록과 달리 고유 종횡비를 가진 콘텐츠라 컨테이너 폭에 맡기면 거대해지거나 찌그러진다(Avatar가 이미 size 토큰으로 치수를 고정한 전례와 동형). **값은 sm/md/lg 토큰으로 닫혀 임의 px 누수 없음.** size가 박스를 고정하므로 `fit`(cover/contain)이 의미를 가진다(폭만 고정하면 fit이 무용).
> **`size='full'`(v0.11.0):** 고정 px이 아니라 *부모가 분배한 폭*(100%)을 받되 4:3 비율만 잠근다 — 임의 px이 아니라 닫힌 값 하나. ObjectCard 카드(상세) 뷰의 풀폭 배너 썸네일용. "폭은 위에서 분배" 원칙과 오히려 정합(고정치수 예외에서 한 발 더 헌법 쪽으로).

#### Tooltip

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `label` | 콘텐츠 | 설명 텍스트(`string`) | ✅ |
| `children` | 콘텐츠 | 감쌀 대상 | ✅ |
| `position` | — | 고정(자동 배치) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Popover

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `children` | 콘텐츠 | trigger(감쌀 대상) | ✅ |
| `content` | 콘텐츠 | `ReactNode`(@/ui 부품 — raw 슬롯, Modal children 동형) | ✅ |
| `opened` | 기능 | `boolean`(controlled) | ✅ |
| `onChange` | 기능 | `(opened: boolean) => void` | ✅ |
| `position` | 스타일 | `'top' \| 'bottom' \| 'left' \| 'right'`(기본 bottom) | ✅ |
| `width` | 값(토큰) | `'sm' \| 'md' \| 'lg' \| 'xl'`(Container 동형, px 내부 매핑·잠정. **xl=600, 다단 패널·MillerColumns 컬럼용**) | ✅ |
| `position="auto"` | — | 제외 — "명시 top"과 "auto가 고른 top"이 경쟁 경로(규칙 2) | ❌ |
| 화면 끝 자동 뒤집기(flip) | — | position과 별개 동작이라 내부 상시 ON | ❌ |
| 그림자·radius·화살표 | — | 고정(shadow md·radius md·withArrow) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

> **content는 raw ReactNode 슬롯이지만 새 예외가 아니다** — Modal `children`과 동형(이미 인정된 방식 A 경계의 재사용). 임의 텍스트가 아니라 부품이 들어가야 Popover가 Tooltip의 열화판이 안 되며, 그 안의 스타일 우회는 강제 3층(hex 차단)+인지(LLM은 부품만 조립)가 막는다. **controlled 토글은 우리가 직접(onChange)** — Mantine Target에 위임하면 이중 래핑에서 핸들러 주입이 어긋나 trigger가 죽는다(런타임 검증으로 확인). 바깥 클릭/ESC 닫기는 Mantine이 onChange(false)로 알린다.

#### Spinner

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `size` | 스타일 | 토큰 | ✅ |
| `color` | — | 고정 | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

---

### 입력군

> 아래 표의 ❌ 사유 약칭: **FormField** = 횡단 1(장식 소유), **controlled** = 횡단 3, **스키마로** = 횡단 4, **래퍼 고정** = radius·variant 정책, **규칙 3** = className·style. 각 표는 단독으로 읽히도록 골격 prop까지 모두 적는다(참조 카탈로그는 자기완결이 미덕 — 중복은 비용이 아님).

#### TextInput

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `size` | 스타일 | `'sm' \| 'md'`(기본 md) | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `placeholder` | 콘텐츠 | `string` | ✅ |
| `value` `onChange` | 기능 | controlled 전용 | ✅ |
| `name` | 기능 | 폼 배선 | ✅ |
| `label` `description` `required` `withAsterisk` | — | FormField 소유 | ❌ |
| `radius` `variant` | — | 래퍼 고정 | ❌ |
| `defaultValue` | — | controlled(uncontrolled 입구 제거) | ❌ |
| `leftSection` `rightSection` | — | 어댄먼트, 닫음(규칙 5) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### PasswordInput

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `size` | 스타일 | `'sm' \| 'md'`(기본 md) | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `placeholder` | 콘텐츠 | `string` | ✅ |
| `value` `onChange` | 기능 | controlled 전용 | ✅ |
| `name` | 기능 | 폼 배선 | ✅ |
| 가시성 토글 | 기능 | Mantine 1경로뿐이라 그대로 둠 | ✅ |
| `label` `description` `required` `withAsterisk` | — | FormField 소유 | ❌ |
| `radius` `variant` | — | 래퍼 고정 | ❌ |
| `defaultValue` | — | controlled(uncontrolled 입구 제거) | ❌ |
| `leftSection` `rightSection` | — | 어댄먼트, 닫음(규칙 5) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### NumberInput

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `size` | 스타일 | `'sm' \| 'md'`(기본 md) | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `placeholder` | 콘텐츠 | `string` | ✅ |
| `value` `onChange` | 기능 | controlled 전용 | ✅ |
| `name` | 기능 | 폼 배선 | ✅ |
| `label` `description` `required` `withAsterisk` | — | FormField 소유 | ❌ |
| `radius` `variant` | — | 래퍼 고정 | ❌ |
| `defaultValue` | — | controlled(uncontrolled 입구 제거) | ❌ |
| `min` `max` `step` | — | 스키마로(값 제약은 검증의 진실) | ❌ |
| `leftSection` `rightSection` | — | 어댄먼트, 닫음(규칙 5) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### CurrencyInput

돈 입력 전용 — `NumberInput`의 형제(Stepper 추출과 동형: 좁은 목적은 옵션-스태킹이 아니라 named 부품). `₩` prefix + 천단위 콤마(입력 중 라이브) + 무소수(`decimalScale 0`) + 컨트롤 없음. 값은 순수 `number`(저장·검증 `z.number`) — ₩·콤마는 *표시*만이고 다운스트림 표시 포맷은 `fmtCurrency` 단일출처 유지(여긴 입력 라이브 포맷). 스키마 `FieldType:'currency'` → FormSection이 매핑(검증은 number와 동일). 단위(개·m·kg)는 별개 축 — `NumberInput`+`InputGroup` 어드온. KR 로케일(₩·무소수)=잠정(phone 마스크와 동류).

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `size` | 스타일 | `'sm' \| 'md'`(기본 md) | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `placeholder` | 콘텐츠 | `string` | ✅ |
| `value` `onChange` | 기능 | controlled, `number \| string` | ✅ |
| `name` | 기능 | 폼 배선 | ✅ |
| `prefix ₩` `thousandSeparator ,` `decimalScale 0` | — | 돈 표현 고정(래퍼) | ❌ |
| `label`·`min`/`max`·`radius`·`className`/`style` | — | NumberInput과 동일(FormField·스키마·래퍼·규칙3) | ❌ |

#### Textarea

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `size` | 스타일 | `'sm' \| 'md'`(기본 md) | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `placeholder` | 콘텐츠 | `string` | ✅ |
| `value` `onChange` | 기능 | controlled 전용 | ✅ |
| `name` | 기능 | 폼 배선 | ✅ |
| `autosize` | 스타일 | `boolean`(canvas는 항상 켜짐) | ✅ |
| `variant` | 스타일 | `'field'`(기본 — 폼의 한 칸, 높이는 내용이 정함) \| `'canvas'`(*글을 쓰는 면* — 비어 있어도 6줄에서 시작해 성장) | ✅ |
| `label` `description` `required` `withAsterisk` | — | FormField 소유 | ❌ |
| `radius` `variant` | — | 래퍼 고정 | ❌ |
| `defaultValue` | — | controlled(uncontrolled 입구 제거) | ❌ |
| `minRows` `maxRows` | — | 안 노출 — 열면 소비처마다 "몇 줄짜리 본문"이 달라져 화면이 갈린다. **대신 `variant`로 두 어휘를 고르게 한다**(v0.61.0). 초판은 field 하나뿐이라 게시글 본문칸이 제목칸과 같은 한 줄로 시작했고, M3가 못박은 "큰 초기 크기 = 긴 응답이 가능·권장됨"의 신호가 통째로 없었다(실화면에서 확인) | ❌ |
| `leftSection` `rightSection` | — | 어댄먼트, 닫음(규칙 5) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Select (텍스트칸 + 선택지 골격 교집합)

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `options` | 기능(데이터) | `{ label: string; value: string }[]` | ✅ |
| `value` `onChange` | 기능 | controlled, `value: string`(단일) | ✅ |
| `placeholder` | 콘텐츠 | `string` | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `size` | 스타일 | `'sm' \| 'md'`(기본 md) | ✅ |
| `name` | 기능 | 폼 배선 | ✅ |
| `label` `description` `required` | — | FormField 소유 | ❌ |
| `radius` `variant` `defaultValue` | — | 래퍼 고정 / controlled | ❌ |
| `searchable` | — | 선택 지점화 방지 → 고정(끔) | ❌ |
| `clearable` | — | "비울 수 있나"=필수 여부 → 스키마로 | ❌ |
| 다중 선택(MultiSelect) | — | 원자 아님 → Select+Chip 분자 | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### DatePicker

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `value` `onChange` | 기능 | controlled, 날짜값(`Date`/ISO) | ✅ |
| `placeholder` | 콘텐츠 | `string` | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `size` | 스타일 | `'sm' \| 'md'`(기본 md) | ✅ |
| `name` | 기능 | 폼 배선 | ✅ |
| `label` `description` `required` | — | FormField 소유 | ❌ |
| `radius` `variant` `defaultValue` | — | 래퍼 고정 / controlled | ❌ |
| 범위·다중 선택 | — | 분자(DateRangeField)로 | ❌ |
| min/max date | — | 스키마로(선택 가능 범위는 검증) | ❌ |
| 달력 내부(보이는 달·요일 시작) | — | 래퍼 정책 고정 | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### MultiDatePicker (✅ v0.3.0~ 확장 — 구현·검증 완료)

> 여러 *개별* 날짜를 한 입력칸에서 고르는 DatePicker 형제 원자(집합 = `string[]`). 화면상 "날짜 입력 한 칸"으로 더 쪼갤 수 없고(원자), 받는 데이터만 다름(단일 vs 배열 — TextInput/NumberInput을 데이터로 가른 논리와 동일). 연속 범위(start~end)는 의미가 달라 DateRangeField 분자가 담당(경쟁 경로 아님). Mantine `DatePickerInput type="multiple"`로 조립.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `value` `onChange` | 기능 | controlled, `string[]`(개별 날짜 집합) | ✅ |
| `placeholder` | 콘텐츠 | `string` | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `size` | 스타일 | `'sm' \| 'md'`(기본 md) | ✅ |
| `name` | 기능 | 폼 배선 | ✅ |
| `label` `description` `required` | — | FormField 소유 | ❌ |
| `radius` `variant` `defaultValue` | — | 래퍼 고정 / controlled | ❌ |
| 범위 선택 | — | DateRangeField(분자)로 | ❌ |
| min/max date | — | 스키마로 | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Checkbox

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `label` | 콘텐츠 | `string`(인라인 — 컨트롤과 한 몸, **유지**) | ✅ |
| `checked` `onChange` | 기능 | controlled 전용 | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `value` `name` | 기능 | 폼 배선 | ✅ |
| `size` | — | 고정(밀도 안 노출 — 안쪽 여백이랄 게 없음) | ❌ |
| `defaultChecked` | — | controlled(uncontrolled 입구 제거) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Switch

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `label` | 콘텐츠 | `string`(인라인 — 컨트롤과 한 몸, **유지**) | ✅ |
| `checked` `onChange` | 기능 | controlled 전용 | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `value` `name` | 기능 | 폼 배선 | ✅ |
| `size` | — | 고정(밀도 안 노출 — 안쪽 여백이랄 게 없음) | ❌ |
| 트랙 radius | — | 고정 `full`(알약 — 전역 defaultRadius 'sm' 누수 차단) | ❌ |
| thumb indicator | — | 고정(끔 — Mantine 기본 안쪽 점 제거, 켜짐 상태 정렬 어긋남 차단) | ❌ |
| `defaultChecked` | — | controlled(uncontrolled 입구 제거) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

> Switch는 트랙이 알약(full)이어야 thumb 원과 곡률이 맞는다. 전역 `defaultRadius:'sm'`가 트랙에 새어들면 둥근 사각형이 되어 어긋나 보이므로 래퍼가 `radius="full"`로 고정한다. Mantine 기본 thumb 안쪽 점(indicator)은 켜짐 상태에서 색 대비로 어긋나 보여 끈다(`withThumbIndicator={false}`). 둘 다 boolean/토큰 — 임의값 아님.

> 토글 골격(Checkbox·Switch)의 라벨이 두 종류임에 주의: *필드 라벨*("배송 방법:")은 FormField 소유(벗김), *인라인 라벨*("동의합니다")은 컨트롤 콘텐츠(유지). 둘은 경쟁 경로가 아니다(인라인은 FormField가 대신 그릴 수 없음). 인라인 `label`은 지금 `string`으로 닫고, 링크 박힌 라벨이 실제 필요해지면(rule of three) "텍스트+선택적 링크" 구조로 좁혀 연다.

#### Radio

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `options` | 기능(데이터) | `{ label: string; value: string }[]` | ✅ |
| `value` `onChange` | 기능 | controlled, `value: string`(단일) | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `name` | 기능 | 폼 배선 | ✅ |
| `label` `description` `required` | — | FormField 소유(그룹 필드 라벨) | ❌ |
| `defaultValue` | — | controlled(uncontrolled 입구 제거) | ❌ |
| `size` | — | 고정 | ❌ |
| 배열 방향(가로/세로) | — | 배치 프리미티브의 일(경로 둘 방지) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

> 선택지 각각의 인라인 라벨("택배")은 `options`의 `label`이 나른다(콘텐츠, 유지). 그룹 전체의 필드 라벨("배송 방법")은 FormField 소유(벗김). Radio·Select 모두 단일 선택, `options`는 `{label,value}[]` 하나로 입구를 좁힌다(Mantine의 `string[]` 등 다른 형태 안 받음). **Radio↔Select 선택 기준(선택지 ~5개)은 소프트 관습 — 원자 prop이 아니라 카탈로그 문서에 산다(「03」 11-4).**

#### SegmentedControl

> 의미 = **같은 대상을 다르게 보는 뷰/모드 토글**(저장 데이터 아님, 화면 상태). 가로로 다 펼치고 즉시 전환. Radio/Select(데이터 선택)와 의미가 달라 별개 원자(경쟁 아님 — 소프트 관습으로 경계 명시, 「03」). Tabs 유기체와 메커니즘은 닮았으나 의미가 다름(뷰 vs 구획).

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `options` | 기능(데이터) | `{ label: string; value: string }[]` | ✅ |
| `value` `onChange` | 기능 | controlled, `value: string`(단일) | ✅ |
| `size` | 스타일 | `'sm' \| 'md'` | ✅ |
| `fullWidth` | 스타일 | `boolean`(가로 꽉 채움) | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `defaultValue` | — | controlled(uncontrolled 입구 제거) | ❌ |
| 외형(캡슐) | — | 고정 | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### TabBar

> 의미 = **다른 구획으로 전환하는 트리거**(대상 자체가 바뀜). 가로 라벨, 하나 활성. SegmentedControl과 메커니즘(가로 단일선택)은 닮았으나 최소 의미가 달라(뷰 토글 vs 구획 전환) 별개 원자 — 입력칸 3종을 생김새 아닌 데이터로 가른 것과 동일 논리. 패널 전환 로직은 TabBar 밖(Tabs 유기체의 일); TabBar는 "어느 키가 활성인가"만 안다.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `options` | 기능(데이터) | `{ label, value, count?, countTone?: danger\|neutral }[]` — `count`=탭별 미처리 건수 → CountBadge(라벨 우측, 행동요구 빨강 기본)(v0.14.0) | ✅ |
| `value` `onChange` | 기능 | controlled, `value: string`(활성 구획 키) | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `defaultValue` | — | controlled | ❌ |
| `size` | — | 고정(탭 높이 변형 거의 없음 — 용례 시 연다) | ❌ |
| 외형(언더라인) | — | 고정 | ❌ |
| 패널 전환 | — | TabBar 밖(Tabs 유기체) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

> `options`가 Radio·Select·MultiSelect·SegmentedControl·TabBar 전체에서 `{label,value}[]` 하나로 통일(선택지 데이터 형태 단일 — 경쟁 경로 없음).

### 레이아웃 원자

#### Card

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `variant` | 스타일 | `'elevated' \| 'outlined' \| 'flat'` | ✅ |
| `padding` | 값(토큰) | `'none' \| 'sm' \| 'md' \| 'lg'`(기본 md) | ✅ |
| `children` | 콘텐츠 | 자식 받음 | ✅ |
| `className` `style` | — | 규칙 3 | ❌ |

> `padding='none'`은 자식이 자체 패딩을 갖는 경우용(예: DataTable을 담는 elevated 카드 — 표가 셀 패딩을 직접 소유하므로 카드는 그릇·그림자만 제공). 카드는 여전히 토큰만 쓰며 임의 px 없음("none"은 토큰이 아니라 "여백 없음"의 닫힌 선택지).

#### Divider

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `orientation` | 방식 | `'horizontal' \| 'vertical'` | ✅ |
| 색·굵기 | — | 고정(보더 토큰 1px, neutral 참조) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Container

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `maxWidth` | 값(토큰) | `'narrow' \| 'default' \| 'wide'` | ✅ |
| `children` | 콘텐츠 | 자식 받음 | ✅ |
| 천장+가운데정렬 | — | 자동 동작(prop 아님) | — |
| `className` `style` | — | 규칙 3 | ❌ |

---

### 배치 프리미티브

> prop의 분류(값 / 방식 / 값+방식)와 기본값의 근거는 「01. 규칙과 구조」 배치 프리미티브 절. 여기서는 노출 형태만 확정한다.

#### Stack (세로 배열)

| prop | 분류 | 값 | 노출 |
|---|---|---|---|
| `gap` | 값 | 간격 토큰(기본 md) | ✅ |
| `align` | 방식 | `'start' \| 'center' \| 'end' \| 'stretch'`(기본 stretch) | ✅ |
| `justify` | 방식 | `'start' \| 'center' \| 'end' \| 'between'`(기본 start) | ✅ |
| `children` | 콘텐츠 | 자식 받음 | ✅ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Group (가로 배열)

| prop | 분류 | 값 | 노출 |
|---|---|---|---|
| `gap` | 값 | 간격 토큰(기본 md) | ✅ |
| `align` | 방식 | `'start' \| 'center' \| 'end'`(기본 center) | ✅ |
| `justify` | 방식 | `'start' \| 'center' \| 'end' \| 'between'`(기본 start) | ✅ |
| `wrap` | 방식 | `boolean`(기본 false) | ✅ |
| `children` | 콘텐츠 | 자식 받음 | ✅ |
| `className` `style` | — | 규칙 3 | ❌ |

#### Grid (격자) — 본체 + Grid.Col 하위 부품

| prop | 분류 | 값 | 위치 | 노출 |
|---|---|---|---|---|
| `columns` | 값 | `1 \| 2 \| 3 \| 4 \| 6 \| 12`(12의 약수) | Grid 본체 | ✅ |
| `gap` | 값 | 간격 토큰(기본 md) | Grid 본체 | ✅ |
| `span` | 값+방식 | `1~12` | `Grid.Col` 자식 | ✅ |
| `children` | 콘텐츠 | 자식 받음 | 양쪽 | ✅ |
| `className` `style` | — | 규칙 3 | 양쪽 | ❌ |

> `span`은 자식별로 다를 수 있어(비대칭 분할) `Grid.Col` 하위 부품에 단다. 균등 분할이면 `columns`만으로 충분, 비대칭일 때만 `Grid.Col span` 사용. `Grid.Col`은 도메인을 모르는 순수 배치 하위 부품으로 `span`(닫힌 값)만 받으므로 헌법 1번과 무관하다.

---

## 분자 prop 규격 (✅ 분자 **36** — 아래 16종 prop 표 + 나머지 20종은 `_catalog`·README 5절이 단일 출처)

> **카운트 실측(`_catalog` 기준, v0.59.0):** 데스크탑 26 = 위 16 + Accordion·Stat·TreeSelect·Cascader·MillerColumns·SearchToolbar·PeriodNavigator·NumberStepper·Editor·RichText / 모바일 10 = MobileSection·MobileListRow·MobileTop·MobileStatRow·MobileDisclosure·MobilePhotoPicker·MobileCalendar·MobileComment·MobileComposer·MobileFileRow. **모바일 분자는 데스크탑과 시각 체계가 정반대**(면·그림자 대신 헤어라인)라 섞어 쓰지 않는다 — 경계는 `Mobile*` 접두 그 자체(「03」 11-19).

> 분자는 원자·프리미티브를 결합하고 일부 상태를 고정해 "이름 붙은 단위"를 만든다(「01」 4-C). 도메인 무관. 16개 = FormField·MultiSelect·DateRangeField·InputGroup·FileUploader·Pagination·IconButton·Callout(v0.2.0 8) + StatusRow·SummaryCard·TotalRow(v0.3.0~) + Menu·ObjectCard·SectionHeader·Breadcrumb(v0.9.0) + Collapsible(v0.11.0).

### FormField (✅ 확정 — 구현·검증 완료)

입력 원자에서 벗긴 장식·에러 표현의 수령자. 입력 컨트롤을 `children`으로 받아(방식 A — FormField는 가운데가 뭔지 모른다) 위아래에 장식을 두른다.

조립 구조 (Stack, gap=토큰): `label`+별표 → `children`(컨트롤) → `error` 메시지(있을 때만). (`description`은 두지 않음 — 아래 참조.)

정렬·간격 default (전부 프리미티브+토큰, 손 px 0):

모든 자식(label·input·error)은 같은 왼쪽 기준선에서 시작(Stack align=stretch). 입력칸 placeholder가 안쪽으로 보이는 것은 입력칸 자신의 밀도(size) 패딩일 뿐 정렬이 아니다.
중첩 Stack으로 구간별 gap을 다르게 준다(균일 gap 아님): 바깥 Stack(gap=xs)에 [필드 묶음, error]를 담고, 안쪽 Stack(gap=xxs)에 [label, input]을 담는다. 라벨·입력칸은 한 덩어리로 붙고(xxs), 에러는 그 아래로 떨어진다(xs).
description prop은 두지 않는다 — 설명은 입력칸 placeholder로 경로를 통일한다(desc와 placeholder는 경쟁 경로). 지속 도움말이 필요한 필드는 별개 분자의 후보이며 rule of three 충족 시 만든다.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `label` | 콘텐츠 | `string` | ✅ |
| `withAsterisk` | 스타일 | `boolean`(별표 *표시*만 — 필수 *검증*은 스키마) | ✅ |
| `error` | 콘텐츠 | `string`(메시지. 있으면 에러 상태로 판정) | ✅ |
| `children` | — | 입력 컨트롤(원자) | ✅ |
| `className` `style` | — | 규칙 3 | ❌ |

고정(pin)하는 상태 — 분자가 붙박이로 만들어 통일성을 강제하는 것:
- label = 타이포 `body-strong` 단계 + `text.primary` 고정
- 별표 = `text.danger` 고정
- error 메시지 = `caption` + `text.danger` 고정
- 조각 간 gap = 토큰 고정

에러 분배 (핵심 메커니즘):
- 사용자는 `error` *메시지 하나만* 준다. FormField가 "메시지 있음 = 에러 상태"로 **1회 판정**한다.
- 메시지는 FormField가 직접 그린다(맨 아래, danger).
- 빨간 테두리는 입력칸이 그리되, **입력칸은 자기가 에러인지 모른다.** 입력칸 테두리는 `--field-border`라는 CSS 역할 변수로 그려지고(없으면 neutral 토큰 fallback), FormField가 에러 상태일 때 자식 영역에 `--field-border: [danger 토큰]`을 깐다.
- prop 구멍이 아니라 CSS 역할 변수다 — 바깥에서 임의 색을 못 넣고(FormField가 neutral/danger 토큰만 넣음), 입력칸 Props엔 테두리 관련 prop이 없다(닫힘 유지). "컴포넌트는 역할만, 값은 문맥·토큰이"의 색 토큰과 동일 구조.

### MultiSelect (✅ 확정 — 구현·검증 완료)

Select + Chip 결합. value 배열을 받아 내부에서 Select+Chip을 조립(내부 조립형 — children 아님). 고른 값이 Chip으로 쌓인다.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `options` | 기능(데이터) | `{ label: string; value: string }[]` | ✅ |
| `value` | 기능 | `string[]`(배열 — Select는 단일 string) | ✅ |
| `onChange` | 기능 | `(string[]) => void` | ✅ |
| `placeholder` | 콘텐츠 | `string`(빈 상태일 때만) | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `size` | 스타일 | `'sm' \| 'md'` | ✅ |
| `name` | 기능 | 폼 배선 | ✅ |
| `label` `withAsterisk` `error` | — | FormField 소유 | ❌ |
| `searchable` | — | Select와 일관되게 고정(끔). Combobox 결정과 함께 보류 | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정하는 상태:
- 고른 값을 **Chip(color=neutral 고정)**으로 표시. Chip 원자는 가변이나 MultiSelect가 neutral로 고정해 가린다(통일).
- Chip의 `onRemove` 항상 켬(다중 선택에선 개별 제거가 본질).
- **항목 선택 시 드롭다운 유지(닫지 않음)** — 단일 Select는 고르면 닫히지만, 다중은 연속 선택이 본질이라 분자가 "열린 채 유지"를 동작으로 고정한다. (분자가 색·모양뿐 아니라 *동작*도 고정하는 사례.)
- 드롭다운 체크 표시는 Icon 원자(SVG + baseline 보정), Group(align=center).

### DateRangeField (✅ 확정 — 구현·검증 완료)

DatePicker 둘을 Group(align=center)으로 잇고 가운데 `~`. 내부 조립형. "시작 ≤ 끝" 제약이 둘을 묶는다.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `value` | 기능 | `{ start: Date\|null; end: Date\|null }` | ✅ |
| `onChange` | 기능 | `(value) => void` | ✅ |
| `startPlaceholder` `endPlaceholder` | 콘텐츠 | `string` | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `size` | 스타일 | `'sm' \| 'md'` | ✅ |
| `name` | 기능 | 폼 배선 | ✅ |
| `label` `withAsterisk` `error` | — | FormField 소유 | ❌ |
| min/max date | — | 스키마로(선택 가능 범위는 검증) | ❌ |
| 가운데 구분(`~`)·배치 | — | 고정 | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정하는 상태 / 검증:
- 검증의 진실은 **스키마**("끝 ≥ 시작", required 여부). 분자는 자체 판정하지 않는다.
- UI 보조 차단: 시작 선택 시 끝 달력에서 시작 이전 날짜 비활성 — 단 이 범위 제한은 스키마가 아는 제약을 받아 반영(분자가 멋대로 정하지 않음).
- "시작만 있고 끝 비었음"은 유효한 중간상태(에러 아님). null 허용 value가 표현.
- value는 `{ start, end }` 객체로 고정(배열은 순서 혼동 → 객체로 의미 명확).

### InputGroup (✅ 확정 — 구현·검증 완료)

입력칸 양 끝에 비대화형 어드온(단위·아이콘)을 두르는 그릇. 방식 A(children으로 입력칸 받음 — 가운데가 뭔지 모름). 입력군에서 닫았던 어댄먼트(raw 구멍)를 분자에선 *좁힌 형태*로 연다.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `leftAddon` | 콘텐츠 | `string \| <Icon>` (둘 중 하나, raw 노드 금지) | ✅ |
| `rightAddon` | 콘텐츠 | `string \| <Icon>` | ✅ |
| `children` | — | 입력 원자(TextInput/NumberInput 등) | ✅ |
| `label` `withAsterisk` `error` | — | FormField 소유 | ❌ |
| 버튼 어드온 | — | 제외(입력+버튼은 별개 분자 후보, rule of three) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정: 어드온은 텍스트(₩·원·kg) 또는 Icon 원자 — 비대화형만. 양끝 동시 허용, 한 슬롯엔 하나. 어드온↔입력칸 테두리 합치기·정확한 gap은 분자 모아보기에서 시각 확정(보류); 구조(Group align=center 결합)만 확정.

### FileUploader (✅ 확정 — 구현·검증 완료)

파일 업로드 상태를 받아 그리고 선택을 트리거. **파일 업로드는 4단계 상태(대기→진행→완료/실패)가 본질**이라 "상태 없는 단순 첨부"를 별도 부품으로 두지 않는다(단일 경로 — kk가 FileButton만 쓴 것은 바로잡을 대상). 실제 업로드는 분자 밖(렌더러가 상태 갱신) — controlled.

`FileItem` = `{ id, name, status: 'pending'|'uploading'|'done'|'error', progress?, error? }`

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `value` | 기능 | `FileItem[]` | ✅ |
| `onChange` | 기능 | `(next: FileItem[]) => void` (다음 목록 통째로 신호 — controlled) | ✅ |
| `multiple` | 스타일 | `boolean` | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `name` | 기능 | 폼 배선 | ✅ |
| `label` `withAsterisk` `error` | — | FormField 소유 | ❌ |
| accept·maxSize | — | 스키마로(검증의 진실) | ❌ |
| 실제 업로드 함수 | — | 분자 밖 | ❌ |
| 상태별 모양·드래그앤드롭 | — | 고정(status enum, 드롭 항상 켬) | ❌ |
| 미리보기 | — | 보류(타입 의존=도메인) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정/검증: status 4 enum(pending=점선/uploading=진행바/done=체크/error=빨강+재시도). 검증(용량·확장자)은 스키마, 위반 시 status='error'. 단일/다중은 multiple로 흡수. **브라우저 `File` → `FileItem`(id·status:'pending') 부여는 분자 책임**(FileItem이 분자의 표현 단위라서). 실제 업로드(진행률·완료/실패 판정)만 분자 밖 — `onChange`는 선택/제거/재시도 모두 "다음 목록"을 통째로 신호(경쟁 경로 없음, 횡단규칙 2와 동형).

### Pagination (✅ 확정 — 구현·검증 완료)

목록을 페이지로 나눠 이동. 테이블·카드그리드·목록 공유 → 독립 분자. **번호형 단일**(이전다음·더보기는 경쟁 경로/다른 의미라 미포함).

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `total` | 기능(데이터) | `number`(전체 페이지 수) | ✅ |
| `value` `onChange` | 기능 | controlled, `number`(현재 페이지) | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| 표현 방식 | — | 번호형 고정(경쟁 경로 차단) | ❌ |
| 축약(양끝+현재 주변 …) | — | 고정 | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정: 화살표 = Icon 원자(SVG+baseline). 현재 페이지 강조·… 축약 고정. "더보기형"은 페이징 아님(목록 누적, 다른 의미).

### IconButton (✅ 확정 — 구현·검증 완료)

Button을 **아이콘 전용으로 고정**한 named 분자(「01」 4-C가 예고한 "아이콘형"의 실현 — 같은 Button 원자에 자식을 텍스트 대신 아이콘으로). 텍스트가 없으므로 의미를 `label`(aria-label)로 보존한다. 정사각. 행 액션의 "윤곽 없는 빨강 삭제", 프로필 더보기(dots) 등에 쓴다.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `icon` | 콘텐츠 | `IconName`(Icon 세트 식별자) | ✅ |
| `label` | 콘텐츠 | `string`(aria-label 필수 — 아이콘만이라 의미를 텍스트로 보존) | ✅ |
| `variant` | 스타일 | `'primary' \| 'secondary' \| 'danger' \| 'ghost'` | ✅ |
| `size` | 스타일 | `'sm' \| 'md'` | ✅ |
| `disabled` | 스타일 | `boolean` | ✅ |
| `onClick` | 기능 | 콜백 | ✅ |
| 외형(정사각·radius·색 매핑) | — | 고정(variant→외형·Icon 색 역할. danger=윤곽 없는 빨강 subtle) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정: variant가 (외형, Icon 색 역할)을 함께 고정 — primary=filled, secondary=default, danger=subtle+danger색, ghost=subtle+secondary색. `renderAction`이 `iconOnly` 액션을 이 분자로 라우팅한다.

### Callout (✅ 확정 — 구현·검증 완료)

인라인 문맥 메시지 — 그 자리에 머무는 **비휘발** 안내(중복 연락처 경고·레거시 주소 힌트 등). 토스트(휘발·우상단)와 의미가 다르다: Callout은 폼/구획 안에 박혀 사라지지 않는다. Group + Stack + Icon + Text 조립.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `tone` | 스타일 | `'info' \| 'warning' \| 'danger' \| 'neutral'`(상태색 역할, primary 제외 — Badge와 동형. success는 토스트 몫) | ✅ |
| `title` | 콘텐츠 | `string`(선택, 강조 첫 줄) | ✅ |
| `children` | 콘텐츠 | `string`(본문 — raw 노드 금지) | ✅ |
| `icon` | 콘텐츠 | `IconName`(선택; 생략 시 tone별 기본 아이콘 내부 고정) | ✅ |
| `className` `style` | — | 규칙 3 | ❌ |

고정: 배경=`var(--mantine-color-{tone}-0)`·보더=`-2`·아이콘=`-7`(의미색 스케일 토큰, raw hex 0). 패딩 md·radius md. tone→기본아이콘 내부 매핑(info→info, warning→alert-triangle, danger→alert-circle, neutral→info). **텍스트는 primary/secondary 역할 통로 유지**(연한 tone 배경 위 틴트 텍스트는 대비 저하 — tone은 배경·보더·아이콘이 전달). 아이콘 광학 보정은 flex라 `vertical-align` 대신 `transform`으로.

### StatusRow (✅ v0.3.0~ 확장 — 구현·검증 완료)

한 항목의 "현재 상태 + 지금 할 수 있는 행동"을 한 줄로: `[아이콘?] 라벨 [상태배지] ──spacer── [액션 0~2]`. Group(justify=between) + Badge + `renderAction` 결합.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `label` | 콘텐츠 | `string`(행 라벨 — Text body-strong) | ✅ |
| `icon` | 콘텐츠 | `IconName`(선택) | ✅ |
| `status` | 데이터 | `{ label; tone: BadgeColor }`(상태 배지) | ✅ |
| `actions` | 기능(데이터) | `Action[]`(0~2 권장, 우측 sm) | ✅ |
| 배치·배지 위치·행 높이 | — | 고정(Group justify=between, 배지 좌측 고정, sm 버튼 높이 reserve) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정: 배지는 라벨 옆(좌측) 고정 — 액션 0/1/2개가 변해도 위치 안 밀림. 버튼 개수는 토글 prop이 아니라 `actions` 배열 길이로(옵션 쌓기 금지 — 「03」 11-3). 행 높이는 액션 유무와 무관하게 sm 버튼 높이만큼 통일.

### SummaryCard (✅ v0.3.0~ 확장 — 구현·검증 완료)

KPI/요약 타일 — `[틴트 아이콘] 라벨 큰수치(건수) 보조수치(금액)`. 대시보드 통계(승인대기 N건·₩). Card + 틴트 Icon + Title(count) + Text 결합.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `label` | 콘텐츠 | `string` | ✅ |
| `icon` | 콘텐츠 | `IconName`(선택) | ✅ |
| `tone` | 스타일 | `BadgeColor`(아이콘 틴트 — Callout 동형 의미색 스케일) | ✅ |
| `count` | 데이터 | `number`(건수 — 선택) | ✅ |
| `amount` | 데이터 | `number`(금액 — 선택) | ✅ |
| 배치·틴트·포맷 | — | 고정(틴트 `var(--mantine-color-{tone}-{n})`, `fmtCurrency`/`fmtNumber`) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정: count·amount는 값 유무로 결정(건수만/금액만/둘 다 — 토글 아님). 아이콘 틴트 raw hex 0(의미색 스케일 토큰). 통화·숫자 포맷은 `_cells`의 단일 진실(`fmtCurrency`/`fmtNumber`) 재사용.

### TotalRow (✅ v0.3.0~ 확장 — 구현·검증 완료)

리스트 하단 합계 행 — `위 Divider(마감선) + [라벨] ──── [금액 강조]`. 시공비·사용내역·청구·정산 반복으로 추출(페이지에서 Divider+Group 매번 조립하던 것을 분자가 흡수 → 합계 패턴 비통일 차단).

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `label` | 콘텐츠 | `string`(기본 '합계') | ✅ |
| `amount` | 데이터 | `number`(₩ + 천 단위 콤마) | ✅ |
| 마감선·정렬·강조 | — | 고정(상단 Divider, Group justify=between, 금액 body-strong) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정: 통화 포맷 `fmtCurrency` 재사용(SummaryCard·셀과 단일 출처).

### Menu (✅ v0.9.0 — 구현·검증 완료)

클릭 시 열리는 액션 메뉴 — Popover + `Action[]` + 선택 header. 프로필 더보기·행 ⋯메뉴. trigger를 받고(보통 IconButton), 항목 클릭 시 닫고 onClick.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `trigger` | 콘텐츠 | `ReactNode`(여는 대상 — 보통 IconButton) | ✅ |
| `items` | 기능(데이터) | `Action[]`(클릭 시 닫고 onClick, variant=danger 지원) | ✅ |
| `header` | 콘텐츠 | `ReactNode`(선택 — 신원/제목 슬롯, 구분선 자동) | ✅ |
| `width` | 값(토큰) | `'sm' \| 'md' \| 'lg'` | ✅ |
| `position` | 스타일 | `'top' \| 'bottom' \| 'left' \| 'right'` | ✅ |
| 호버 배경·구분선·닫기 동작 | — | 고정(controls.css hover, Divider, 항목 클릭=닫기) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정: Popover(슬롯)를 "닫힌 `Action[]` + 신원 header"로 좁힌 특수형(content raw 슬롯 대신 Action 배열 — content보다 더 닫힘). header 슬롯은 신원 표시(프로필 카드)용.

### ObjectCard (✅ v0.9.0 — 구현·검증 완료)

계층 안 "오브젝트 한 칸"의 *완결된* 닫힌 표현(미리보기 아님 — 그 자리에서 완결). **단일 사진 카드(역할 슬롯)**. 자유 render 구멍 대신 닫힌 스키마(title·subtitle?·status?·thumbnail?/icon?·headline?·attributes?·actions?·onClick?) — 값 표현은 DataTable·DescriptionList 셀 어휘(CellType)와 동형. 도메인 무지. **(HierarchyExplorer는 0.17.0부터 목록 전용 — ObjectCard는 카드형 표현이 필요한 곳에서 직접 사용.)**

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `title` | 콘텐츠 | `string`(필수 — 이름) | ✅ |
| `subtitle` | 콘텐츠 | `string`(선택 — 식별자/코드) | ✅ |
| `status` | 데이터 | `{ label; tone: BadgeColor }`(선택 — 상태 배지 1개, media 좌상단 overlay) | ✅ |
| `thumbnail` `icon` | 콘텐츠 | `string`(이미지 src) / `IconName`(폴백 글리프, 기본 `package`) — media는 **항상** 채움 | ✅ |
| `headline` | 데이터 | `{ label; value; type: CellType; note? }`(선택 — 핵심값 **단 하나**, 도메인이 주입) | ✅ |
| `attributes` | 데이터 | `{ label; value; type: CellType; note? }[]`(선택 — 보조 라벨—값) | ✅ |
| `actions` | 기능(데이터) | `Action[]`(media 우상단 아이콘 overlay, 전파 차단) | ✅ |
| `onClick` | 기능 | `() => void`(카드 클릭 — actions는 stopPropagation) | ✅ |
| 배치·값 렌더·note 위치·높이 | — | 고정(Card outlined·padding sm, **height:100%로 셀 채움**, media flex + footer 고정, 값=`renderCell`, note=값 우측 Badge) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정: 값은 `renderCell(type, value)`로 그려 통화·날짜가 표·설명목록과 동일 포맷(셀 어휘 재사용). **평면 `fields[]` 자루 폐기 → 역할 슬롯**(status·headline·attributes)로 "무엇을 어디에" 매핑을 강제(스키마가 사고를 시킨다 — 최소정보 무성의 투입 차단). **도메인 규칙(무엇이 status·headline인가)은 카드가 모른다** — 위(페이지)가 `actions`로 의도를, `headline`/`attributes[].note`로 "이 값이 비정상 상태(예: 가격 변경요청중)"를 국소로 먹인다(헌법 1). 카드는 `'단가'` 같은 도메인 어휘를 모르고, `headline` 라벨/값을 도메인이 주입한다.

> **단일 사진 카드(v0.12.0~ — `layout` detail/normal 2뷰 폐기):** media(썸네일 필수 자리, 없으면 폴백 아이콘이 같은 자리를 채워 footprint 균일) + 이름 + 식별자 + `headline` + `attributes`. status·actions는 media 위 overlay(좌상단/우상단)라 footer를 이름·값에 양보. 높이는 바깥 그리드 셀이 분배(타일, `height:100%`). 밀도 비교는 카드 축소가 아니라 별도 목록(DataTable)이 맡는다.

### SectionHeader (✅ v0.9.0 — 구현·검증 완료)

카드/구획의 표준 헤더 — 제목(subheading = 본문 +1 tier) + 설명? + 액션?. "헤더 ≥ content+1tier" 규칙의 부품화. **페이지 최상위는 PageHeader(heading), 카드 안 구획은 SectionHeader(subheading)로 티어 분리.**

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `title` | 콘텐츠 | `string`(subheading) | ✅ |
| `titleNode` | 콘텐츠 | `ReactNode`(title 대신 좌측 헤더 슬롯 — 예: 인터랙티브 Breadcrumb. 둘 중 하나) | ✅ |
| `description` | 콘텐츠 | `string`(선택, caption) | ✅ |
| `controls` | 콘텐츠 | `ReactNode`(도구 — 검색·뷰 토글 등. **제목존 우측에 고정**, 3존)(v0.11.0) | ✅ |
| `actions` | 기능(데이터) | `Action[]`(헤더 **우측 끝** = 액션 존, sm) | ✅ |
| `divider` | 스타일 | `boolean`(하단 구분선 = 헤더 밴드) | ✅ |
| 티어·정렬·안정 밴드 | — | 고정(Title subheading, **3존 flex**, **min-height 38 예약**으로 흔들림 0) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정: `titleNode`는 정적 제목 대신 인터랙티브 헤더(Breadcrumb 등)를 주입하는 슬롯 — title과 경쟁 아님(둘 중 하나). **3존 레이아웃(v0.14.0): `[제목존: flex:1 말줄임] [도구(`controls`) 고정] [액션(`actions`) 우측 끝 고정]`** — 제목이 길이 변동을 전부 흡수(말줄임)해 도구·액션은 **제목 길이·조건부 토글에 안 밀린다**(「03」 헤더 안정성 원칙). **헤더 행 안정 밴드**(min-height **38** — sm 검색 InputGroup 실측: 36 입력+1px 보더×2)로 액션 유무·내용에 헤더가 위아래로 안 흔들리고, 같은 카드 좌/우 패널 헤더의 **divider까지 정렬**(CDP 실측 0px).

### Breadcrumb (✅ v0.9.0 — 구현·검증 완료)

위치 경로(a › b › c). 앞 단계는 이동 가능(onClick), 마지막=현재(강조·비링크). 항목 사이 chevron-right. 인터랙티브라 그 자체가 헤더 역할(SectionHeader titleNode로 주입).

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `items` | 기능(데이터) | `{ label: string; onClick?: () => void }[]`(마지막=현재, 비링크) | ✅ |
| 구분·강조·트렁케이션 | — | 고정(chevron-right, 마지막 body-strong/primary, 앞 단계 secondary, **한 줄 nowrap + 마지막 크럼 우선**) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정: 마지막 항목은 onClick 무시(현재 위치 = 비링크). 앞 단계만 클릭 가능(상위로 이동). HierarchyExplorer 우측 헤더가 디렉토리 경로에 사용. **한 줄 고정(nowrap — 두 줄/글자쪼갬은 헤더 밴드를 키워 divider 유격을 내므로 차단)**. **폭 부족 시 마지막(현재) 크럼을 고정하고 앞 조상부터 클립**(v0.14.0) — 헤더가 좁아져도 "내가 지금 어디"는 항상 보인다(앞을 가려 "부자재 ›"만 남는 잘못 방지).

### Collapsible (✅ v0.11.0 — 구현·검증 완료)

헤더(항상 보임) + 본문(접힘/펼침) 한 단위. 헤더 클릭으로 토글. 여러 개를 세로로 쌓아 "요약 목록 → 각자 펼쳐 상세"를 구성. Mantine `Collapse`(애니메이션 엔진)를 격리 래핑(MultiSelect가 Combobox 래핑한 것과 동형, 헌법 7).

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `header` | 콘텐츠 | `ReactNode`(요약 슬롯, 항상 보임 — Modal children 동형. 보통 StatusRow/TotalRow를 꽂음) | ✅ |
| `children` | 콘텐츠 | `ReactNode`(상세 슬롯, 접힘 대상) | ✅ |
| `defaultOpen` | 스타일 | `boolean`(처음 상태, 기본 false=접힘) | ✅ |
| 토글·셰브론(▼/▲)·애니메이션 | — | 내부 고정(Card outlined + Divider + 헤더 hover) | ❌ |
| `opened`/`onChange`(controlled) | — | 미노출 — 펼침은 UI 표현 상태라 uncontrolled(내부 상태). accordion 필요 시 rule of three로 추후 | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

> **상태 = uncontrolled(`defaultOpen`).** 펼침/접힘은 도메인 데이터가 아니라 UI 표현 상태 → 내부 상태(뷰 토글·Tree 인라인편집 선례). 입력군 "controlled 전용"은 *데이터* 규칙이라 무관.
> **경계(중요): 헤더+본문 한 단위까지만.** "여러 개 중 하나만 열림(accordion)"은 이 부품 일이 아니다 — 여러 개를 쌓아 구성(§11-3 "옵션 쌓지 말고"). header 슬롯 안 인터랙티브 요소(StatusRow 액션 등)는 호출측이 전파 차단(ObjectCard actions 동형). 실수요: 정산 발주별 요약행→품목+소계, 발주 완료섹션 헤더→완료 카드들(기본 접힘).

## 유기체 prop 규격 (✅ 유기체 **28** — 아래 9종 prop 표 + 나머지 19종은 `_catalog`·README 5절이 단일 출처)

> **카운트 실측(`_catalog` 기준, v0.59.0):** 위 9(Modal·DataTable·EmptyState·PageHeader·DescriptionList·AppShell·Timeline·Calendar·Tree) + Drawer·PaperModal·Stepper·Transfer·ToastHost·LineItemList·FieldGrid + **위젯 2**(ListWidget — TanStack Table 흡수 / NotificationPanel) + **저작 툴킷 5**(Repeater·InheritedValueField·ExpressionField·KeyValueField·AssignPicker) + **OptionSet 계열 4**(OptionSetEditor·OptionSetComposer·OptionSetPicker·CompositionOutline) + **MobileShell**. 뒤 세 묶음의 근거·설계는 「03」 11-23.

> 유기체 = 화면 한 구획 차지, 도메인 스키마를 입력받아 작동(「01」). 헌법 1번이 가장 빡세게 적용 — 도메인은 컴포넌트로 안 들어오고 스키마(데이터)로만. 측량(kk) 빈도가 우선순위를 안내했을 뿐, 정의는 도메인 무관. 측량의 도메인 박힌 컴포넌트(OrderView 등)는 부품 아닌 *스키마*(DataTable+발주 스키마).

### 공통 패턴 — 셀/값 표현 타입 enum (현재 **16종**)
DataTable·ListWidget·DescriptionList·ObjectCard가 "값을 어떤 표현으로 그리나"를 공유(`_cells.tsx`의 `CellType`). 자유 render 함수 금지(raw 구멍). 새 타입은 사람이 큐레이션으로만 추가(헌법 4번). v0.2.0 7종에서 ERP 실수요로 9종 더 추가 → 현재 16종:
- 기본 7: `text` / `badge`(값→색 매핑 스키마 주입) / `number`(천 단위 콤마) / `currency`(₩ + 콤마, KR locale 잠정) / `date` / `boolean`(true→Icon `check`(primary), false→엷은 대시 `—`) / `actions`(`Action[]`, 셀에선 sm).
- 추가 9: `user`(`{name, src?}` → Avatar sm + 이름) / `tags`(`string[]` → neutral Badge 묶음) / `link`(`{label, href}` → Anchor) / `percent`(`number` → `N%`) / `secondary`(`{primary, secondary?}` → 본문+보조 2줄) / `relative-time`(ISO → "3일 전" 상대시간, dayjs ko) / `thumbnail`(src → Image sm) / `chevron`(`boolean?` → 디스클로저 `›` — "행=상세로 인도" 시각 장치, 자체 동작 없음·클릭은 행 onRowClick으로 버블) / `menu`(`Action[]` → 행 ⋯ 케밥 — 액션이 3개 이상이라 나란히 못 펴는 행용. `actions`(나란히)와 같은 데이터·다른 배치).
- 정렬: number/currency/percent/actions는 우측정렬, 나머지 좌측/기본. 통화·숫자 포맷은 `fmtCurrency`/`fmtNumber` 단일 출처(SummaryCard·TotalRow도 재사용).

### 공통 패턴 — actions 배열 + `renderAction` 헬퍼
Modal·PageHeader·EmptyState·DataTable·DetailPage가 버튼을 `Action` 형태로 받고 *배치는 컴포넌트가 고정*한다.

`Action` = `{ label: string; variant?: 'primary'|'secondary'|'danger'|'ghost'; onClick: () => void; icon?: IconName; iconOnly?: boolean }`
- `icon` — 좌측 아이콘(행위 중심 액션). 중립 액션(취소·이동)은 생략 → 텍스트만.
- `iconOnly` — true면 아이콘 전용 버튼(IconButton)으로 렌더. `icon` 필수, `label`은 aria-label로 보존(접근성). 행 액션의 "윤곽 없는 삭제" 등.

**`renderAction(action, key, size='md')`** — 모든 호출처 공유 단일 헬퍼(`_cells.tsx`). 분기: `iconOnly+icon` → IconButton / `icon+label` → Button(leftIcon) / 그 외 → 텍스트 Button. **PageHeader·DataTable 셀은 `size='sm'`로 호출**(헤더는 제목이 주인공·표는 밀도). 닫힌 enum만 받으므로 자유 노드 구멍 없음. (ActionGroup 분자 후보 — rule of three 충족이나 배치 맥락이 미묘하게 달라 구현 시 추출 판단. 「03」)

### Modal (✅ 확정)

도메인 무관 껍데기. 본문(children)에 도메인 폼이 오되 Modal은 모름(방식 A).

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `opened` | 기능 | `boolean`(controlled) | ✅ |
| `onClose` | 기능 | 닫기 콜백(X·오버레이·ESC·취소 모두 수렴) | ✅ |
| `title` | 콘텐츠 | `string` | ✅ |
| `actions` | 기능(데이터) | `{ label, variant, onClick }[]` | ✅ |
| `size` | 스타일 | `'sm' \| 'md' \| 'lg' \| 'xl' \| 'full'`(폭 — sm/md/lg/xl=Mantine 토큰, **full=95vw·90vh**, 풀스크린 아님·radius/여백 유지) | ✅ |
| `closeOnOverlayClick` | 스타일 | `boolean`(폼 모달서 끄게) | ✅ |
| `children` | — | 본문(도메인, Modal은 모름) | ✅ |
| 스크롤 구조(헤더·푸터 고정·본문 스크롤) | — | 고정. 3영역(헤더/본문/푸터) flex 세로. 헤더·푸터 `flex:none`(고정), 본문 `flex:1 + overflowY:auto`(긴 내용일 때만 스크롤), 컨테이너 `maxHeight:85vh`(full=90vh)로 캡. Mantine 본문 패딩(`padding=0`) 끄고 패딩 주인을 우리 3영역으로 이관 | ❌ |
| 푸터 배치 | — | 고정(오른쪽 정렬, primary/danger 맨 오른쪽, gap=xs, 배열 순서 무관) | ❌ |
| 헤더(제목+X) | — | 고정. Mantine 기본 헤더(`title` prop) 미사용 → 우리 `Group(justify=between, align=center)` + Title + Icon `x`로 직접 조립 | ❌ |
| 오버레이·중앙배치·그림자(md) | — | 고정. 그림자 토큰 `md`(02 "모달용"). 카드(sm)보다 강해 떠 보임 | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

> **헤더 정렬은 우리 프리미티브 보장.** 제목·X를 Mantine 내부 헤더(`title`)에 위임하면 정렬이 Mantine CSS 지배라 우리 보장이 끊긴다(미세 세로 어긋남 발생). `withCloseButton={false}`로 기본 헤더를 끄고 우리 Group으로 직접 조립한다. (서드파티 내부 레이아웃 불신 — 「01」 따름정리.)

> **스크롤은 직접 조립(명시 예외 2건).** ① 본문만 스크롤하는 3영역 flex는 Stack 프리미티브가 노출하지 않는 표현(`flex:none`/`flex:1`/`overflowY`/`maxHeight`)이라 src/ui 격리 구역 내 Modal 한 파일의 **raw flex**가 된다 — 이는 "서드파티 내부 레이아웃 불신" 따름정리(헤더 직접 조립)의 *재적용*이며, 사례가 Modal 하나뿐이라 Stack 확장(프리미티브 손대기)은 rule of three 미달로 보류. ② `maxHeight: 85vh`는 공간 장부에 처음 들어오는 **잠정 단위(vh)** — 뷰포트 캡은 vh가 자연 단위라 수용, 값은 화면 검증에서 조정. xl size는 보류(요청).

푸터: actions를 variant 보고 배치 고정(primary/danger 끝, secondary 왼쪽). 주 행동 모달당 하나 전제. 닫기 X는 Icon 원자. 유형 자동(폼/확인/정보)은 패턴 굳으면 별도 분자(ConfirmModal).

### DataTable (✅ 확정)

도메인 무관 표 껍데기. columns(표현 enum)·rows(데이터) 주입, "고객" 모름. 정렬·페이징 상태만 들고 실제 수행은 바깥(controlled). Pagination·EmptyState 조합.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `columns` | 데이터 | `{ key, label, type, span?, sortable? }[]` | ✅ |
| `rows` | 데이터 | 행 데이터 배열 | ✅ |
| `status` | 스타일 | `'loading' \| 'empty' \| 'ready'` | ✅ |
| `sort` `onSortChange` | 기능 | controlled 정렬 상태(실제 정렬 밖) | ✅ |
| `page` `onPageChange` `totalPages` | 기능 | Pagination 연결(상태 밖) | ✅ |
| `onRowClick` | 기능 | 행 클릭 이동 콜백(없으면 비활성). actions 셀은 stopPropagation으로 행 클릭과 분리 | ✅ |
| `emptyState` | 콘텐츠 | empty일 때 EmptyState 내용(스키마) | ✅ |
| 행 줄·간격·정렬·헤더 | — | 고정(토큰·프리미티브) | ❌ |
| 셀 렌더 | — | type enum 결정(자유 render 금지) | ❌ |
| 컬럼 폭 | — | Grid span(닫힌 비율, px 금지) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

셀 타입: text/badge(값→색 매핑 스키마)/number(우측정렬)/date/actions(`Action[]` 스키마, 셀에선 sm 렌더). 폭: text 열은 스키마 span(없으면 균등), number/actions/badge는 타입이 기본 폭. 정렬: sort 상태·헤더 화살표(Icon)만, 줄세우기는 밖(서버 페이징과 맞음). status 3상태로 로딩↔빈↔데이터 명확(boolean 조합 모호함 차단). **세로여백: 데이터행 sm(12)·헤더 xs(8)** — 헤더가 데이터보다 낮아 위계 유지(둘 다 토큰). **하단 푸터: 좌 "총 N건"(`totalCount`, ko-KR 포맷·number 있을 때만) + 우 Pagination, 상단에 Divider 한 줄**(표↔페이지 경계 — 목록 하단 표준 구성. 하나만 있으면 그것만, 페이지 단독이면 중앙). 행 클릭(`onRowClick`)은 범용 이동 경로이며 actions 셀은 클릭 전파를 막아 경쟁하지 않는다.

### EmptyState (✅ 확정)

empty 전용(로딩·에러 별도 — rule of three 시 ErrorState). 아이콘+제목+설명+[action] 세로 중앙. 내용 스키마 주입, 배치 고정.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `icon` | 콘텐츠 | Icon 원자(또는 name) | ✅ |
| `title` | 콘텐츠 | `string` | ✅ |
| `description` | 콘텐츠 | `string` | ✅ |
| `action` | 기능(데이터) | `{ label, variant, onClick }`(optional — 없으면 버튼 미조립) | ✅ |
| 세로 배치·중앙정렬·gap·아이콘 크기 | — | 고정(Stack align=center, 토큰) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

action은 optional — **숨김이 아니라 미조립**(안 주면 처음부터 버튼 없음). 헤더(PageHeader)에 주 행동 있으면 EmptyState action 비움(소프트 관습 「03」 — 한 화면 primary 하나). 제목=Title, 설명=Text(secondary), 아이콘=Icon(SVG+baseline).

### PageHeader (✅ 확정)

페이지 상단 제목/설명(좌) + 액션(우). 도메인 무관 골격.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `title` | 콘텐츠 | `string` | ✅ |
| `description` | 콘텐츠 | `string`(선택) | ✅ |
| `actions` | 기능(데이터) | `{ label, variant, onClick }[]`(우측) | ✅ |
| 좌우 배치·정렬·간격 | — | 고정(Group justify=between, align=center) | ❌ |
| 제목 타이포 | — | 고정(Title heading) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

좌측 제목묶음(Stack: Title+Text secondary) / 우측 actions(Group). actions 배치는 PageHeader 고정. **액션 버튼은 `size='sm'` 고정** — 제목(heading)이 페이지 주인공이므로 버튼 세로를 낮춰 위계를 잡는다(헤더를 키우는 대신 버튼을 낮춤). **description 관습: 목록(ListPage)=제목만 / 상세(DetailPage)=제목+보조정보**(예: "유입경로 · 2026-05-02 등록"). 보조정보의 내용은 도메인이므로 스키마(데이터)가 정한다.

### DescriptionList (✅ 확정)

"라벨: 값" 쌍 나열, 읽기 전용, 도메인 무관. 발주서·견적서·고객상세 공유. 내용 스키마 주입.

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `items` | 데이터 | `{ label, value, type }[]` (type = 셀 타입 enum 공유) | ✅ |
| `columns` | 스타일 | `1 \| 2 \| 3`(한 행 쌍 수, Grid 기반) | ✅ |
| 배치·정렬·간격 | — | 고정(Grid + 토큰) | ❌ |
| 라벨·값 타이포 | — | 고정 — **라벨=caption + text.secondary, 값=body + text.primary**(크기로 구분) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

"정보 라벨" 역할이라 caption 단계 사용(폼 라벨의 body-strong과 다름 — 맥락이 단계를 고름). value 표현은 DataTable 셀 타입 enum 공유(경쟁 경로 차단). 배지 색 매핑 스키마 주입.

### AppShell (✅ 확정 — **2티어 반응형 + 하한**, v0.52.0. ※ v0.46.0의 3티어에서 폰 티어를 걷어냄)

페이지 전체 골격. 다른 유기체를 담는 그릇, 도메인 무관(메뉴·로고·프로필·알림 다 주입). **2티어 반응형** — 뷰포트 폭에 따라 내비 형태가 바뀐다(부품 계약, prop 아님):

| 티어 | 폭 | 내비 | 상단바 | 유틸리티(알림·프로필) |
|---|---|---|---|---|
| **데스크탑** | ≥1280(80em) | 좌측 풀 넷바 260(로고+메뉴) | **없음(해체)** | 넷바 **하단**(프로필 확장행 + 알림) |
| **태블릿** | 768–1279 | 좌측 아이콘 레일 72(세로 타일, **로고 없음**) | **없음(해체)** | 레일 **하단**(알림·아바타 아이콘) |
| ~~폰~~ | <768 | **범위 밖** — `MobileShell`(형제 층)이 받는다 | — | — |

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `logo` | 콘텐츠 | 로고 슬롯(주입 — img/svg는 박스 contain, 텍스트는 호출측 크기). **데스크탑 넷바 최상단 / 폰 상단바 좌측. 태블릿 레일엔 로고 없음** | ✅ |
| `onLogoClick` | 기능 | 홈 이동 | ✅ |
| `menuItems` | 데이터 | `{ label, icon, path, group, count? }[]` — `count`(미처리 건수)는 CountBadge로(v0.14.0, 데스크탑 NavLink·태블릿 레일 타일·폰 탭·더보기 공통) | ✅ |
| `activePath` | 기능 | 현재 활성 경로 | ✅ |
| `onNavigate` | 기능 | 메뉴 클릭 콜백 | ✅ |
| `profile` | 콘텐츠 | 프로필 정보(주입). `profile.menu?: Action[]` 주면 클릭이 Menu(완전 닫힘, renderAction 재사용), 없으면 `onMenuClick` 폴백. 형태: 데스크탑=아바타+이름·직책+caret 확장행 / 폰·레일=아바타-only | ✅ |
| `notification` | 데이터 | `{ hasUnread?, onClick?, content? }` — bell + 안 읽음 점(danger). `content?: ReactNode`(목록 슬롯) 주면 클릭이 Popover, 없으면 `onClick` 폴백 | ✅ |
| `children` | — | 콘텐츠 영역 | ✅ |
| 넷바/레일/상단바 골격·폭·높이·티어 분기·메뉴 정렬/활성 | — | 고정(브레이크포인트 768/1280 포함) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

**상단바 해체(v0.46.0 — 데스크탑·태블릿).** 헌법상 상단바 좌측은 비고(넷바가 전체 경로 제공)·정체성은 PageHeader 소유라 **상단바에 남은 책임이 유틸리티(알림·프로필)뿐** → 해체하고 그 둘을 **넷바 하단 유틸리티 존**(3존: 로고/메뉴grow/유틸)으로 이관. 세로공간(64px)을 본문에 반환. 유틸리티 존은 티어마다 *같은 데이터·다른 배치*(menuItems가 넷바↔탭 재배치되는 것과 동형). `header.collapsed=tier!=='phone'`로 폰만 상단바. 프로필 = `Menu`, 알림 = `Popover`. 데스크탑 하단 행은 `justify:space-between`(프로필 좌·알림 우).

**유틸리티 팝오버 규격(v0.48.0 — 데스크탑).** 좌하단 프로필 UI 관습(Linear·Slack·Vercel)대로 **둘 다 `position="top"`** — 넷바 하단 앵커에서 위로 솟는다(이전엔 알림만 `right` 플라이아웃이라 방향이 제각각). 폭·정렬은 내용 성격에 따라 다르다: **프로필 = 넷바폭 `md`(280) + `align="start"`(넷바 좌변 flush)**, **알림 = `lg`(360) + `align="center"`(벨 중심)**. 알림을 넷바폭으로 좁히지 않는 이유는 목록·본문이 들어가 옹색해지기 때문(`NotificationPanel` 구성이 lg 전제). 알림에 `align="end"`(우변 flush)를 못 쓰는 이유는 벨이 넷바 우측 끝이라 좌변이 화면 밖으로 계산돼 shift 보정에 의존하게 되어서다. **태블릿·폰은 불변**(레일 옆 `right`/상단바 아래 `bottom`). 이를 위해 `Menu`에 `align` prop 신설(기본 `center` — 기존 소비처 불변).

**태블릿 아이콘 레일(v0.46.0 — 72px).** 메뉴 = 세로 타일(아이콘+라벨, 하단탭 `tile()` 재사용), 활성 = 좌측 짧은 강조막대(primary). **로고 밴드 없음** — 72px엔 정사각 마크만 들어가 부실해서 제거(메뉴부터 시작). Popover/Menu는 `position="right"`로 옆에 열림.

**브레이크포인트.** 태블릿 48–79.99em(768–1279) · 데스크탑 ≥80em(1280). **티어 판정은 JS 하나**(`useMediaQuery` 1개)가 넷바 width(260↔72)·내용을 스왑한다. **1280 경계 근거**: iPad 가로(≤Pro11 1194)를 태블릿 레일에 담기(1440으로 올리면 1366px 노트북이 레일로 새므로 상한). em=브라우저 16px 기준(font-scale 무관). *시각 검증·튜닝은 `/dev/preview` 2티어 iframe 갤러리(dev 영구 자산).*

**폰 티어 삭제 + 하한 export (v0.52.0).** 폰 티어(슬림 상단바·하단탭·'더보기' 오버플로·상수 4개·모바일 스크롤 CSS)를 전부 제거했다 — 착지점(`MobileShell`)이 생겼기 때문이고, 셸만 반응형으로 만들어도 *안쪽 어휘*(PageHeader·DataTable·면)가 폰에서 성립하지 않아 층 전체를 갈랐다(「03」 11-19·11-20).
- **`APPSHELL_MIN_WIDTH = 768` export** — 셸 루트 `min-width`. 하한 아래에선 레이아웃이 깨지는 대신 **가로 스크롤로 예측 가능하게** 무너진다(안전망, 정상 경로에선 안 보임). **소비처는 이 상수를 import해 같은 값으로 모바일 라우팅을 판정한다** — 각자 숫자를 들면 언젠가 반드시 어긋난다.
- **768 근거**: CalendarPage 타임라인이 하한을 결정한다(그 아래는 2주 뷰 일 컬럼이 24~26px). 더 낮추려면 ① `NAME_COL` 반응형 ② DataTable 가로 스크롤이 선행(「03」 남은 것).

**치수(px).** 넷바 폭 데스크탑 260·태블릿 레일 72, `LOGO_BAND=88`/`LOGO_SLOT_HEIGHT=56`(데스크탑 로고 박스, img/svg는 contain 최대 적합·찌그러짐 0). **`logoMark` prop은 없음** — v0.9.0에서 제거됐고 v0.46.0에서 태블릿 레일용으로 잠시 재도입했다 로고 밴드 삭제로 렌더 0이 되어 다시 제거(미배포 → breaking 아님, 약속 안 지키는 prop 정리 재확인). **경계: AppShell은 셸 크롬만 — PageHeader는 페이지 템플릿이 소유, AppShell은 children만.** 셸 골격(M.Navbar 슬롯·티어 raw 분기)은 격리 raw(헌법 7 명시 예외 — Modal raw flex 동류), 슬롯 *콘텐츠*는 우리 Stack/Group/Icon/Text 조립(도그푸드).

### Timeline (✅ v0.3.0~ 확장 — 구현·검증 완료)

시간순 누적 이벤트를 `[시각·작성자·구분·내용]` 말풍선으로. 이벤트 shape를 닫힌 스키마로 받음(DataTable=columns, DescriptionList=items 동형 — 도메인 무지 헌법 1). 내부 고정: timestamp 오름차순 + 날짜 구분 + 말풍선 카드.

`TimelineEvent` = `{ id; timestamp; actor: { name }; category?: { label; tone: BadgeColor }; title?; body? }`

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `events` | 데이터 | `TimelineEvent[]` | ✅ |
| `emptyState` | 콘텐츠 | `{ icon?; title; description? }`(선택) | ✅ |
| 정렬·날짜 구분·말풍선 | — | 고정(timestamp 오름차순, 가운데 caption 날짜 구분, 카드 말풍선) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정(스펙↔부품 정합 2건): ① 날짜 구분은 Divider(label=…)이 아니라 가운데 Text(caption) — 우리 Divider는 orientation만 노출(닫힘). ② actor 색(tone)을 안 받음 — Avatar 원자가 색을 primary로 고정(닫힘)하므로 per-actor 색은 별도 결정(rule of three). DataTable(동종 다수행 표)과 결과물이 달라 경쟁 경로 아님.

### Calendar (✅ v0.3.0~ 확장 — 구현·검증 완료)

월 달력 — 셀당 이벤트 배지 + 셀 클릭. 이벤트 색·라벨은 데이터 주입(Timeline=events 동형, 도메인 무지). **월 뷰 단일**(뷰 토글·셀 렌더 슬롯 0 — 변형 필요 시 별개 부품, §11-3).

`CalendarEvent` = `{ id; date; label; tone: BadgeColor }`

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `month` `onMonthChange` | 기능 | controlled `'YYYY-MM'` | ✅ |
| `events` | 데이터 | `CalendarEvent[]` | ✅ |
| `onSelectDate` | 기능 | `(date, dayEvents) => void`(선택) | ✅ |
| `onSelectEvent` | 기능 | `(event) => void`(선택) | ✅ |
| 7열 격자·오늘 강조·배지 | — | 고정(명시 예외: raw CSS grid `calendar.css`) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정: **모달 비소유** — 셀 클릭은 `onSelectDate`로 신호만, 그날 무엇을 띄울지는 페이지가 결정(raw 슬롯 차단). **명시 예외:** 7열 그리드는 우리 Grid(12의 약수만)로 못 만들어 격리 구역 raw CSS grid(`calendar.css`) — Modal raw flex 따름정리의 재적용.

### Tree (✅ v0.9.0 — 구현·검증 완료)

재귀 계층 표시 + 펼침/접힘 + 선택 + 노드 ⋯메뉴 + 제자리 편집(범용). controlled — 선택·펼침·쓰기 전부 콜백 위임(순수 표현). 멘탈모델: VS Code Explorer.

`TreeNodeData` = `{ id; label; icon?; children? }`

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `nodes` | 데이터 | `TreeNodeData[]` | ✅ |
| `selectedId` `expandedIds` | 기능 | controlled(선택·펼침) | ✅ |
| `onSelect` `onToggle` | 기능 | 콜백(행 클릭 / 꺽쇠) | ✅ |
| `title` | 콘텐츠 | `string`(패널 헤더, 선택) | ✅ |
| `editable` | 스타일 | `boolean`(⋯·추가 노출 게이트) | ✅ |
| `onAddRoot` `onRename` `onDelete` | 기능 | 쓰기 위임(제자리 인라인 입력 → 콜백). 케밥=이름변경·삭제. 하위(분류) 추가는 트리 밖(소비처)에서 | ✅ |
| 들여쓰기 가이드·선택 강조·인라인 편집 | — | 고정(16px 가이드선, primary-0 강조, Enter/Esc/blur) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정: **`editable`이 수정·편집(⋯메뉴·추가·인라인 입력) 전체의 게이트** — 안 주면 분류 + 펼침/접힘 + 선택만(보기 전용 모드). 쓰기는 제자리 입력 후 콜백 위임(상태 주인은 부모). dev 박물관 좌측이 이 보기 전용 모드를 도그푸드.

## 피드백 시스템 — Toast (`notify`) (✅ 확정 — 구현·검증 완료)

휘발 피드백 — 우상단에 잠깐 떴다 사라지는 작업 결과(저장됨·삭제 실패 등). **부품이 아니라 명령형 트리거**라 페이지 스키마에 안 들어간다(LLM 스키마 표면 밖, resolver·onClick과 같은 *코드 배선* 자리). 그래서 "LLM은 스키마, 렌더러는 그림" 원칙을 안 깬다.

- API: `notify.success(p)` / `notify.danger(p)` / `notify.warning(p)` / `notify.info(p)`. `p`는 `string` 또는 `{ title?, message }`.
- 닫힘: tone = success/danger/warning/info(역할 enum) + 메시지 string. tone→(의미색 토큰·아이콘) 내부 매핑. **위치(우상단)·지속시간(4s)·애니메이션은 시스템 고정**(`Providers`의 `<Notifications/>`).
- 격리: `@mantine/notifications` import·Provider 배선은 `src/ui` 안에만. 바깥은 `notify.*`만 호출(헌법 7). **신규 의존성**: `@mantine/notifications`(공식 패키지).
- **경계(중요)**: 토스트는 **서버/작업 레벨 결과**(성공·작업 오류)만. **필드 검증 표시는 인라인 FormField 몫**(토스트로 안 띄움 — 「03」 검증 흐름). 둘이 경쟁하지 않는다.



> **부품은 어휘, 스키마는 문법**(「00」). 스키마 층은 부품이 아니다(원자/분자 분류에 안 들어감). LLM이 생성하고 Zod가 검증하는 "한 입력칸의 명세(데이터 shape)"다. **도메인은 오직 여기로만 들어온다(헌법 1번)** — `FieldSpec[]`이 곧 도메인의 진입로. 산출물 위치는 `src/schema/`(부품 격리 구역 `src/ui`와 분리 — 데이터 세계).

### FieldSpec (한 입력칸의 명세)

```ts
type FieldType = 'text' | 'number' | 'textarea' | 'select' | 'date' | 'checkbox' | 'lookup';
type FieldSpec = {
  name: string;                                  // 폼 값 키
  label: string;
  type: FieldType;
  required?: boolean;
  placeholder?: string;
  options?: { label: string; value: string }[]; // select 전용 (원자 options와 같은 형태)
  lookupKey?: string;                            // lookup 전용: 어떤 resolver를 쓸지 식별(문자열 — 함수 아님)
  span?: 1 | 2;                                   // FormSection columns=2일 때 한 줄 전체(기본 1)
  pattern?: string;                               // 정규식(데이터). buildZodSchema가 .refine(regex)로 검증. 잘못된 패턴은 가드.
  mask?: 'phone';                                 // 입력 마스킹(명명 enum, 임의 금지 — 큐레이션). FormSection이 onChange에서 적용.
  requiredWhen?: { field: string; filled: boolean }; // 조건부 필수(선언형, 함수 금지). .superRefine 교차검증.
  disabledWhen?: { field: string; filled: boolean }; // 조건부 비활성. FormSection이 토글.
};
```

- **함수(resolver 등 배선)는 스키마에 두지 않는다** — 직렬화 불가 + LLM이 생성 불가. lookup은 `lookupKey`(문자열)로만 가리키고, 실제 조회 함수는 코드 측(앱)에서 `resolvers`로 주입한다. 부품은 그게 "카카오 주소조회"인지 모른다 — "외부 조회"만 안다.
- **`lookup` 타입**: 외부 조회로 채워지는 입력(주소·우편번호·사업자조회 등). 입력칸이 행을 채우고 우측에 검색 버튼이 붙는다(FormSection이 조립).
- **`pattern`(정규식)은 우리 시스템의 유일한 열린 스칼라**다. 다만 *컴포넌트 styling prop*(헌법 5가 막는 대상)이 아니라 **스키마/데이터 층의 필드**라서, `label`·`placeholder`가 string인 것과 같은 성격(도메인이 합법적으로 들어오는 자리). `new RegExp(pattern)`이 잘못된 패턴에 throw하지 않도록 **가드**(`safeRegExp`).
- **`mask`와 `pattern`은 별개 축**: 마스킹(입력 중 하이픈)은 *보이는 형태* 변환, pattern은 *검증*. 같은 필드가 둘 다 가질 수 있다(전화처럼). 마스크 카탈로그(`_masks.ts`, src/ui)는 사람이 큐레이션으로만 늘린다(헌법 4) — LLM은 명명 enum에서 **고르기만**, 정규식 마스크를 쓰는 열린 구멍이 아니다.
- **`requiredWhen`/`disabledWhen`은 선언형**(함수 금지 — 직렬화·LLM 생성 가능). 술어는 현재 `filled` 하나(참조 필드가 채워졌는가). 예: `detail.requiredWhen={field:'address',filled:true}`, `detail.disabledWhen={field:'address',filled:false}`.
- FieldSpec은 직렬화 가능한 순수 데이터 → LLM이 생성하는 단위이자, Zod 검증의 입력이다.

### buildZodSchema (강제 3층의 스키마판)

`buildZodSchema(fields: FieldSpec[])` → Zod 객체 스키마를 도출한다. "LLM이 뱉은 데이터가 렌더러에 가기 전 런타임 관문". 타입별 매핑:
- number → `z.number()`, checkbox → `z.boolean()`, select → `z.enum(options.value들)`, text/textarea/date/lookup → `z.string()`.
- `required`면 그대로, 아니면 `.optional()`. required string은 `.min(1, "${label}은(는) 필수입니다")`.
- **`pattern`** → `.refine(v => v==='' || regex.test(v), …)`(빈 값은 통과 — 필수 여부는 별도). 잘못된 pattern은 무시(가드).
- **`requiredWhen`** → 마지막에 `.superRefine`으로 교차검증: 참조 필드의 filled 상태가 조건과 일치하고(=활성) `disabledWhen`으로 비활성이 아니면, 비어 있을 때 이슈 추가.
- `isFilled(v)` 단일 헬퍼(빈문자/null=false, boolean은 값 자체, 그 외 true)를 **조건부 술어와 검증이 공유**(단일 정의).
- **검증의 진실은 여기 하나**(원자에서 min/max·error를 벗긴 이유와 같은 단일 출처 — 「01」 입력군 횡단규칙 3). select enum이 옵션 값에서 자동 도출되어 원자·스키마 이중 정의가 없다.

> 타입 7종 + required + 정규식 + 조건부 필수까지 구현·검증(런타임 probe로 정규식 위반·조건부 발화·비활성 면제·패턴 가드 확인). min/max·교차필드(끝≥시작)·다중 술어는 데이터 계약 후 확장. 강제 3층(타입·린트)과 같은 사상의 *데이터 버전*이다.

## 조직(폼 조립) prop 규격 (✅ FormSection 확정 — 구현·검증 완료)

### FormSection

`FieldSpec[]`을 받아 **FormField + 입력 원자로 조립**하는 조직. 도메인을 모른다(fields를 받아 그릴 뿐). "신규 고객 등록"은 Modal + FormSection + 고객 스키마의 조립이지, "고객폼"이라는 부품이 아니다(헌법 1번).

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `fields` | 데이터 | `FieldSpec[]`(스키마) | ✅ |
| `values` | 기능 | `Record<string, unknown>`(controlled 폼 값) | ✅ |
| `onChange` | 기능 | `(name, value) => void` | ✅ |
| `columns` | 스타일 | `1 \| 2`(B안 — 스키마가 1·2열까지 지정, 임의 배치 금지) | ✅ |
| `resolvers` | 기능 | `Record<string, (apply:(patch)=>void)=>void>` (lookupKey→조회함수, 코드 주입 — 직렬화 밖. **patch로 여러 필드 한 번에**) | ✅ |
| `errors` | 콘텐츠 | `Record<string, string>`(Zod 결과, 필드명→메시지) | ✅ |
| 타입→원자 매핑·배치·gap·FormField 감싸기·마스크·조건부 토글 | — | 고정(코드) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

고정(코드): ① 타입→원자 매핑(text→TextInput, number→NumberInput, textarea→Textarea, select→Select, date→DatePicker, checkbox→Checkbox, lookup→TextInput+검색버튼). ② 모든 필드를 FormField로 감싸(label·error 분배). ③ 배치=1열 Stack / 2열 Grid(`span`으로 한 줄 전체 허용), gap=토큰. ④ **마스크**: `field.mask` 있으면 onChange에서 `applyMask` 적용(text 계열). ⑤ **조건부**: 매 렌더 현재 값으로 `effectiveRequired = required || requiredWhen 충족`, `effectiveDisabled = disabledWhen 충족` 계산 → withAsterisk·disabled 토글. ⑥ **lookup resolver**: `apply(patch)`로 여러 필드를 한 번에 채움(단일 set 폐기 — 경쟁 경로 차단). 가변(스키마): 어떤 필드가 몇 개·무슨 타입·라벨·필수·options·span·pattern·mask·조건부. **errors는 부모가 Zod로 판정해 주입** — FormSection은 검증하지 않고 분배만(검증의 진실은 스키마, 「01」).

## 템플릿 prop 규격 (✅ 페이지 템플릿 **9** + 폼 조립 조직 1 — 아래 prop 표는 ListPage·DetailPage·LedgerPage·HierarchyExplorer·HierarchyCollector 5종, 나머지 4종[CalendarPage·BoardList·BoardView·BoardWrite]은 `_catalog`·README 5절이 단일 출처. ※ PageGrid는 Bento로 승격돼 배치 프리미티브로 재분류 — 아래.)

> **신설 방침(v0.54.0~): 템플릿 층은 동결.** 새로 만드는 것은 page 위에 얹는 *위젯*(유기체)이고, 2-pane 골격·높이 배관·배너는 페이지가 조립한다(「03」 11-23). 템플릿이 페이지 전체를 소유하면 소비처가 그 안에 아무것도 못 끼운다는 것이 OptionSet 작업에서 확인됐다.

> 템플릿 = ERP 페이지의 골격. 도메인의 *형태*는 가지나 *내용*은 모르며, 스키마를 입력받아 작동(「01」 계층표). 도메인 0줄 — 도메인은 schema·rows·fields·actions(데이터)로만(헌법 1번). 5개 = ListPage·DetailPage(v0.2.0~) + HierarchyExplorer·PageGrid(v0.9.0). (FormSection은 폼 조립 *조직* — 위 절.) **공통 불변: AppShell 아래 모든 화면 = PageHeader(고정) + 본문. 템플릿이 PageHeader를 내장해 그 불변을 구조로 보장(→ 「03」 11-12).**

### ListPage (목록 골격)

헤더 행(카드 밖) + 테이블 elevated 카드("위젯 하나" = 한 구획·한 그림자). 헤더 우측 액션 배열 = [필터(좌) → secondary… → primary(우)].

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `schema` | 데이터 | `{ title; description?; primaryAction?; secondaryActions?; filterable?; toolbar?; columns; emptyState? }` | ✅ |
| `rows` | 데이터 | `DataTableRow[]` | ✅ |
| `status` | 스타일 | `'loading' \| 'empty' \| 'ready'` | ✅ |
| `sort` `onSortChange` | 기능 | DataTable 정렬(controlled) | ✅ |
| `page` `onPageChange` `totalPages` | 기능 | Pagination(controlled) | ✅ |
| `totalCount` | 데이터 | `number` → 푸터 좌측 "총 N건"(포맷 ko-KR 고정). number 있을 때만 표시 | ✅ |
| `onRowClick` | 기능 | 행 클릭 → 상세 이동 | ✅ |
| `onFilterClick` | 기능 | filterable일 때 필터 동작(없으면 자리만) | ✅ |
| 컨테이너 폭·헤더/툴바/카드 배치·gap | — | 고정(Container wide·Stack lg·Card elevated padding=none) | ❌ |

관습: **헤더는 제목만**(보조 desc 없음 — 상세와 대비). **toolbar(헤더↔표 사이)**는 닫힌 스키마 — `toolbar.segment`(유형 토글: B2C/B2B 등)를 SegmentedControl 원자로 controlled 렌더, 실제 필터링은 페이지 밖(DataTable sort와 동형). 임의 JSX 슬롯 금지 — 전부 데이터. 검색칸·유입경로 Select는 보류(추후 닫힌 필드로 확장, rule of three). **Pagination·totalCount는 목록 표준 구성요소**(자리는 규격이 보장)이되 실제 표시는 데이터(`totalPages`/`totalCount`)가 결정 — 렌더러가 데이터 없이 멋대로 만들지 않음(filterable 슬롯과 같은 논리). 푸터: 좌 "총 N건" + 우 Pagination(상단 Divider). `filterable`은 헤더에 필터 버튼 자리만.

### DetailPage (상세 골격)

PageHeader + 좌(정보 DescriptionList) / 우(폼 FormSection) 2분할(Grid columns=2). 우측 폼은 선택(`form` 없으면 정보만).

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `title` | 콘텐츠 | `string` | ✅ |
| `description` | 콘텐츠 | `string`(상세는 제목+보조정보. 내용은 스키마/도메인) | ✅ |
| `actions` | 기능(데이터) | `Action[]`(예: 목록으로·수정) | ✅ |
| `info` | 데이터 | `{ heading?; items: DescItem[]; columns? }`(읽기 — DescriptionList) | ✅ |
| `form` | 데이터 | `{ heading?; fields; columns?; values; onChange; resolvers?; errors?; footer? }`(입력 — FormSection, optional) | ✅ |
| 2분할 배치·카드·gap | — | 고정(Container wide·Grid 2·Card elevated lg) | ❌ |

관습: **상세=제목+보조정보** / 목록=제목만(PageHeader description 관습). 좌 정보·우 폼은 각각 elevated 카드(구획=그림자). form.footer 액션은 primary 기본(예: "프로젝트 생성").

### LedgerPage (✅ v0.12.0 — 돈 지표 골격)

"돈 지표" 페이지 골격(정산·매출·매입·미수금 등 *기간 스코프 금액 화면* 공통 해부). ListPage·DetailPage 형제, 도메인 0줄(스키마 주입). **4단 해부: ① 기간(`PeriodNavigator`) · ② KPI 밴드(`metrics` → Stat/SummaryCard) · ③ 분해표(`SegmentedControl`→`DataTable`→`TotalRow`) · ④ 드릴(`Drawer`).**

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `title` `description` `status` `actions` | 콘텐츠/데이터 | PageHeader(고정) — `title` 필수, `status` 배지 | ✅ |
| `period` | 데이터 | `{ label, onPrev, onNext, disabledPrev?, disabledNext? }`(PeriodNavigator 중앙 스트립 — `label`은 *포맷된 문자열*) | ✅ |
| `metrics` | 데이터 | `LedgerMetric[]` = `{kind:'stat',…}`(추세) \| `{kind:'summary',…}`(건수+금액) — 열 수 n≤4 자동(Grid equalRows) | ✅ |
| `breakdown` | 데이터 | `{ tabs, value, onChange, columns, rows, total?, onRowClick?, emptyState? }`(분해 토글→표→합계) | ✅ |
| `detail` | 데이터 | `{ opened, onClose, title, actions?, size?, content }`(행 클릭 드릴 — Drawer 우측 슬라이드, optional) | ✅ |
| 4단 배치·KPI 열·합계·드릴 | — | 고정(Container wide, KPI Grid equalRows, breakdown elevated 카드, detail=Drawer position right) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

관습: 합계·페이지네이션·빈상태는 데이터 유무로 결정(토글 prop 아님). `metrics`의 `stat`(추세형)·`summary`(건수+금액)는 `kind`로 자동 매핑. 포맷은 `fmtCurrency`/`fmtNumber` 단일출처 재사용. 드릴 상세(`detail.content`)는 도메인 라인 슬롯(Drawer children 계약 그대로).

### HierarchyExplorer (✅ v0.9.0 — 구현·검증 완료)

계층(디렉토리) 기반 마스터-디테일 표준 — 좌 Tree(+검색 바) / 우: 하위 분류 타일 + 직속 제품 목록(DataTable) **공존**. 멘탈모델: Unity Hierarchy / Figma Layers / VSCode Explorer. **페이지 템플릿(ListPage·DetailPage 동급): Container wide + 고정 PageHeader 내장.** 좌/우는 카드 안 수직 경계 구획(카드 2개 아님), 고정 높이 위젯(내부 스크롤·페이지네이션 없음).

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `title` `description` `status` `actions` | 콘텐츠/데이터 | PageHeader(고정) — `title` 필수 | ✅ |
| `nodes` `selectedId` `expandedIds` | 데이터 | 좌측 트리(controlled) | ✅ |
| `onSelect` `onToggle` `onAddRoot` `onRename` `onDelete` | 기능 | 트리 선택·쓰기 위임(최상위 추가=트리 헤더 ＋, 케밥=이름변경·삭제) | ✅ |
| `treeTitle` `editable` | 콘텐츠/스타일 | 트리 패널 헤더 / 편집 게이트 | ✅ |
| `selectedLabel` `objects` | 콘텐츠/데이터 | 선택 디렉토리 라벨 / `HierarchyObject[]`(직속 제품만 — 하위 분류와 공존) | ✅ |
| `onAddObject` `onAddChild` | 기능 | 우측 ＋ 드롭다운 [제품 추가 / 분류 추가] — 둘 다 완전 위임(소비처 모달). `onAddChild: (parentId)=>void`. editable+디렉토리 선택 시 항상 노출(EmptyState CTA와 배타) | ✅ |
| `searchQuery` `onSearchChange` `searchResults` | 기능/데이터 | 전역 검색(controlled) — `searchResults: HierarchySearchResult[]`(오브젝트+경로). 입력은 **좌측 분류 헤더 아래**, 결과는 우측 목록(분류 경로 컬럼)(v0.17.0) | ✅ |
| 2분할 배치·고정높이·스크롤·자동펼침 | — | 고정(좌 280 고정폭, 70vh 내부 스크롤, scrollbar-gutter, 페이지네이션 없음, 검색 바=좌측 분류 헤더 아래, 우측=하위 타일+제품 목록, 우측→좌측 조상 자동 펼침) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

관습: 우측 헤더는 인터랙티브 Breadcrumb(현재 디렉토리 경로 + 상위 이동, SectionHeader titleNode). **불변식: 제품은 정확히 한 디렉토리·직속만·단일 트리.** **한 디렉토리는 하위 분류(n+1)와 직속 제품을 동시에 가질 수 있다 — 잎/폴더 이분법 없음(아래 v0.11.0 '잎-전용 모델'은 폐기).** 좌측 Tree는 같은 부품의 editable on(편집)/off(보기) 양쪽을 수용. `HierarchyObject`(= ObjectCard 스키마 + id)로 우측을 그린다(자유 render 0).

> **우측 영역 재설계(v0.11.0):**
> - **폴더(상위 분류) 선택 → 하위 분류를 클릭 가능한 폴더 타일**(빈 상태 텍스트 폐기 — 폴더는 하위를 드러내야 로직상 맞다). 타일 클릭=드릴다운. 잎(자식 없음)만 품목 보유(잎-전용 모델).
> - **다중 뷰**: 카드(상세)/목록(요약) `SegmentedControl` 토글(object 보일 때만 노출 — 폴더엔 숨김). 품목 카드 = `ObjectCard layout`. **고정 4열**(`repeat(4)`=span 3, 아이템 수로 동적 분할 안 함). 폴더 타일도 같은 4열.
> - **전역 검색**: 헤더 좌측 도구. 쿼리 시 결과 모드(각 결과에 경로 Breadcrumb, 클릭=집 점프+검색 해제). 필터는 소비처(`searchResults` 주입). 중복은 불변식상 0.
> - **자동 펼침**: 우측에서 노드로 점프(폴더 타일·브레드크럼·검색결과) 시 좌측 트리의 접힌 조상을 자동으로 펼친 뒤 선택(`onToggle`은 접힌 것만 가드).
> - **헤더 안정성(→「03」 헤더 안정성 원칙):** **3존(v0.14.0)** — `[breadcrumb 제목존: flex:1 말줄임] [도구: 고정] [품목 추가 액션: 우측 끝 고정]`. 제목(경로)이 길이 변동을 흡수해 도구·버튼이 제목 길이·조건부 토글에 안 밀림. 품목 추가는 품목 있을 때만(빈 잎은 EmptyState 중앙 CTA가 대신 — 배타·중복 제거).

> **검색 좌측 이전 + 목록 단일화(v0.17.0):**
> - **검색 입력 위치: 우측 헤더 → 좌측 분류 헤더 아래**(Tree `toolbar` 슬롯 신설). 전역 검색은 트리를 관통하는 행위라 트리 옆이 정합(dev 박물관과 동형). 우측 헤더는 '선택 분류 콘텐츠' 역할만.
> - **카드/목록 뷰 토글 폐기 → 목록(DataTable)만.** 잎 품목·검색 결과 모두 목록. 카드(12×6 벤토)·우측 ObjectCard 사용은 잠정 보류(가성비, 복원 백로그).
> - **검색 결과 = 목록 + `분류` 경로 컬럼**(`부자재 › 경첩`). 행 클릭=onClick(상세, 소비처 조립). 건수 `N건`은 헤더 우측 `controls`에 고정(제목존 flex:1이 흡수 → 폭 밀림 0).
> - **목록 컬럼(v0.17.1)**: 품목 · (검색 시)분류 · 핵심값 · **비고** · 상태 · 액션. `headline.note`(값-국소 배지, 예 '변경요청중')는 카드의 '값 옆 배지'를 표에선 별도 `비고` 배지 컬럼으로(`_cells` 셀 어휘 무변경, 기존 badge 타입 재사용).

### HierarchyCollector (✅ v0.42.0 — 계층 *수집* 골격, HE의 짝)

계층 카탈로그에서 수량과 함께 골라 담는 "작성 면". **HierarchyExplorer의 짝 — 같은 계층 데이터, 반대 목적**(HE=계층 *관리*, HC=계층에서 *수집*·누적·횡단). 정당화: "모든 분류를 보며 횡단 + 수량 누적"이 HE+버튼으로 안 됨 — 횡단·누적이 새 정체성. **페이지 템플릿(Container wide + 고정 PageHeader).**

| prop | 범주 | 값 | 노출 |
|---|---|---|---|
| `title` `description` `actions` | 콘텐츠/데이터 | PageHeader + 풋터 액션(renderAction) | ✅ |
| `catalogs` | 데이터 | `CollectorCatalog[]`(카탈로그별 분류 트리) — 책갈피로 횡단 | ✅ |
| `products` | 데이터 | `CollectorProduct[]`(카탈로그·경로·단가·썸네일) | ✅ |
| `cart` `onCartChange` | 데이터/기능 | 담은 내역(controlled, `CollectorCartItem[]`) | ✅ |
| `showAmount` `onProductClick` `emptyCart` | 스타일/기능/콘텐츠 | 금액 표시 / 행 클릭(상세 위임) / 빈 카트 | ✅ |
| 2분할·책갈피·cascader·검색·카트 well·고정합계·@container 강등 | — | 고정(좌 브라우즈 / 우 sunken well, 카트 카탈로그=▾ 셀렉터, 합계 well 하단 고정, 폭 강등=container query) | ❌ |
| `className` `style` | — | 규칙 3 | ❌ |

설계 결정: 책갈피·cascader·검색·카트칩=**템플릿 종속 raw 컨트롤**(→ 01 「4-D」 — 아톰이 width·체크 못 닫음). 공유 부품(NumberStepper·LineItemList)은 재사용. 카트 카탈로그 토글=**▾ 컨텍스트 셀렉터**(메뉴 per-카탈로그 카운트 + 트리거 점 — 조사: 320px·6+칩은 드롭다운이 정답). 담긴 행 강조=**틴트 + 코너 체크 + 볼드**(4큐 과잉→절제, 조사). **윤곽→음영 2축**(위젯=raised, 카트=sunken well). 반응형=**@container**(소비처 무관, → 01 폭-적응).

### PageGrid → Bento (배치 프리미티브로 재분류, v0.41.0)

> **PageGrid는 Bento로 승격돼 *배치 프리미티브*(Stack·Grid 동급)가 되었다 — 템플릿 아님.** 페이지 본문 격자는 도메인 골격이 아니라 *공간 배열 단위*라는 재분류(행동 보존, 이름만 `Bento`/`Bento.Tile`). 아래 규격은 그대로.

페이지 공간을 닫힌 격자로 — 위젯이 정수 칸 점유(iOS 홈 / Bento). **Bento 본질 = 가변 높이 불허:** 셀 높이는 고정(`ROW_UNIT`), 위젯은 닫힌 n×n footprint를 colSpan·rowSpan으로 *선택*. 내용이 넘치면 위젯 내부에서 처리(Tile `overflow:hidden`).

| prop | 분류 | 값 | 위치 | 노출 |
|---|---|---|---|---|
| `columns` | 값 | `2 \| 3 \| 4 \| 6 \| 12`(닫힌 열 수) | PageGrid | ✅ |
| `gap` | 값(토큰) | `'sm' \| 'md' \| 'lg'` | PageGrid | ✅ |
| `colSpan` | 값+방식 | `1..columns`(위젯 가로 칸) | `PageGrid.Tile` | ✅ |
| `rowSpan` | 값+방식 | `1 \| 2 \| 3`(위젯 세로 칸) | `PageGrid.Tile` | ✅ |
| 셀 높이(ROW_UNIT)·overflow | — | 고정(140px 잠정 — 가변 높이 불허, Tile overflow:hidden) | — | ❌ |
| `className` `style` | — | 규칙 3 | 양쪽 | ❌ |

고정: **1×1 = 절대 높이(ROW_UNIT) × 파생 폭(콘텐츠폭 ÷ columns)** — columns 선택이 곧 고정 높이에 대한 1×1 모양 선택. ROW_UNIT(현 140 잠정)은 가장 작은 자기완결 위젯(SummaryCard) 실측 높이를 8px 베이스라인에 스냅해 확정 예정(**「03」 백로그**). 1×1보다 얇은 컨트롤(검색·필터·전환탭)은 타일이 아니라 PageHeader↔그리드 사이 **컨트롤 스트립**으로(→ 「03」 11-12 화면 해부도). raw CSS grid는 Calendar 7열과 동류의 명시 예외.

## 검증 방법 (프론트 3층)

1. **타입 체크** (제일 싸고 빠름): `npx tsc --noEmit` — 실행 안 하고 타입만 검사. 오타·규칙 위반을 잡음. **theme·타입의 1차 관문.**
2. **눈으로 보기**: `npm run dev` → 브라우저에서 실제 모습 확인. 색·간격 같은 *값*의 검증은 여기서. (부품 모아보는 Storybook은 나중에.)
3. **자동 테스트** (Vitest 등): 지금은 불필요. 로직이 쌓이면 그때.

> 현재 단계: 1층(`tsc --noEmit`) + 2층(`npm run dev`)이면 충분.

### 9-1. 검증 태도: "거짓 통과"를 경계하라

> **"통과(에러 0)"는 두 가지를 뜻할 수 있다 — "검사했고 문제없음" 또는 "검사 자체를 안 함". 둘 다 0으로 보인다.**

실제 사건: 첫 `npx eslint .`가 에러 0으로 통과했는데, 규칙이 잘 작동해서가 아니라 flat config가 .ts/.tsx를 *아예 안 훑어서*였다(거짓 통과). 그냥 믿었으면 강제가 작동한다고 착각한 채 위에 다 쌓았을 것이다.

**교훈: 통과를 믿지 말고, 일부러 틀린 걸 넣어서 "막히는지"를 확인하라.**
- probe 파일에 일부러 위반을 심는다: ① src/ui 바깥에서 Mantine 직접 import → 에러 나야 함 ② src/ui 안에서 같은 import → 안 나야 함 ③ 아무 데나 hex 리터럴 → 에러 나야 함 ④ Button.tsx(래퍼)에 hex → *여전히* 에러 나야 함(hex 예외가 theme.ts 하나로 정확히 좁혀졌는지).
- "에러가 안 났다"가 아니라 "틀린 걸 넣었더니 에러가 났다"를 봐야 진짜 작동 확인이다. 자물쇠는 밀어봐야 잠긴 걸 안다.

위 4개 probe로 강제 3층(그물 1·2)이 실제 발화함을 확인했다. ✅

---

