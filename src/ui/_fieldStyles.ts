// 입력칸 테두리 통로. 평소엔 --border-field, FormField(분자)가 에러 시 --field-border를
// 자식 영역에 깔면 그 값으로 덮인다. 입력칸은 자기가 에러인지 모른다(역할 변수 통로).
//  ※ 폴백이 --border-default였을 때는 입력칸이 *구분선*과 같은 색이었다(1.27:1 — 칸과 선이 구분 불가).
//    --border-field는 입력칸 경계만을 위한 역할이다(theme.ts border.field, 06 §1-3).
export const fieldBorder = { borderColor: 'var(--field-border, var(--border-field))' };

/**
 * 날짜 표기 — **패키지 단일 출처.**
 *
 * `_cells.fmtDate`(표 셀)가 먼저 이 표기를 정했다. 입력칸이 다른 표기를 쓰면 **같은 값이 한 화면에서
 * 두 얼굴**이 된다(폼의 입력칸 `2026년 8월 5일` 바로 아래 표가 `2026-08-05`).
 *
 * 왜 한글 표기가 아닌가: 날짜는 *읽는 값*이 아니라 **대조하는 값**이다 — 옆 필드·표·다른 행과 맞춰본다.
 * 고정폭이라 입력칸 폭이 안 출렁이고(`2026년 8월 5일` 10자 vs `2026년 12월 25일` 12자), 표에서 정렬이 선다.
 * 드롭다운 안쪽(`8월`·`월 화 수`)은 DatesProvider의 ko 로케일이 한글로 만들어주므로,
 * "영어로 나온다"는 문제는 이 형식 선택과 **무관하게** 해결된다.
 *
 * ⚠ prop으로 열지 않는다. 소비처마다 날짜 표기가 갈리면 같은 값이 화면마다 다르게 읽힌다.
 */
export const DATE_FORMAT = 'YYYY-MM-DD';
