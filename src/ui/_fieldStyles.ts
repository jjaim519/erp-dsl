// 입력칸 테두리 통로. 평소엔 --border-field, FormField(분자)가 에러 시 --field-border를
// 자식 영역에 깔면 그 값으로 덮인다. 입력칸은 자기가 에러인지 모른다(역할 변수 통로).
//  ※ 폴백이 --border-default였을 때는 입력칸이 *구분선*과 같은 색이었다(1.27:1 — 칸과 선이 구분 불가).
//    --border-field는 입력칸 경계만을 위한 역할이다(theme.ts border.field, 06 §1-3).
export const fieldBorder = { borderColor: 'var(--field-border, var(--border-field))' };

/**
 * 날짜 표기의 **기본값**. 안 주면 이걸 쓴다 — 「우리가 고른 한 표기」가 아니라 «예시»다.
 *
 * ⚠ 이건 **표현이고, 표현은 소비처가 정한다**(오너 결정 2026-08-17). 날짜 하나에 걸린 변수가
 *   너무 많다 — 연도를 보일지(같은 해 안에서만 쓰는 화면이면 `2026`은 잉크 낭비다), 요일을 붙일지,
 *   구간을 어떻게 이을지. 그건 그 화면을 아는 쪽만 안다. 그래서 각 날짜 입력이 `format` prop을 연다.
 *   (전에는 여기서 못박고 "prop으로 열지 않는다"고 적어 두었다. 그 근거는 «한 화면 두 얼굴»이었는데,
 *   같은 화면 안에서 표기를 맞추는 것도 결국 소비처가 할 일이다 — 패키지는 그 화면을 못 본다.)
 *
 * 왜 기본값이 ISO인가: 날짜는 *읽는 값*이 아니라 **대조하는 값**이라(옆 필드·표·다른 행과 맞춰본다)
 * 고정폭이 안전한 기본이다 — 입력칸 폭이 안 출렁이고(`2026년 8월 5일` 10자 vs `2026년 12월 25일` 12자)
 * 표에서 정렬이 선다. `_cells.fmtDate`(표 셀)도 같은 기본을 쓴다.
 * 드롭다운 안쪽(`8월`·`일 월 화`)은 DatesProvider의 ko 로케일이 그리므로 이 형식과 무관하다.
 */
export const DATE_FORMAT = 'YYYY-MM-DD';
